# Progress

Append-only ledger of the `/goal` run (`GOAL.md`). One row per finished or
blocked task, written by the host session right after the task's
`tasks/README.md` row changes (`goal/EXECUTION.md` §5). Authoring runs for
M6..M12 folders are recorded as `<milestone>-00 authoring`. Attempts and
lane fallbacks (D-057) go into the result cell, never into separate rows.

| Task | Lane | Path | Result | Evidence | When (UTC) |
| --- | --- | --- | --- | --- | --- |
