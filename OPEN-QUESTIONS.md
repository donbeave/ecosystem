# Open questions

None. Every question raised so far is settled in its domain owner, with its
D-NNN definition in `SPEC.md`. The full text of Q-001..Q-025 and each
closing outcome is in `QUESTIONS.md`; that file is
where any `Q-0nn` reference in this repository resolves. New questions are
added here only with a recommended answer or a note on what fact is missing,
and appended to `QUESTIONS.md` at the same time. Settling one updates its
domain owner, the `SPEC.md` registry, and its `QUESTIONS.md` entry together;
nothing here may block
execution (D-050, D-053).
