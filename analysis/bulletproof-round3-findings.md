# Bulletproof round 3 findings (unapplied; workflow stopped 2026-08-27)

Rounds 1–2 applied 111 fixes (commits `62d52fe`, `f2aa595`). Round 3 finders below returned findings that were NOT verified or applied. Each item: title, file, where, risk, fix, severity.

## R3-01 [blocker] jackin's own `ci.yml` routes `pull_request` `ci-required` to the offline velnor runner; no task switches it, so the M11-01a merge can never go green

- File: `ROADMAP.md`
- Where: M1-02 row (line 130, sentence "CI for jackin on `feat/managed-execution` uses GitHub-hosted runners (D-064)") and M11-01a row (line 358, "Ensure the rolling jackin PR is green on `ci-required` and `DCO` (fix on the branch if not …)"); DECISIONS.md D-064 consequence 1
- Risk: Verified live: `jackin-project/jackin/.github/workflows/ci.yml:168` is `runs-on: … github.event_name == 'push' && 'ubuntu-26.04' || ['self-hosted','velnor-target-mvp']` and lines 104/126/148 pass `lane: … || 'velnor'` for `pull_request`; the `protect-main` ruleset requires `ci-required` + `DCO` with `bypass_actors: []`. D-089 fixed exactly this for the-architect (M1-13) but jackin got only a prose sentence in M1-02 with no step and no verify. The rolling PR opened in §6a (M2-08) therefore fails/queues `ci-required` for the whole run, and M11-01a's `gh pr checks --watch --fail-fast` never reports green. `fix on the branch if not` is a generator-rendered DO-NOT-EDIT file plus a fleet-contract job, far outside an S-sized merge task; three D-063 rounds → `exhausted: M11-01a` → M11-02, M11-03, M11-04, M12 unreachable → BLOCKED.
- Fix: In ROADMAP.md M1-02 scope replace the D-064 sentence with the M1-13 wording: "First jackin commit on `feat/managed-execution`: in `.github/workflows/ci.yml` make the `ci-required` job and the three `lane:` inputs resolve to GitHub-hosted `ubuntu-26.04` for `pull_request` events (D-064, D-089; generator-rendered file, override intentional); confirm with `gh workflow view ci.yml -R jackin-project/jackin`." Add to M1-02 verify: "`git show origin/feat/managed-execution:.github/workflows/ci.yml | grep -c velnor-target-mvp` is 0 on the `pull_request` path". In M11-01a delete "fix on the branch if not" and add the precondition "the `pull_request` run on the rolling PR head already shows `ci-required` success (`gh pr checks <n> --json name,state`)". Update D-064 consequence 1 accordingly.

## R3-02 [blocker] Two manifest schema bumps on one rolling PR (M3-02 v1alpha7, M4-01 v1alpha8) fail jackin's CI-enforced one-bump-per-PR rule at M11-01a

- File: `ROADMAP.md`
- Where: M4-01 row (line 214, "manifest `[prompt]` or launch env `JACKIN_INITIAL_PROMPT`, one schema bump, Q-021") and M3-02 row (line 193); SPEC.md lines 337-339 ("otherwise as two consecutive versions (Q-021 adopted)")
- Risk: jackin `PRERELEASE.md:23` requires exactly one version bump per PR and it is enforced by `cargo xtask schema-check --base <merge-base>` in the `policy` partition of `xtask ci` (`crates/jackin-xtask/src/ci.rs:216`, `schema.rs:87-131`): the check diffs base vs head version and demands `fixtures/from-<base>/meta.toml` with `target_version = "<head>"`. The only PR that ever merges to jackin `main` is the rolling one at M11-01a; with base `v1alpha6` and head `v1alpha8`, `from-v1alpha6/meta.toml` declares `v1alpha7` → schema-check fails → `ci-required` red → M11-01a cannot merge, and the sanctioned remedy (rewriting history) is forbidden. SPEC's "two consecutive versions" wording is exactly the pattern PRERELEASE forbids.
- Fix: ROADMAP M4-01 scope: replace "(manifest `[prompt]` or launch env `JACKIN_INITIAL_PROMPT`, one schema bump, Q-021)" with "(launch env `JACKIN_INITIAL_PROMPT` only — no manifest field and no schema bump: the rolling PR may carry exactly one manifest bump, M3-02's `v1alpha7`, because `cargo xtask schema-check` diffs against `main` at merge time)". SPEC.md 337-339: replace "when M3-02 and M4-01 ship together, otherwise as two consecutive versions" with "`default_agent` is the single manifest bump of the run (`v1alpha7`); no other task bumps a versioned schema on `feat/managed-execution`". Add to M11-01a verify and to M3-02 verify: "`cargo xtask schema-check --base origin/main` prints `1 schema bump(s), all required artifacts present`".

## R3-03 [major] termrock's AGENTS.md/CLAUDE.md and CONTRIBUTING.md forbid branches and PRs; M10-02 only appends a clause, leaving the contradicting rule the agent reads first

- File: `ROADMAP.md`
- Where: M10-02 row (line 318, "add the agent-authored-changes clause to termrock `CONTRIBUTING.md`") and its verify (`grep -qi 'agent-authored'`)
- Risk: termrock `AGENTS.md:252-254` (symlinked as `CLAUDE.md`, the file crew-builder loads in the checkout) says "All TermRock work happens directly on `main`. Do not create feature branches or pull requests … push `main` immediately", and `CONTRIBUTING.md` para 2 says "never create or publish another branch, and never open a pull request … verify local `HEAD` equals `origin/main`". An appended clause leaves both instructions in force. A crew-builder that obeys them pushes to `main` (rejected: termrock `protect-main` requires `ci-required`/`DCO` on the commit) or refuses the branch; M10-02 and M10-03 loop through D-063 and exhaust, and the verify grep passes even though the contradiction remains.
- Fix: M10-02 scope: "First commit on `feat/managed-execution`: rewrite the trunk-only rule — in `CONTRIBUTING.md` replace paragraph 2 ("All work is trunk-only … never open a pull request …") and in `AGENTS.md` lines 252-254 replace "All TermRock work happens directly on `main`. Do not create feature branches or pull requests …" — with the agent-authored-changes clause (branch `feat/managed-execution`, PR to `main`, `crew-reviewer` review requested, agent merges when the task says so; D-047, D-055)." Verify: add `! git grep -n -i -E 'never open a pull request|Do not create feature branches' origin/feat/managed-execution -- CONTRIBUTING.md AGENTS.md`.

## R3-04 [major] DCO pre-push gate counts merge commits: after every `git merge origin/main` merge-back the shared branch fails the gate forever

- File: `goal/EXECUTION.md`
- Where: §4 DCO rule (lines 350-359: `test "$(git log origin/main..HEAD --format=%B | grep -c '^Signed-off-by:')" -eq "$(git rev-list --count origin/main..HEAD)"`) and §4 branch rule lines 336-339 (`git fetch origin main && git merge origin/main && git push`)
- Risk: The merge-back mandated after each squash merge (the-architect at M1-13 and M3-02, jackin at M11-01a) creates a merge commit with no `Signed-off-by:` on `feat/managed-execution`; it stays in `origin/main..HEAD` because the squash left the branch's commits outside `main`. From then on the count is off by one for every push in that repository (M3-02 on the-architect right after M1-13; M11-03/M12 tasks on jackin), the gate blocks the push, and the only sanctioned repair (`git rebase --signoff` + `--force-with-lease`) rewrites a branch that in jackin has many writers and is explicitly not the exception.
- Fix: §4 branch rule: change the merge-back to `git merge --no-ff --signoff origin/main` (both the the-architect sentence and the "if one is, the same task merges `origin/main` back" sentence; same in ROADMAP M1-13, M3-02, M11-01a). §4 DCO rule: change both sides of the test to `git log --no-merges origin/main..HEAD --format=%B | grep -c '^Signed-off-by:'` and `git rev-list --no-merges --count origin/main..HEAD`, and add "merge commits are exempt (the DCO check ignores them)".

## R3-05 [minor] "rebase on `origin/main` first when the PR is behind" on a shared branch requires the forbidden `--force` push (strict up-to-date policy)

- File: `ROADMAP.md`
- Where: M1-13 row (line 145, "`gh pr merge <n> --squash` (rebase on `origin/main` first when the PR is behind)") and M11-01a row (line 358, "rebase on `origin/main` when behind"); goal/EXECUTION.md §4 branch rule lines 341-343
- Risk: Both `jackin` and `jackin-the-architect` `protect-main` rulesets have `strict_required_status_checks_policy: true` (verified via `gh api repos/<r>/rules/branches/main`), so a PR behind `main` must be updated before merge. Rebasing `feat/managed-execution` rewrites every commit and the push is rejected without `--force`, which GOAL.md and §4 forbid (the DCO `--force-with-lease` exception covers only missing trailers on the-architect). The session either violates the rule or stalls in a fetch/rebase/push loop until exhaustion.
- Fix: Replace both "rebase on `origin/main`" phrases with "`git fetch origin && git merge --no-ff --signoff origin/main && git push` when `gh pr view <n> --json mergeStateStatus --jq .mergeStateStatus` is `BEHIND`, then wait for the new `ci-required` run". Add the same sentence to EXECUTION §4 after "Merges to a protected `main` … `--admin` never works".

## R3-06 [minor] "No Homebrew tap publish in this run" contradicts the `preview.yml` push run that M11-01a must watch and whose result its verify asserts

- File: `DECISIONS.md`
- Where: D-055 (line 909-911, "No jackin release and no Homebrew tap publish happens before M11"), D-090 (4) ("no release or tap publish in this run"); ROADMAP.md M11-01a row line 358 ("No tag, no release, no Homebrew tap") versus the same row's `gh run watch` on `preview.yml` and verify `gh release view preview … targetCommitish`
- Risk: `jackin/.github/workflows/preview.yml` runs on every push to `main` and its `publish-preview` job (concurrency group `homebrew-tap-publish`) updates the `preview` GitHub release and the `homebrew-tap` `jackin-preview.rb` formula; the squash merge in M11-01a triggers it unconditionally and the task's verify depends on it. A session applying "no tap publish" literally (cancelling the run or gating `publish-preview`) breaks M11-01a's own verify and M11-02's validator download; the contradiction is a D-063 loop trigger with no correct resolution in the documents.
- Fix: Reword D-055/D-090 and M11-01a to: "No tag, no `release.yml` run, no stable formula in this run; the rolling `preview` release and the `jackin-preview` tap formula are refreshed automatically by `preview.yml` on the M11-01a merge, and that refresh is required (M11-02's validator comes from it)." Add to M1-02a: "`jackin-preview` stays uninstalled after M11-01a even though the formula is refreshed."

## R3-07 [minor] Rolling PR and squash merge ignore jackin's `.github/AGENTS.md` PR contract (template body, PR number in squash title, `jackin-pr-trailers` body)

- File: `goal/EXECUTION.md`
- Where: §5 step 6a (`gh pr create --title feat/managed-execution --body "Rolling PR; reviewed per milestone (D-055, D-074)"`) and ROADMAP.md M11-01a (`gh pr merge <n> --squash` with no `--subject`/`--body-file`)
- Risk: `jackin/.github/AGENTS.md:8-15` requires PR bodies built from `.github/PULL_REQUEST_TEMPLATE.md` (including the `jackin-dev pr sync <PR>` block), squash titles that keep the PR number, and squash bodies generated by `jackin-pr-trailers`. the-architect agents and reviewers read that file inside the checkout and will rewrite or dispute the host-created PR (a D-063 analysis names it a repository-rule violation), and M11-01a's default squash body strips the trailers the repository's tooling expects. Not CI-enforced today, but it is a standing source of agent refusals and rework on every jackin review task.
- Fix: §6a: create the PR with `--title "feat: managed execution (rolling, D-074)" --body-file <(sed "s/<PR_NUMBER>/TBD/" .github/PULL_REQUEST_TEMPLATE.md)` and, after creation, `gh pr edit <n> --body-file` with the number filled in. M11-01a: `gh pr merge <n> --squash --subject "feat: managed execution (#<n>)" --body-file <(jackin-pr-trailers …)` per `.github/AGENTS.md`, with the exact trailer command recorded in `tasks/M11-01a/`.

## R3-08 [blocker] M3-04 host verify needs a daemon-dispatched container, which only M3-05 (a dependent of M3-04) can produce — verify/dependency cycle

- File: `ROADMAP.md`
- Where: §2 M3 table, row M3-04 (line ~196), verify column: "host (D-061, D-091): the task files tasks/M3-04/status-before.json and status-after.json taken around a `jackin daemon restart` it performs while a managed container runs on a scratch issue"; row M3-05 depends_on = "M3-02, M3-03, M3-04, M2-04, M1-13"
- Risk: M3-04 depends only on M3-01. At M3 wave 2 nothing dispatches an issue to a container (dispatch is M3-05, which depends_on M3-04 being `done`). The host part of M3-04's verify therefore cannot be satisfied by any attempt: 3 attempts → `exhausted: M3-04` → M3-05 never runnable → whole M3..M12 chain stays `ready`, run ends BLOCKED at M3 wave 2 with a defect the human cannot fix by hand because the enabling code does not exist yet.
- Fix: Split the live check out of M3-04. In ROADMAP.md row M3-04 replace the host sentence with: "host (D-061): none; the live restart/adoption check is M3-07's." In row M3-07 verify append: "host (D-061, D-091): while the scratch issue's container runs, the host session performs `jackin daemon restart` and files `tasks/M3-07/status-before.json` and `status-after.json`; `jq -e` shows both bind the same container id to the issue (M3-04 adoption)." Mirror in DECISIONS.md (new D-0xx amending D-072/D-091) and in the M3-04/M3-07 TASK.md text that M1-01 copies. Alternative if the check must stay in M3-04: make M3-04 depend on nothing new but write the host part as a container launched by `jackin load the-architect probe-M3-04 --agent claude` labelled by hand with the M3-04 label API — but that tests nothing real; splitting is correct.

## R3-09 [blocker] M8-01 is an explicit early-start task (depends_on M1-03) but its verify reads tasks/M3-07/scratch-repo.txt, an artifact of M3-07 which is not a dependency

- File: `ROADMAP.md`
- Where: §2 M8 table row M8-01 (line ~283): depends_on = "M1-03"; verify: "the `jackin-project` listing contains the repository named in `tasks/M3-07/scratch-repo.txt`"; §3 waves table row M6..M12: "M8-01 (L6) depends only on M1-03 and may start early"; goal/EXECUTION.md §3 line ~192: "M6-01, M8-01, M10-02, M10-03 after M1-12 is `done`"
- Risk: L6 is idle in most M2 waves (only M2-08 uses it, at the end), so the priority rule ("early-start tasks fill only slots that would otherwise idle") dispatches M8-01 right after M1-12 — during M2, before M3-07 exists. `cat tasks/M3-07/scratch-repo.txt` fails deterministically, verify fails 3 times, `exhausted: M8-01` is filed, and the run can no longer end COMPLETE (an open PREFLIGHT-DEFECTS row forces the BLOCKED outcome at the end, and M8-02/M8-03/M9/M11-02+ stay `ready`). Guaranteed human round trip.
- Fix: Either (a) add the edge: ROADMAP.md row M8-01 depends_on → "M1-03, M3-07"; §3 mermaid add `M3-07 --> M8`; §3 waves row M6..M12 change to "M8-01 (L6) depends on M1-03 and M3-07 and may start as soon as M3-07 is `done`"; goal/EXECUTION.md §3 keep M8-01 in the early-start list but the depends_on now gates it; or (b) keep the early start and drop the scratch-repo clause from M8-01's verify, moving "the `jackin-project` listing contains the repository named in `tasks/M3-07/scratch-repo.txt`" into M8-03's verify. (a) is the smaller change and keeps the check where the App token is minted.

## R3-10 [major] Proof-run tasks have no dependency edge to the dispatcher (M3-05) or the scratch repository (M3-07), and M3-05 has none to the polling tick (M2-03); with M6..M12 waves 'derived from depends_on' they become runnable while their real input is blocked or waiting

- File: `ROADMAP.md`
- Where: §2 rows: M3-05 depends_on "M3-02, M3-03, M3-04, M2-04, M1-13" (no M2-03; its host verify needs the tick to detect the delegated issue); M4-04 host verify "launch from a scratch issue on the host daemon" with depends_on "M4-01, M4-02, M2-04, M1-13"; M4-06, M5-06, M6-03, M7-04, M8-03, M9-02, M10-05, M11-04, M12-03 all read `tasks/M3-07/scratch-repo.txt` or need a live dispatch but none lists M3-07 (or M3-05) and none has M3-05 as a transitive ancestor except M5-06 (via M5-05) — e.g. M6-03 → M6-02 → M5-01 → M4-04 → M4-01 → M3-01; M7-04 → M7-03 → M7-02 → M7-01 → M6-02; M8-03 → M8-02 → M7-01 (M8-01 → M1-03)
- Risk: goal/EXECUTION.md §3 derives M6..M12 waves from depends_on and defines runnable purely by depends_on + M1-12 + caps. If M3-05 or M3-07 is `blocked`/`waiting`/exhausted (a Codex-lane build failure, quota wait, the scratch-repo owner issue), M4-04, M4-06, M6-03, M7-04, M8-03… are still runnable, each burns three attempts against a verify that cannot pass, each lands as a separate `exhausted:` row, and the intended behaviour ("dependents of a blocked row stay ready, not runnable") never engages. Same for M3-05's own live smoke if M2-03 is late/blocked (M2-03 is in M2 wave 3 beside M2-04; M3-05 needs only M2-04).
- Fix: Add the edges in ROADMAP.md §2 depends_on cells, §3 mermaid, and the waves text: M3-05 += M2-03; M4-04 += M3-07 (its host verify needs a dispatchable scratch issue with a clonable `repo:`); M4-06 += M3-07; M5-06 += M3-07; M6-03 += M3-07; M7-04 += M3-07; M8-03 += M3-07; M9-02 += M3-07; M10-05 += M3-07; M11-04 += M3-07; M12-03 += M3-07. Mermaid additions: `M2-03 --> M3-05`, `M3-07 --> M4-04`, `M3-07 --> M4-06`, `M3-07 --> M5-06`, `M3-07 --> M6`, `M3-07 --> M7`, `M3-07 --> M8`, `M3-07 --> M9`, `M3-07 --> M10`, `M3-07 --> M11`, `M3-07 --> M12`. Record as a D-088-style graph amendment in DECISIONS.md; M1-01 copies the new depends_on into task.toml.

## R3-11 [major] GOAL prompt's Runnable formula has no milestone gate, while goal/EXECUTION.md §3 restricts cross-milestone starts to four named tasks — two incompatible schedulers

- File: `GOAL.md`
- Where: GOAL.md prompt line 43: "Runnable = every depends_on row `done`, lane caps allow (D-071; throwaway loads count) and, for M2+ ids except M3-01, M3-03, M4-02, M4-03, M1-12 `done` (D-088)" versus goal/EXECUTION.md §3 lines ~170-198: "M1..M5 waves exactly as ROADMAP.md §3 lists them" and "Tasks from a later milestone start early where ROADMAP.md §3 says they may (M3-01, M3-03, M4-02, M4-03 …; M6-01, M8-01, M10-02, M10-03 …)"
- Risk: By the prompt's formula, M3-02 is runnable during M2 wave 2 (M3-01 done), M4-01 during M3, M2-05 right after M1-12, M5-02..M5-05 while M4-06 is still proving, etc.; by EXECUTION §3 they are not. The session (and the /goal judge) reads the prompt first and treats it as the rule; the wave tables then only order what is already running. Consequences: the ~/.claude cap of 2 and the Codex homes are consumed by later-milestone tasks while the critical path (M2-04 → M3-05 → M3-07) waits (the priority rule only demotes the *named* early-start tasks, not M3-02/M4-01), and the missing-edge problem above is fully exposed. Conversely, under strict waves M1-07/M1-10 wait for the long M1-13 in wave 5b without any edge requiring it.
- Fix: Pick one and state it in both files identically. Recommended text for GOAL.md line 43: "Runnable = every depends_on row `done`, lane caps allow (D-071; throwaway loads count), for M2+ ids M1-12 `done` (D-088), and the task's milestone is reached: every non-review task of every earlier milestone is `done`, `blocked`, or `waiting`, except the early-start tasks M3-01, M3-03, M4-02, M4-03 (before M1-12) and M6-01, M8-01, M10-02, M10-03, M6-05 (after M1-12), which need only their depends_on." In goal/EXECUTION.md §3 replace "M1..M5 waves exactly as ROADMAP.md §3 lists them" with "waves are a priority order, not a barrier: a task starts when the Runnable rule of GOAL.md holds" so M1-07 is not held behind M1-13. Add the same sentence to ROADMAP.md §3 under "Parallel waves per milestone".

## R3-12 [major] M10-03 has depends_on '—' but is gated on M10-02's CONTRIBUTING commit only in prose (D-088(5) says 'no new edge'); the mechanical Runnable rule launches both together

- File: `ROADMAP.md`
- Where: §2 M10 table row M10-03 (line ~319): depends_on "—"; §3 waves row M6..M12: "M10-03 starts once M10-02's `CONTRIBUTING.md` clause commit is on `origin/feat/managed-execution`"; DECISIONS.md D-088 (5): "a prose gate on the same lane pair, no new edge"; goal/EXECUTION.md §3 lists both as early-start after M1-12
- Risk: M10 authoring writes both folders at once; both rows are `ready`, L1 and L2 are the two ~/.claude slots, so the session dispatches M10-02 and M10-03 in the same wave. M10-03 then pushes termrock commits before the agent-authored-changes clause exists (the D-055 authorization that the review relies on) and races M10-02's first push on the shared `feat/managed-execution` (rebase conflicts in `CONTRIBUTING.md`/CI files, up to five retries then a false stuck). task.toml `depends_on = []` for M10-03 also means M1-01's/authoring's verify shape cannot catch it.
- Fix: ROADMAP.md row M10-03 depends_on → "M10-02"; §3 mermaid: no M10 internal edges exist, so add a note line or `M10-02 --> M10-03` if M10 is expanded; §3 waves row: "M10-03 (L2) follows M10-02"; DECISIONS.md D-088 (5): change "a prose gate on the same lane pair, no new edge" to "an edge: M10-03 depends_on M10-02"; goal/EXECUTION.md §3 early-start list: keep M10-02, drop M10-03 (it follows by dependency).

## R3-13 [minor] M1-13 sits in wave 5b beside M1-03 but its verify needs an L6 throwaway load that must wait for M1-03 to eject; the 30-minute stuck clock does not exempt lane-cap waits

- File: `goal/EXECUTION.md`
- Where: §3 M1 wave table row 5b: "M1-13: … L6 only after M1-03 ejected"; §2 stuck rule: "a task that has produced no new evidence for 30 minutes … gets an analysis pass … time this session spends limited (§4) does not count" (only quota time is exempt); ROADMAP.md §3 waves M1: "{M1-03 (L6), M1-13 (L1 …)}"
- Risk: M1-13's six probes run one at a time; the L6 probe (and, on the runnable reading, the operator throwaway) waits for M1-03, then M1-07 and M1-10 (all L6, sequential critical path). While waiting, M1-13 produces no evidence; after 30 minutes the D-063 analysis fires, the 'stuck' attempt is re-laned to L2 (same ~/.claude home, so nothing changes), an attempt is consumed, and after three such waits M1-13 is `exhausted` — blocking M1-09 and M1-12 (both depend on it) and thereby everything M2+.
- Fix: In goal/EXECUTION.md §2 stuck rule append: "Time a task spends waiting for a lane cap or for another task's container to eject (for example M1-13's per-lane probes) is recorded as `waiting on <lane>` in the result cell and does not count against its clock." And in §3 wave table move M1-13 to its own wave after M1-10 ("| 8 | M1-13 | subagents plus throwaway loads … |", M1-09 to wave 9, M1-11/M1-12 to wave 10), mirroring the change in ROADMAP.md §3 waves M1 and D-088 (2); nothing in M1-07/M1-10 needs M1-13, so the critical path is unchanged.

## R3-14 [minor] M4-03 is an early-start task (depends_on M1-02) whose host verify calls `jackin daemon exec <instance>` — the host daemon process and an instance the daemon knows exist only from M2-03/M3-05 on

- File: `ROADMAP.md`
- Where: §2 M4 table row M4-03 (line ~216): depends_on "M1-02"; verify: "host (D-061): `jackin daemon exec <instance> -- sh -c 'echo status: DONE'` returns exit 0 and the line"; §3 waves M4: "M4-02 and M4-03 can start as soon as M1-02 exists"; goal/EXECUTION.md §5 step 4a: `jackin daemon start` only "from M2-03 on"
- Risk: If M4-03 is dispatched during M1/M2 (L4 is free after M1-08 and M2-05), its verify has no daemon to talk to (analysis/jackin.md: the host daemon is an "empty daemon shell"; 4a starts it only from M2-03) and no instance name to pass; three attempts → `exhausted: M4-03`, which then blocks M7-01 (verify runner) and M11-01a-era work. The row does not say whether a plain `jackin load` instance counts as `<instance>`.
- Fix: ROADMAP.md row M4-03 verify host part → "host (D-061): start the host daemon from the branch build if it is not running (§5 step 4a applies from this task on when it precedes M2-03), `jackin load the-architect probe-M4-03 --agent claude` on the host, then `jackin daemon exec <that instance> -- sh -c 'echo status: DONE'` returns exit 0 and the line; eject the probe." Also amend goal/EXECUTION.md §5 step 4a "(else, from M2-03 on, `jackin daemon start`)" to "(else, from the earlier of M2-03 or M4-03, `jackin daemon start`)".

## R3-15 [minor] Review task M2-08 is scheduled 'beside the first M3 wave', but M3's first wave is defined as starting during M2 — the anchor is ambiguous and the review's pr.txt head is fixed by a task (M2-07) that does not touch jackin

- File: `ROADMAP.md`
- Where: §3 waves M2: "M2-08 (L6) non-blocking, beside the first M3 wave (D-055)"; §3 waves M3: "{M3-01 (L1), M3-03 (L4)} (both can start during M2)"; M2-08 depends_on "M2-07" (crew-operator proof run, repos ecosystem/Linear), verify reviews `git diff <line 3>..<line 2>` of the rolling jackin PR
- Risk: M2-08 becomes runnable when M2-07 is `done`, which may be before or after M3-01/M3-03 started; if M3-01 has already pushed to `feat/managed-execution`, the head SHA the session pins in pr.txt line 2 includes half of M3-01's launch-pipeline change, so the M2 review covers M3 code and M3-08's `<line 3>..<line 2>` window later double-reviews or skips it. Not a stall, but a wrong artifact for every review after it.
- Fix: Anchor reviews to the last jackin-touching task of the milestone rather than the proof run: ROADMAP.md row M2-08 depends_on → "M2-06, M2-07" and goal/EXECUTION.md §5 6a add: "line 2 is the `origin/feat/managed-execution` head recorded in `tasks/<last jackin task of the milestone>/verify.host.out` at that task's step 7 (M2-06, M3-06, M4-05, M5-05, …), not the head at review launch." Replace "beside the first M3 wave" with "when M2-07 is done; L6 permitting".

## R3-16 [blocker] M1-03, M1-07, M1-09, M1-10, M8-01 verifies need `op`/the Linear token but carry no `host (D-061):` sentence, so M1-01 authors them as container parts that fail deterministically

- File: `ROADMAP.md`
- Where: §2 M1 table, verify column of rows M1-03 (`op item get linear-agent-app …`), M1-07 (`op item get … --fields label=<f>`), M1-09 (`GraphQL with the workspace token from M1-10`), M1-10 (`jackin-exec op read … | curl`), M8-01 (`jackin-exec op read` + `openssl`); concept/task-format.md lines 71-77 define the host part solely by the `host (D-061):` sentence; M1-01 scope says the verify text is copied verbatim
- Risk: The container part runs as `docker exec -u agent … sh .jackin/task/verify.sh container`. Inside `crew-operator` `op` has no token by design (M1-05d on-demand binding is only reached through the capsule picker, and M1-03's own verify asserts `printenv` never shows it), and `jackin-exec` invoked from a `docker exec` shell has no capsule tab to raise the picker in, so the verify blocks or errors. Result: `status: FAILED` three times → `exhausted: M1-03` at wave 5b, run BLOCKED inside M1 with no human-fixable cause; same for M1-07, M1-10, M1-09 (token), M8-01.
- Fix: In ROADMAP.md prefix each of those five verify cells with `host (D-061):` and rewrite the token/`op` calls for the host: M1-03 `op item get linear-agent-app --vault jackin --format json | jq -r '.fields[].label'` (host op, desktop app) plus container part `! printenv | grep -q '^OP_SERVICE_ACCOUNT_TOKEN='`; M1-07 host `op item get … --fields label=<f> --format json | jq -e '(.value // "") | length > 0'`; M1-10 host `op read "op://jackin/linear-workspace-$(cat tasks/M1-10/org.txt)/access token" | curl -s -H @- …` (drop `jackin-exec`); M1-09 host GraphQL per the §4 Linear-token rule; M8-01 host `op read … | openssl dgst -sha256 -sign /dev/stdin`. Add to concept/task-format.md `verify.sh` bullet: "When a row has no `container:`/`host (D-061):` split, M1-01 places every sentence that names `op`, `jackin-exec`, the workspace token, `jackin load`, `jackin status`, `jackin daemon`, `docker`, `ssh`, or a daemon log in the host part and the rest in the container part."

## R3-17 [blocker] Staging root `<ws>` is ambiguous (workdir vs repository) so `docker exec -w /workspace sh .jackin/task/verify.sh container` cannot find the script; container-part cwd on the host/subagents paths is undefined

- File: `goal/EXECUTION.md`
- Where: §4 container row: `jackin workspace create task-<id> --workdir ~/.jackin/managed/<id>` … **Staging** `mkdir -p <ws>/.jackin/task` … `echo .jackin/ >> <ws>/.git/info/exclude` … **Container verify** `docker exec -u agent -w /workspace … sh .jackin/task/verify.sh container`; §2 Workspace bullet (repo lives at `~/.jackin/managed/<id>/<repo>`); §5 step 5 "Container part: … here on the host and subagents paths"
- Risk: The workdir `~/.jackin/managed/<id>` is what lands at `/workspace`; the repository is `/workspace/<repo>`. The `.git/info/exclude` line implies `<ws>` is the repository, so the file is staged at `/workspace/<repo>/.jackin/task/verify.sh` and `-w /workspace sh .jackin/task/verify.sh` fails with `sh: .jackin/task/verify.sh: No such file` → `status: FAILED` for every container task from wave 2 on. The same path is typed into the agent prompt (`/goal Read this file: .jackin/task/TASK.md`). On the host and subagents paths nothing stages `.jackin/task/` at all and the cwd for `verify.sh container` is unstated, so relative paths inside the script resolve differently per path. Container parts that read `tasks/<id>/…` (M1-05c "REVIEW_CRUCIBLE_SHA recorded in tasks/M1-05c/", M1-12 `tasks/M1-12/issues.json`) are unreachable in-container.
- Fix: §4 Staging: replace `<ws>` with `~/.jackin/managed/<id>` explicitly: "`mkdir -p ~/.jackin/managed/<id>/.jackin/task` (this directory is `/workspace` in the container; the repository is `/workspace/<repo>`); no `.git/info/exclude` is needed because the workdir is not a git repository". §5 step 5: "Container part cwd is the workdir on every path: on the container/daemon paths `docker exec -u agent -w /workspace …`; on the host and subagents paths the session stages the same `.jackin/task/` and runs `cd ~/.jackin/managed/<id> && sh .jackin/task/verify.sh container > <ecosystem>/tasks/<id>/verify.container.out 2>&1`. Host part cwd is the ecosystem root." concept/task-format.md `verify.sh` bullet: "The container part reads only `./<repo>/…` and `.jackin/task/refs/*`; any evidence file it consumes is staged as a ref by the host session and referenced as `.jackin/task/refs/<name>`." Rewrite M1-05c to `git -C /opt/review-crucible rev-parse HEAD` equals `grep -oE 'REVIEW_CRUCIBLE_SHA=[0-9a-f]+' jackin-crew-reviewer/Dockerfile | cut -d= -f2`, and M1-12 container to `ecosystem/tasks/M1-12/issues.json`.

## R3-18 [blocker] Verifies that require a live `jackin load … starts`, `jackin status` of a running instance, or `hardline … attaches` cannot be executed by a non-interactive `verify.sh` before M3-01, and fail on any resume

- File: `ROADMAP.md`
- Where: §2 verify cells: M1-05a (`jackin load donbeave/crew-builder --agent claude starts; inside: …`), M1-05b (`… starts (a throwaway load …); inside: …`), M1-05c (`loads on both runtimes; test -f … inside`), M3-01 (`prints an instance id, and jackin hardline <instance id> attaches`), M3-02 (`a real launch without --agent … jackin status --format json shows the agent`), M3-02a (`a real jackin load … starts a claude session (jackin status … agent field)`), M4-01 (`launch with a prompt … attach still works`), M4-04 host (`a real reply on the live session reaches the PTY within one interval`)
- Risk: `jackin load` is a TUI launch (needs tmux, `CI` unset, dialog handling, up to 15 min readiness); `hardline` is an interactive attach. A POSIX `verify.sh` run as `sh tasks/<id>/verify.sh host > verify.host.out` cannot do either, and the M1-01 authoring subagent has no defined shape to write. If it instead asserts `jackin status --format json`, the throwaway has already been ejected (EXECUTION §3 wave 5: "the load ejects before wave 5b") or is gone after a crash/resume (D-091 (2)), so the verify fails on retry → exhaustion. M1-05a..c are wave 3: the run stalls in M1.
- Fix: Extend D-091 (2) to these rows and make the session do the launch: in ROADMAP.md replace each `… starts; inside: <checks>` with `host (D-061): tasks/<id>/throwaway/status.json (the \`jackin status --format json\` row of the throwaway load the host session performed per goal/EXECUTION.md §5 step 4b) shows role <selector> and agent <runtime>; tasks/<id>/throwaway/inside.out (output of \`docker exec -u agent <name> sh -c '<checks>'\`) ends with \`inside: ok\``. M3-01: replace `jackin hardline <instance id> attaches` with `jackin status --format json | jq -e --arg i "$(cat tasks/M3-01/instance.txt)" '.[]|select(.instance==$i)'` taken as `tasks/M3-01/status.json` at launch time, then `jackin eject` by name in the task. M3-02/M3-02a/M4-01/M4-04: assert on `tasks/<id>/status.json` / `capture.txt` filed during the task. Add goal/EXECUTION.md §5 step 4b: "Throwaway loads named in a verify cell are performed by this session (container row mechanics, caps of §3), their `jackin status --format json` row and `docker exec` outputs are filed under `tasks/<id>/throwaway/`, and the instance is ejected before step 5; `verify.sh` never launches, attaches, or ejects."

## R3-19 [major] `<org>` (Linear `urlKey`) and the app user id are never filed, so every host verify that builds `op://jackin/linear-workspace-<org>/…` has no deterministic way to name the item

- File: `ROADMAP.md`
- Where: §2 M1-10 scope ("use `urlKey` as `<org>`; store … in `op://jackin/linear-workspace-<org>`") and verify; M1-09 verify (`op://jackin/linear-workspace-<org>`), M1-11 verify (`--arg app` "the app user id recorded in the item"), M1-12/M2-06/M3-05/M3-06 host verifies via the goal/EXECUTION.md §4 Linear-token rule (line ~251: `op://jackin/linear-workspace-<org>/access token`); concept/task-format.md evidence list (`lanes.json`, `dind.out`, `pr.txt`, `scratch-repo.txt`) omits it
- Risk: M1-01 must write ~15 host verify scripts before M1-10 has run; none can know the item name. Scripts either hard-code a guessed slug (fails) or scan `op item list --vault jackin | grep linear-workspace-` (ambiguous if the human already has such an item, and non-deterministic). Every downstream Linear-token check is then a coin flip and M1-09/M1-11/M1-12 exhaust in wave 8-9.
- Fix: M1-10 scope: append "and write the `urlKey` to `tasks/M1-10/org.txt` and the app user id to `tasks/M1-10/app-user-id.txt` (one line each, no secret)"; M1-10 verify: add `test -s tasks/M1-10/org.txt && op item get "linear-workspace-$(cat tasks/M1-10/org.txt)" --vault jackin --format json | jq -e '[.fields[].label]|index("access token")'`. goal/EXECUTION.md §4 Linear-token rule: "`<org>` is `$(cat tasks/M1-10/org.txt)`; the app user id is `$(cat tasks/M1-10/app-user-id.txt)`." concept/task-format.md evidence sentence: add `tasks/M1-10/org.txt`, `tasks/M1-10/app-user-id.txt`. M1-11 verify: `--arg app "$(cat tasks/M1-10/app-user-id.txt)"`.

## R3-20 [major] M10-03 container verify requires `mise run gate` green, but the goldens it needs can only be blessed by the host session after the story is pushed; no step orders the bless before the verify

- File: `ROADMAP.md`
- Where: §2 M10-03 verify ("golden frames recorded by the agent, blessed by the host session under D-075 …, committed, …; `mise run gate` green on the branch"); goal/EXECUTION.md §5 has no bless step (only §6 mentions D-075); PREFLIGHT §2 D-075 item says "Nothing to do at run time"
- Risk: The container agent extends the flagship set; `mise run gate` runs the goldens test against `crates/termrock-lookbook/goldens/`, which has no frames for the new stories until the host blesses. The container part therefore prints `status: FAILED`, the host part must then print FAILED (D-086 masking rule), three attempts on three lanes all fail identically, `exhausted: M10-03` blocks M10-04..M12. Same for M10-04 if it changes flagship output.
- Fix: ROADMAP.md M10-03 verify → "container: tests pass excluding goldens (`cargo nextest run --workspace -E 'not test(goldens)'` plus the rest of `mise run gate`), the new story renders to `tasks/M10-03/frames/*.txt`. host (D-061): after the session's bless (goal/EXECUTION.md §5 step 4c) `git -C ~/.jackin/managed/M10-03/termrock status --porcelain` is empty, `git log origin/feat/managed-execution -1 --format=%s -- crates/termrock-lookbook/goldens` names the bless commit, and `mise run preview-goldens` passes in that checkout." goal/EXECUTION.md §5 add step 4c: "M10-03 and M10-04 only: when the container's last message reports the story pushed, run in `~/.jackin/managed/<id>/termrock`: `git fetch origin && git checkout feat/managed-execution && git pull --ff-only && mise install && mise run bless-previews && git add crates/termrock-lookbook/goldens && git commit -s -m 'chore: bless previews for <id>' && git push`; file the frame text in `tasks/<id>/`; then step 5." Add `mise` (host, `brew install mise`) to goal/PREFLIGHT.md §1 with proof `mise --version`.

## R3-21 [major] D-091 (2) missed several verifies that still assert live daemon/Docker state or mutate Linear at verify time: M2-06, M3-06, M4-03, M4-06, M7-04, M9-02, M11-04, M12-01, M12-03

- File: `ROADMAP.md`
- Where: §2 verify cells: M2-06 (`jackin daemon status --format json | jq … length==1` live), M3-06 host ("cancel the scratch issue by GraphQL `issueUpdate` …; `docker ps --filter label=…` empty within one tick"), M4-03 host (`jackin daemon exec <instance> -- …` with no named instance), M4-06 ("The file exists in the workspace" = host `~/.jackin/managed/<key>`), M7-04 and M9-02 ("Daemon log shows …" with no filed log path), M11-04 ("`jackin daemon status` on the server lists the issue"), M12-01 (`jackin daemon status --host <server>`), M12-03 ("Ledger shows …" no file)
- Risk: After §5 step 8 closes the scratch issue, after a §5 4a daemon restart, or on any resume, the live state is gone: M2-06's issue is no longer listed, M3-06 has nothing to cancel (passes vacuously on retry — wrong artifact), M4-03 has no running instance, M11-04/M12-01 depend on the server daemon still holding the issue. Each is a deterministic second-attempt failure or a vacuous pass; M7-04/M9-02/M12-03 are unwritable because no evidence file is named.
- Fix: Apply the D-091 (2) snapshot rule to each row: M2-06 → "the task files `tasks/M2-06/status.json` while the scratch issue is delegated; verify: `jq -e --arg id "$(cat tasks/M2-06/scratch-issue.txt)" '[.issues[]|select(.identifier==$id)]|length==1' tasks/M2-06/status.json`". M3-06 → the task performs the cancel and files `cancel.json`, `docker-ps-after.txt` (empty), `workspace-after.txt` (`test ! -d` result); verify reads the three files. M4-03 → task files `tasks/M4-03/exec.json` (`{exit:0, stdout:"status: DONE"}`); verify `jq -e '.exit==0 and (.stdout|test("status: DONE"))'`. M4-06 → `tasks/M4-06/workspace-ls.txt` contains the named file. M7-04, M9-02, M11-04, M12-01, M12-03 → name `tasks/<id>/daemon.log`, `tasks/<id>/status.json`, `tasks/<id>/ledger.json` as the filed evidence and grep those. Add the nine rows to D-091's Consequences list.

## R3-22 [minor] M1-01 verify's `shellcheck -s sh … clean` and "its last echo is a `status:` line" are underspecified and fail on style-level output

- File: `ROADMAP.md`
- Where: §2 M1-01 verify: "`dash -n verify.sh` and `shellcheck -s sh verify.sh` are clean, … and its last echo is a `status:` line"; concept/task-format.md line 62-64 (same wording)
- Risk: `shellcheck` exits 1 on any finding at its default minimum severity `style` (SC2086, SC2129, SC2181 are near-certain in 48 generated scripts), so "clean" as exit 0 fails the wave-0 task until every style hint is fixed across 48 files — three attempts, possible `exhausted: M1-01` before anything else runs. "Last echo" has no grep shape: a subagent may test `tail -n1` (multi-line scripts end with `exit 1`) or `grep echo | tail -1`, and different authors yield different verdicts.
- Fix: ROADMAP.md M1-01 verify and concept/task-format.md: replace with "`dash -n verify.sh` exits 0 and `shellcheck -s sh -S warning verify.sh` exits 0 (severity `warning` and above; style hints are not failures); `grep -qE '^ *echo "?status: (DONE|FAILED)' verify.sh` matches at least twice (one DONE, one FAILED) and `grep -q 'case "\$1"' verify.sh` matches".

## R3-23 [minor] M2-01 verify names "a manual run against the real client secret logs a successful `viewer` query" with no filed evidence and no host marker; M11-01a/M11-02 verifies depend on GitHub Actions `conclusion` of runs the verify itself has to trigger

- File: `ROADMAP.md`
- Where: §2 M2-01 verify ("a manual run against the real client secret logs a successful `viewer` query without printing any token"); M11-01a verify ("`gh run list … | jq -e '.[0].conclusion=="success"'` after a re-run for all three"); M11-02 verify (`gh run list … --workflow publish-image.yml … conclusion=="success"`)
- Risk: M2-01 is an L1 the-architect container task: the real client secret is only readable by host `op`, so the check is a host part, but the cell has no `host (D-061):` sentence and names no file; M1-01 cannot write it and a subagent may produce a script that greps a transcript. M11-01a/M11-02: `.[0]` is the newest run, which may be `in_progress`/`queued` (cold multi-arch cosign build takes tens of minutes) or a run for an unrelated push; the verify returns FAILED, consumes an attempt, and the retry re-triggers a run — three attempts can pass without one completed run being observed, ending in exhaustion.
- Fix: M2-01: replace with "container: `cargo nextest run -p jackin-daemon linear::auth` passes … host (D-061): `tasks/M2-01/viewer.json` (filed by the session from `jackin daemon linear-check --format json`, or the daemon's `viewer` log line redacted) shows `.data.viewer.id` equal to `$(cat tasks/M1-10/app-user-id.txt)`; `gitleaks detect --no-git --source tasks/M2-01` clean". M11-01a and M11-02: replace `gh run list … --limit 1 … .[0].conclusion=="success"` with "the task records the run id it triggered in `tasks/<id>/run-<p>.txt` and the verify runs `gh run view "$(cat tasks/<id>/run-<p>.txt)" -R donbeave/jackin-crew-<p> --json status,conclusion | jq -e '.status=="completed" and .conclusion=="success"'`"; the task itself does `gh run watch <id> --exit-status` before finishing.

## R3-24 [blocker] Standing proof `op whoami` fails on this host while `op read` works → preflight defect filed at every session start (wave 0 BLOCKED)

- File: `goal/PREFLIGHT.md`
- Where: §1, 1Password item, line 38 ("Proof: `op whoami` succeeds and…"); mirrored in goal/EXECUTION.md §1 step 4 ("`op` with stdin closed")
- Risk: Measured now: `op whoami` prints `[ERROR] account is not signed in` (~/.config/op/config has `"accounts": null`, desktop-app integration only), yet `op read "op://Private/Context7/API Keys/Claude" </dev/null` returns 44 bytes and `op vault list` works. EXECUTION §1 step 4 turns a failed standing check the session cannot fix into a preflight defect → the first run stops BLOCKED before M1-01, and every re-run stops the same way.
- Fix: Replace the proof with what the run actually needs: "Proof: `op account list` lists the account and, after ≥15 min idle, `op read \"op://Private/Context7/API Keys/Claude\" </dev/null >/dev/null && echo ok` prints `ok` and `op read op://tailrocks/op-service-account-jackin-operator/credential </dev/null | wc -c` is non-zero; `op whoami` is not used (it fails under desktop-app integration with no CLI account added)." Apply the same wording in EXECUTION §1 step 4.

## R3-25 [blocker] Server host preflight omits the `op` CLI and any transfer of the daemon service-account token; M11-03's verify (`op://` resolves on the server) is unreachable

- File: `goal/PREFLIGHT.md`
- Where: §5 M11 item, lines 208-231 (package list `git curl build-essential clang pkg-config`, `sudo apt-get` only); ROADMAP.md M11-03 row line 360; concept/credentials.md #17 ("`OP_SERVICE_ACCOUNT_TOKEN` from `op://tailrocks/...` at daemon start")
- Risk: jackin resolves `op://` by shelling out to `op` (analysis/jackin.md:137). Nothing installs `op` on the server and the passwordless-sudo grant covers only `apt-get` (the 1Password apt repo needs `sudo tee` for the keyring/sources; `/usr/local/bin` needs `sudo install`). The daemon token lives in vault `tailrocks`, which a service account cannot read and which has no desktop app on the server, so "from op://tailrocks at daemon start" cannot execute there; no step scp's/exports it. M11-03's host verify `jackin daemon status … resolves every op:// reference` fails 3× → `exhausted: M11-03`, M11-04, M12-* never run.
- Fix: PREFLIGHT §5 M11 item: add "`op` CLI 2.39.x installed on the server (`op --version` in the ssh proof line) — or M11-03 installs it by direct download into `~/.local/bin` (no sudo)". ROADMAP M11-03 scope: add "the host session reads `op://tailrocks/op-service-account-jackin-daemon/credential` and writes it on the server as `~/.config/jackin/daemon.env` (`umask 077; ssh -i … 'cat > …'` fed from `op read` on stdin, never argv); `jackin daemon install` loads `OP_SERVICE_ACCOUNT_TOKEN` from that file (build it in jackin on the branch if absent, D-046)". credentials.md #17: change "at daemon start" to "materialised once by M11-03 on the server". State whether `jackin daemon install` needs `systemctl`/`loginctl enable-linger` and widen the sudo grant accordingly.

## R3-26 [blocker] GitHub App preflight item is under-specified and contradicts credentials.md #5: item names, field labels, one-app-vs-per-org, visibility, webhook

- File: `goal/PREFLIGHT.md`
- Where: §2 GitHub App item, lines 147-163; concept/credentials.md §4 row 5 (line 135) and §5.5 bullet "#5: one GitHub App … versus one per org" (line ~240); ROADMAP.md M8-01 row line 283
- Risk: credentials.md #5 (the "single source M11-01 verifies against") names one item `github-app-jackin-daemon` with fields `App ID`, `Installation ID`, while PREFLIGHT/M8-01 read `op://jackin/github-app-jackin-daemon-<org>/app id|client id|installation id|PEM private key`. A human following credentials.md creates the wrong item → M8-01 `jackin-exec op read` fails 3× → exhausted; M8-02/M9/M11-04 (which need the App token) stall. PREFLIGHT also leaves open: which account owns the App; a GitHub App created under `jackin-project` with visibility "Only on this account" cannot be installed on `tailrocks`; two per-org apps cannot both have slug `jackin-daemon` (slugs are global), so the proof `select(.app_slug=="jackin-daemon")` fails for one org; the creation form requires a webhook URL unless "Active" is unticked. §5.5 still lists this as an open question.
- Fix: PREFLIGHT §2 item: "Create ONE GitHub App under the `jackin-project` organization (Settings → Developer settings → GitHub Apps → New), name `jackin-daemon` (if the slug is taken, record the actual slug in `tasks/M8-01/app-slug.txt` and use it in the proof), Webhook: Active unticked, 'Where can this GitHub App be installed?' = Any account, permissions contents:write / pull_requests:write / metadata:read; install it on `jackin-project` and `tailrocks` with All repositories; store per org `op://jackin/github-app-jackin-daemon-<org>` with fields exactly `app id`, `client id`, `installation id`, `PEM private key` (lower-case labels)." credentials.md #5: rewrite the item column to the per-org names and lower-case labels and delete the §5.5 "#5 one vs per org" bullet (decided).

## R3-27 [major] M4-05 preflight offers an API-key path (`op://jackin/<runtime>-daemon/api key`) that no laptop-side mechanism consumes; only a host login can reach the container

- File: `goal/PREFLIGHT.md`
- Where: §3 lines 177-183 ("either a host login … or an API key stored as `op://jackin/<runtime>-daemon/api key`"); ROADMAP.md M4 preflight line 204-210 and M4-05 row line 218; goal/EXECUTION.md §4 rules ("the laptop stays on forwarded logins (`auth_forward = \"sync\"`) for the whole run", D-090)
- Risk: With `auth_forward = "sync"` jackin copies the runtime's host config dir (`~/.amp`, `~/.kimi-code`, `~/.opencode`, `~/.grok`, analysis/jackin.md adapter table); an `op://` item is used only in `api_key` mode, which D-090 forbids on the laptop and which the-architect (no `op`) cannot pull itself. A human who provides only the vault item (as the wording allows) gets a runtime that is neither skipped (credential 'provided') nor working → M4-05 verify (`Blocked` then `Working` for every runtime that has a credential) fails 3× → exhausted; M11-01's 'runtimes in use' set is then wrong too.
- Fix: Either (a) PREFLIGHT §3: "a host login in that runtime's config directory (`~/.amp`, `~/.kimi-code`, `~/.opencode`, `~/.grok`) is the only form M4-05 can use on the laptop; the `op://jackin/<runtime>-daemon/api key` items are for M11 only"; or (b) ROADMAP M4-05 scope + EXECUTION §4 container row: "for a runtime whose credential is an `op://` item, `task-M4-05.toml` sets workspace-level `[auth] <runtime> = \"api_key\"` and `[env] <RUNTIME_KEY> = \"op://jackin/<runtime>-daemon/api key\"` (workspace-layered auth config, analysis/jackin.md 'layered workspace-role → workspace → global'); the global config stays `sync`". Add the per-runtime env-var name (AMP_API_KEY, KIMI_CODE_API_KEY, …) to the §3 proof.

## R3-28 [major] `gh` scope proof (`repo`, `workflow`) is insufficient for the org-level proofs and M8-01: `/orgs/<org>/installations` and `user/memberships/orgs` need `admin:org`/`read:org`

- File: `goal/PREFLIGHT.md`
- Where: §1 line 49 ("`gh auth status` shows `donbeave` with `repo` and `workflow` scopes"); §2 proofs at lines 96 and 160-162; ROADMAP.md M8-01 row line 283
- Risk: The current token happens to carry `admin:org`, but the item tells the human only `repo` and `workflow` are required. A re-login (`gh auth login` defaults or a scoped refresh) mid-preflight drops `admin:org` → the §2 App proof and M8-01's `gh api /orgs/<org>/installations` return 403 → M8-01 exhausted with a 'defect' nobody listed. Same for `gh secret set` in M11-02 (needs `repo`) and the `gh api -X PATCH repos/... delete_branch_on_merge` fix (needs repo admin).
- Fix: Line 49: "`gh auth status` shows `donbeave` with scopes `repo`, `workflow`, `admin:org` (org installation listing in §2 and M8-01, `gh api user/memberships/orgs`), and `delete_repo` is NOT required. Proof: `gh auth status 2>&1 | grep -E \"admin:org\" && gh api /orgs/jackin-project/installations --jq .total_count`."

## R3-29 [minor] Docker Hub namespace contradiction: credentials.md #15 says `projectjackin/*`, PREFLIGHT/M11-02 say `docker.io/donbeave/...` — the human may mint the token for the wrong account

- File: `concept/credentials.md`
- Where: §4 row 15 (line 145: "Docker Hub `projectjackin/*`, GHCR"); goal/PREFLIGHT.md §5 lines 207-209; ROADMAP.md M11-02 row line 359
- Risk: M11-01 verifies against credentials.md §4 by name only, so a token for a `projectjackin` org member passes M11-01, but `publish.yml` pushes to `docker.io/donbeave/jackin-crew-<p>` → push denied → M11-02 fails 3× → exhausted; M11-03 (pulls roles from M11-02) stalls.
- Fix: credentials.md row 15: replace "Docker Hub `projectjackin/*`" with "Docker Hub `donbeave/jackin-crew-*` (token minted by user `donbeave` with Read & Write, namespace `donbeave` confirmed by `curl -s -o /dev/null -w '%{http_code}' https://hub.docker.com/v2/users/donbeave/` = 200)"; PREFLIGHT §5 proof: add `echo "$(op read op://jackin/registry-dockerhub/token)" | docker login -u "$(op read op://jackin/registry-dockerhub/username)" --password-stdin && docker logout`.

## R3-30 [minor] M10 crates.io item is conditional on a decision no task or document makes ("If termrock 0.14 publishes to crates.io")

- File: `goal/PREFLIGHT.md`
- Where: §5 M10 item lines 193-196; ROADMAP.md M10 preflight lines 304-313
- Risk: No M10 row publishes to crates.io and D-055/D-090 say no release or tap publish in this run, yet the preflight asks the human to decide and store a token under an unnamed `<name>`. An operator who guesses 'yes' creates an unused secret; one who guesses 'no' while a subagent reads the M10 prose as a publish step files a missing-input defect mid-M10 (ambiguous instruction → wrong STOP).
- Fix: Replace the item with: "M10: nothing to do. No task publishes termrock to crates.io in this run (D-055, D-090); blessing is pre-approved in §2." Remove the crates.io sentence from ROADMAP.md M10 preflight (lines 309-313).

## R3-31 [blocker] Daemon path can never run a roadmap task's container verify: verify.sh and refs are never delivered to the daemon-launched container

- File: `ROADMAP.md`
- Where: M4-04 row (line 217: "the task's verify.sh and reference files from the issue attachments or description"); M1-12 row (line 144: description = TASK.md text only, no attachments); goal/EXECUTION.md §4 daemon row (line 191) and §5 step 5 (lines 341-346: container part via `jackin daemon exec <instance> -- sh .jackin/task/verify.sh container`)
- Risk: M1-12 attaches nothing and embeds only TASK.md; M4-04 pre-fetches only what is on the issue, so every daemon-dispatched task (M4-04 onward: M5-*, M6-*, M7-*, ...) has no `.jackin/task/verify.sh` and no `refs/` in its container. `jackin daemon exec ... sh .jackin/task/verify.sh container` fails on every attempt, the D-063 analysis re-lanes three times, the task is filed `exhausted:` and the run ends BLOCKED at the first daemon-path task.
- Fix: ROADMAP M4-04 scope: replace "from the issue attachments or description" with "by fetching `tasks/<identifier-title-id>/` from the public ecosystem repository at the sha given by a `task_source: https://github.com/donbeave/ecosystem/tree/<sha>/tasks/<id>` line the M1-12 description carries (`git clone --depth 1` or raw URLs; no token needed, D-065)". ROADMAP M1-12 scope: add "first description line `task_source: https://github.com/donbeave/ecosystem/tree/<sha of HEAD>/tasks/<id>`, refreshed on every idempotent re-run". goal/EXECUTION.md §4 daemon row: add "Interim until M4-04's fetch exists: right after the daemon reports the instance, the session stages the folder with `docker cp tasks/<id>/. <container>:/workspace/.jackin/task/` and the refs into `refs/`, then continues."

## R3-32 [blocker] Issue convention (`agent:<lane value>`) contradicts M2-05 validation, and the daemon has no defined source for the per-launch account home

- File: `SPEC.md`
- Where: SPEC.md §4 line 64 and concept/task-format.md line 18 ("until M3-01 the lane value, for example `agent:codex-chainargos`"); ROADMAP.md M2-05 (line 168: "reject unknown runtime for the role's `agents`"), M1-09 (line 141: label groups), M1-12 (line 144: "labels per convention"), M3-05 (line 197: "choose the account home per launch" with no source named)
- Risk: M1-12 runs before M3-01, so per the convention it labels Codex-lane issues `agent:codex-chainargos`/`agent:codex-chainargos2`; M2-05 rejects any runtime not in the role's `agents = ["claude","codex"]`, so on the daemon path every such roadmap issue gets an `error` activity and is never dispatched. If M1-12 instead writes `agent:codex`, the daemon has no field from which to pick `~/.codex-chainargos` vs `~/.codex-chainargos2` and the D-056 per-home cap 1 / D-071 fallback cannot be enforced. Either way M3-05/M3-07 and every later daemon-path task stall or run on the wrong account.
- Fix: SPEC.md §4 and concept/task-format.md: runtime row → "label `agent:<runtime>` (`claude`|`codex`), never a lane value"; add row "lane | yes | label `lane:L<n>` from `task.toml` `lane` | account home, model, effort per `tasks/M1-13/lanes.json`". ROADMAP M1-09: add `lane` to the label groups (values L1..L6). M1-12: set `lane:<task.toml lane>`. M2-05: parse `lane`; account home resolved by M3-05 from daemon config `[lanes.L<n>] account_home = "..."` (M3-05 scope: "add the `[lanes]` table; the host session seeds it from `tasks/M1-13/lanes.json` before the first daemon start, goal/EXECUTION.md §5 step 4a").

## R3-33 [blocker] M8-01 is an early-start task but its verify needs `tasks/M3-07/scratch-repo.txt`, which exists only after M3-07

- File: `ROADMAP.md`
- Where: M8-01 row line 283 (`depends_on` = M1-03; verify: "the `jackin-project` listing contains the repository named in `tasks/M3-07/scratch-repo.txt`"); §3 waves table line 515 ("M8-01 (L6) depends only on M1-03 and may start early"); mermaid line 496 (`M1-03 --> M8`); goal/EXECUTION.md §3 line 134 (early-start list includes M8-01)
- Risk: After M1-12 is `done` the session authors M8 and dispatches M8-01 at once (its only dependency is M1-03, done in M1 wave 5b). The verify reads a file that M3-07 writes many waves later, fails three times on three lanes, and M8-01 is filed `exhausted:`; M8-02 stays not runnable, and the run ends BLOCKED until a human re-runs — a guaranteed mid-run stop.
- Fix: ROADMAP M8-01 `depends_on` → `M1-03, M3-07` (D-054 decision note in DECISIONS.md); mermaid add `M3-07 --> M8`; §3 line 515: "M8-01 (L6) may start as soon as M3-07 is done"; goal/EXECUTION.md §3 line 134: replace `M8-01` in the early-start list with "M8-01 after M3-07". Alternatively drop the scratch-repository clause from M8-01's verify and move it to M8-03.

## R3-34 [blocker] M11-05 (and M10-06) verify demands a PR review per reviewed repository, but role repositories have no PR (they commit to `main`) and one `pr.txt` cannot hold two PRs

- File: `ROADMAP.md`
- Where: M11-05 row line 362 ("One such review per reviewed repository ... `tasks/<id>/pr.txt`", repos = jackin, role repositories); M10-06 row line 322 (jackin + termrock, one `pr.txt`); goal/EXECUTION.md §5 step 6a lines 367-377 (writes a single three-line `tasks/<review-id>/pr.txt`); DECISIONS.md D-074 line 1371-1373 (role repositories commit directly to `main`)
- Risk: For `donbeave/jackin-crew-*` no `feat/managed-execution` branch or PR ever exists, so `gh api .../pulls/<n>/reviews` with a review per role repository is impossible; M11-05 exhausts after three attempts and the run ends BLOCKED three tasks before DONE. M10-06 needs two PR records but step 6a and the reviewer's staged `.jackin/task/pr.txt` define exactly one, so the second repository's review has no pinned SHA.
- Fix: ROADMAP M11-05 verify → "A review on the jackin rolling PR as in M2-08; for each role repository a commit comment (`gh api repos/donbeave/jackin-crew-<p>/commits/<sha>/comments --input -`) whose body starts with `verdict:` on the sha in line 2 of `tasks/M11-05/pr.<p>.txt`". ROADMAP M10-06 and goal/EXECUTION.md 6a: "one record per repository: `tasks/<review-id>/pr.txt` for the first repository named in `repos`, `tasks/<review-id>/pr.<repo>.txt` for each further one; all staged into `.jackin/task/`; for a repository without a PR line 1 is `commit`".

## R3-35 [blocker] M11-01's `depends_on` (M1-03) makes it runnable right after M1-12, so M11 authoring and M11-01 start in M1 time despite the prose gate

- File: `ROADMAP.md`
- Where: M11-01 row line 357 (`depends_on` = M1-03; scope prose "starts with M11, after M10-05"); goal/EXECUTION.md §3 lines 124-127 (Runnable = every `depends_on` row done + M1-12 done) and lines 159-161 (authoring triggers when "the first task of a milestone would otherwise be runnable"); DECISIONS.md D-088 (4) lines 1965-1967
- Risk: The runnable definition is structural; the M11-01 gate is only a sentence in a scope cell and in §3 prose. A session applying §3 literally authors M11 right after M1-12 and dispatches M11-01, whose verify needs `tasks/M4-05/` (runtimes in use) and the daemon service-account resolution; it fails three times and is filed `exhausted:` — a BLOCKED stop, or at best a wasted L6 slot and an early `<milestone>-00 authoring` with M1-12 re-run while M2 is still on the critical path.
- Fix: ROADMAP M11-01 `depends_on` → `M1-03, M10-05` (record under D-088 as a graph amendment); mermaid line 501 keep `M1-03 --> M11` and add `M10 --> M11` already present; goal/EXECUTION.md §3 line 132-135: "Only M3-01, M3-03, M4-02, M4-03, M6-01, M8-01 (after M3-07), M10-02, M10-03 may start before their milestone is the lowest unfinished one; every other task additionally waits for that."

## R3-36 [major] Lane templates carry role-specific Docker grants (privileged DinD, network allowlist) although every lane serves builder, operator, and reviewer tasks

- File: `ROADMAP.md`
- Where: M1-13 row line 145 ("for builder lanes, `[docker.grants] dind = \"privileged\"` ... network allowlist grant ... for operator lanes"; verify: "for builder lanes, the plan shows the `dind` grant"); §5 lane table lines 587-593 (L3 runs M1-01 builder, M1-11/M2-07 operator; L6 runs M1-05c the-architect, M1-03 operator, reviews); concept/roles.md line 132 (operator `dind = "none"`, reviewer hardened); goal/EXECUTION.md §4 container row line 190 (merge `tasks/M1-13/lanes/L<n>.toml` into every `task-<id>.toml`)
- Risk: "Builder lanes" and "operator lanes" are undefined sets: a lane is runtime+model+account (§5) and each one runs all three roles. Merging a per-lane template gives crew-operator and crew-reviewer loads a privileged DinD sidecar (contradicting their manifests and threat models, and possibly a launch conflict/dialog jackin never documented), or the-architect/crew-builder tasks on L3/L5/L6 no DinD at all so `docker build`/testcontainers steps fail; M1-13's own verify clause cannot be written deterministically by M1-01.
- Fix: ROADMAP M1-13 scope: "templates `tasks/M1-13/lanes/L<n>.toml` hold `sync_source_dir` and `[env]` only; role grants live in `tasks/M1-13/grants/<role>.toml`: `the-architect` and `donbeave/crew-builder` → `[docker.grants] dind = \"privileged\"`; `donbeave/crew-operator` → the network allowlist grant; `donbeave/crew-reviewer` → none"; verify: "the-architect and crew-builder probes show the `dind` grant, the operator probe the allowlist, the reviewer probe neither". goal/EXECUTION.md §4 container row: "merge the lane template, then `tasks/M1-13/grants/<role>.toml`". concept/roles.md line 132 and §7: same wording.

## R3-37 [major] Daemon `~/.claude` cap: D-056/SPEC/M3-05 say 3, D-071/EXECUTION say 2 for this run, and no step ever configures the daemon to 2

- File: `DECISIONS.md`
- Where: D-056 lines 926-942 (`~/.claude` 3; "wave planning may schedule up to three"; no "Amended by D-071" note); SPEC.md §6 step 2 line 117 ("for `~/.claude` 3"); ROADMAP.md M3-05 line 197 ("3 for `~/.claude`"); D-071 lines 1261-1264 ("the daemon's laptop cap for `~/.claude` is set to 2 for this run"); goal/EXECUTION.md §3 lines 115-117 and §4 daemon row line 191 (no cap configuration step)
- Risk: Once the daemon path is active it dispatches up to three L1..L3 containers while the host session and its subagents also draw on `~/.claude`; the session bucket drains, the reserve rule (below 20 %) forbids Claude dispatch, and every Claude-lane chain goes `waiting` at once — hours lost per 5-hour window and the session itself limited mid-verify. An agent reading D-056 (unamended) may also schedule three Claude container tasks per wave.
- Fix: DECISIONS.md D-056: add preamble "Amended by D-071: `~/.claude` cap is 2 while the host session runs; wave planning schedules at most two." SPEC.md §6 step 2: "for `~/.claude` 3 (2 during this run, D-071)". ROADMAP M3-05 scope: "per-account caps configurable in daemon config (`[daemon.accounts.\"~/.claude\"] max = 2`); default 3". goal/EXECUTION.md §5 step 4a: "before the first `jackin daemon start` write `max = 2` for `~/.claude` and 1 per Codex home into `~/.config/jackin/config.toml` and verify with `jackin daemon status --format json`".

## R3-38 [major] Documents the session reads first still forbid actions the run requires: D-045/D-048 "the-architect is used as is" and AGENTS.md rule 1 "no build files, scripts; Markdown only"

- File: `DECISIONS.md`
- Where: D-045 lines 651-657 ("Existing roles ... are not modified for it", no amendment note); D-048 lines 724-731 ("The role is used as it is; it is not extended"); versus M1-13 (ROADMAP line 145: remove `[claude].model`, switch CI, install sign-off hook), M3-02 (line 193: `default_agent` in the-architect); AGENTS.md lines 13-17 (rule 1) and README.md "Working rules" versus `task.toml`, `tasks/M1-13/lanes/L<n>.toml`, `lanes.json`, `issues.json`, `verify.out`, `container.txt`, `codex.log`, `example-<n>.md`
- Risk: GOAL.md orders AGENTS.md read before any task and names DECISIONS.md as source of truth. A subagent implementing M1-13 or M3-02 that honours D-045/D-048 leaves `[claude].model` in place or refuses the CI edit, so M1-13's verify (`git show origin/main:jackin.role.toml` has no `[claude].model`; `ci-required` green) fails three times → `exhausted: M1-13` on the critical path (M1-09, M1-12 wait on it). A session honouring rule 1 strips or refuses to commit the TOML/JSON evidence other verifies consume (`lanes.json` for M1-09/M1-12, `checkout.txt` for step 4a).
- Fix: DECISIONS.md D-045 and D-048: add "Amended by D-074, D-078, D-089: `jackin-the-architect` receives exactly the run's three changes — CI on GitHub-hosted runners for `pull_request` (M1-13), removal of `[claude].model` plus the DCO sign-off hook (M1-13), and `default_agent` (M3-02); no project-specific tooling." AGENTS.md rule 1: "... except everything under `tasks/<id>/` written by the run (`task.toml`, `verify.sh`, evidence: `.out`, `.json`, `.toml`, `.txt`, `.log`, `.cast`) and the root `verify.sh` (D-038, D-069)"; README.md "Working rules" first bullet: same clause.

## R3-39 [blocker] M4-04 host verify needs a Linear `prompt` activity that nobody on the run can post at that point (deadlock with M4-06)

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/ROADMAP.md`
- Where: M4-04 row (line 217), verify column: "a real reply on the live session reaches the PTY within one interval (log delta)"; goal/EXECUTION.md §4 daemon row says a Linear `prompt` "only a human actor can post" and "Linear-UI replies are made by the proof task's own crew-operator"
- Risk: Linear forbids agents from creating `prompt` activities (analysis/linear-agents.md A3/C4), the host session has no browser profile, and M4-04 is a the-architect task with no `agent-browser`. The only actor that can reply in the session UI is crew-operator in M4-06, which depends on M4-04 being `done`. M4-04's host part therefore fails 3 times → `exhausted: M4-04` → M4 and everything after it never runs; run ends BLOCKED.
- Fix: In ROADMAP.md M4-04 verify column replace the host sentence with: "host (D-061, D-091): launch from a scratch issue on the host daemon; the attach capture filed in `tasks/M4-04/attach.txt` contains the rendered prompt; the `prompted` round trip is proven in-process by the container fixture only." Move the live reply assertion to M4-06 verify: "crew-operator posts the reply in the session panel and files `tasks/M4-06/reply-delta.json` (activity `createdAt` and the daemon `prompted` log timestamp); host (D-061): `jq -e '.delta_seconds <= 5'` on that file." Apply the same rule to M7-04 ("reply resumes the session" is posted by its own crew-operator, filed as a snapshot).

## R3-40 [blocker] M1-10 code capture via `agent-browser url` after the connection-refused redirect returns `chrome-error://chromewebdata/`, not the callback URL

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/ROADMAP.md`
- Where: M1-10 row (line 142): "the browser lands on a connection-refused page — expected; `agent-browser url` and extract `code`"; M1-07 row (line 139): "loopback; nothing serves it"; D-080
- Risk: Chromium replaces the main-frame URL with `chrome-error://chromewebdata/` when navigation to `http://localhost:53682/callback?code=…` fails with ERR_CONNECTION_REFUSED; Playwright/CDP-based `get url` then reports the error URL and the `code`/`state` query is lost. M1-10 is wave 7 on the critical path: it exhausts after 3 attempts and M1-09, M1-11, M1-12 and every M2+ task never run.
- Fix: In M1-10 scope replace the capture step with a deterministic loopback listener inside the operator container, started before clicking Authorize: `python3 -m http.server 53682 --bind 127.0.0.1 >tasks/M1-10/callback.log 2>&1 &` (or `socat TCP-LISTEN:53682,reuseaddr,fork SYSTEM:'printf "HTTP/1.1 200 OK\r\n\r\nok"'`), then read `code` and `state` from the `GET /callback?code=…&state=…` request line in `callback.log` (delete the log before commit; it holds a one-time code only). Add `python3` (or `socat`) to the crew-operator image in M1-05b and to the M1-10 preflight section. Keep the registered callback `http://localhost:53682/callback` unchanged (D-080), and change M1-07's "nothing serves it" to "served only by M1-10's one-shot listener for the duration of the authorize step".

## R3-41 [major] No preflight proof that the Linear workspace plan allows a new team and ~100 more issues (Free: 2 teams, 250 issues; Basic: 5 teams)

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/goal/PREFLIGHT.md`
- Where: §2 first item "The human's Linear account is a workspace admin … Proof: Linear Settings → Administration is visible" (line ~611); SPEC.md line 288 "the existing workspace is used (Q-019 adopted)"
- Risk: Linear caps teams by plan (Free 2, Basic 5, per https://linear.app/docs/teams) and Free workspaces at 250 issues. M1-09 creates team `JACKIN` in the existing workspace and M1-12 creates ~64 roadmap issues plus every proof run's scratch issues. If the workspace is Free with two teams (or Basic with five), team creation fails in the browser with no CLI-fixable cause; the session files it as `exhausted: M1-09` (a wrong classification: it is a billing action) and the run stops at wave 8. Client-credentials tokens also act only in public teams, and Free plans cannot make a team private, so the team must be public.
- Fix: Add to goal/PREFLIGHT.md §2, after the admin item: "- [ ] Workspace headroom for the run: the plan allows one more team and at least 150 more issues (Free: fewer than 2 teams and under 100 issues; Basic: fewer than 5 teams; Business/Enterprise: always). Proof: Settings → Plans shows the plan and issue count; Settings → Teams lists the count. A workspace at its team cap is a billing action, not a run-time defect." In ROADMAP.md M1-09 scope add "create `JACKIN` as a public team (client-credentials tokens reach public teams only; record the chosen team key in `tasks/M1-09/team.txt` and use it, not the name, in every GraphQL `team(key:)` lookup)". In goal/EXECUTION.md §6 add "a Linear plan limit (team or issue cap)" to the list of preflight defects.

## R3-42 [major] M1-12 never attaches `verify.sh`/refs to the issue, but M4-04 pre-fetches them "from the issue attachments or description" — daemon-path roadmap tasks run without a verify or references

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/ROADMAP.md`
- Where: M1-12 row (line 144): "description = the full `TASK.md` text" — no attachment step; M4-04 row (line 217): "plus, for roadmap issues, the task's `verify.sh` and reference files from the issue attachments or description"
- Risk: The container never sees this repository (D-086) and Linear has no file field beyond `attachments` (URL links). Nothing creates those links, so on the daemon path (every M2+ task from M3-05 on) `.jackin/task/verify.sh` and `.jackin/task/refs/*` are absent; the `goal` prompt "until sh .jackin/task/verify.sh container prints status: DONE" can never succeed, the agent loops or improvises a verify, and every daemon-dispatched task exhausts.
- Fix: In M1-12 scope add: "for every issue, `attachmentCreate` one link per staged file, idempotent by title: title `task.toml`, `verify.sh`, and `refs/<name>` for each `## References` entry, url `https://raw.githubusercontent.com/donbeave/ecosystem/main/tasks/<id>/<file>` (repository is public); reconcile titles on re-run." In M1-12 host verify add "every issue has attachments titled `verify.sh` and `task.toml`". In M4-04 scope replace "from the issue attachments or description" with "from the issue attachments titled `task.toml`, `verify.sh`, `refs/*`, fetched by the daemon over HTTPS (never inside the container) into `<workspace>/.jackin/task/` and `.jackin/task/refs/`", and add a fixture test for it in the M4-04 container verify.

## R3-43 [major] M3-05 host verify asserts a transient `pending` session that the daemon itself turns `active` in the same tick

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/ROADMAP.md`
- Where: M3-05 row (line 197), verify column: "delegates it from the host by `issueUpdate(delegateId)` and asserts a `pending` session appears within one interval (JSON filed)"; contradicts M2-02 ("uses the returned id for the ack") and D-091 ("a verify never asserts on a transient live state")
- Risk: With the live daemon running, the `agentSessionCreateOnIssue` call and the acknowledging `thought` happen inside one tick, so the session is `active` before the host's next GraphQL read; a poll every ≥1 s rarely catches `pending`. The assertion fails nondeterministically, burns the three attempts across L2→L3→L4, and `exhausted: M3-05` blocks M3-06, M3-07, M4, M5 …
- Fix: Replace the M3-05 host sentence with: "a live smoke on one scratch issue only, delegated from the host by `issueUpdate(delegateId)`; within one interval the session poll (`tasks/M3-05/session.json`) shows one session of the app user on that issue whose first activity is a `thought` by the app user (status `pending` or `active`, never asserted separately); `jackin daemon status` lists zero issues whose `tasks/README.md` row is `done`." Apply the same wording to M2-07's acceptance sentence ("session `active`").

## R3-44 [major] `agentSessions` tick read is not scoped to this app user; sessions of other agents (Linear's own Agent, any other installed app) are acked → activity-create errors every tick

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/ROADMAP.md`
- Where: M2-02 row (line 165): "the `agentSessions` page-and-diff read from `analysis/linear-agents.md` C5 (a session is new when it has no app-user activity, whether `pending` or `stale`)"; analysis C5 query selects no `appUser`
- Risk: `Query.agentSessions` has no filter and the analysis notes app-user scoping is "not explicitly documented". Linear Agent (bundled with every plan) and any other agent create sessions in the same workspace; each has "no app-user activity" by our app, so the daemon posts `thought` on a session it does not own, which Linear rejects ("Only updatable by the OAuth application that owns the session"), and retries every 5 s. The M5-06/M6-03 log-shape verifies ("every other line is `poll`") then fail with extra error lines, and the retry storm eats the shared 5,000/h budget.
- Fix: In M2-02 scope change the sessions read to `agentSessions(first:50, orderBy: updatedAt){nodes{id status updatedAt appUser{id} issue{id identifier} activities(first:1, filter:{...}) …}}` and add: "a node whose `appUser.id` differs from the stored app user id (`op://jackin/linear-workspace-<org>/app user id`) is dropped before any write; the unit fixture contains one foreign session and asserts zero writes for it." Add the same `appUser.id` guard to the M1-11 `api-delegation.json` poll so the M1 finding is not polluted by foreign sessions.

## R3-45 [minor] M1-09 leaves the type and position of the `review` and `merging` states unspecified; M2-04's "first `started` state" rule can land pickups in Review, which SPEC §6.1 makes non-dispatchable

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/ROADMAP.md`
- Where: M1-09 row (line 141): "a review state and a merging state per team"; M2-04 row (line 167): "move the issue to the first `started` state"; SPEC.md §6 step 1 (line ~105): dispatchable = type `unstarted`/`started` "and neither the repository's review nor merging state"
- Risk: M1-01 authors M1-09's TASK.md from this text and must guess the states' `type`; the daemon selects the lowest-position `started` state. If Review is created as `started` and ends up positioned before `In Progress` (a drag in the UI, or a workspace whose default `started` states differ), every ack moves the issue straight into Review, `dispatchable` flips false on the next tick, and the daemon stops the run and keeps the workspace for every issue — a silent stall on the daemon path that D-063 analysis cannot fix without editing Linear.
- Fix: In M1-09 scope write: "create `Review` and `Merging` as `started`-type states positioned after `In Progress` (and after any other pre-existing `started` state); record their ids in `tasks/M1-09/states.json`". In M1-09 verify add: "`workflowStates(filter:{team:{key:{eq:$key}}, type:{eq:\"started\"}})` sorted by `position` yields a state that is neither `Review` nor `Merging` first". In M2-04 scope change the rule to "the lowest-position `started` state excluding the configured review and merging state names".

## R3-46 [minor] Auto-disabled webhook (intentionally unreachable URL) may drop the app's agent status mid-run; no task re-checks or re-enables it

- File: `/Users/donbeave/Projects/tailrocks/ecosystem/ROADMAP.md`
- Where: M1-07 row (line 139): "on the fixed, intentionally unreachable URL `https://jackin-webhook.invalid/linear` (Linear auto-disables it after failed deliveries; polling is the correctness path, Q-015)"; analysis A1 step 2 lists an enabled Agent-session-events webhook as part of what makes an app an agent
- Risk: Linear retries a failed delivery 3 times (1 min, 1 h, 6 h) and then disables the webhook — about 7 hours after the first session event of M1-11, i.e. during M2/M3. If agent recognition depends on an enabled webhook, the app disappears from the Delegate picker and `app:assignable` delegation from the host (`issueUpdate(delegateId)`) is rejected, so M2-07, M3-05, M3-07 and every daemon-path dispatch fail with a cause the D-063 analysis cannot repair from the CLI (app settings need the browser profile) and the session misfiles as `exhausted:`.
- Fix: In M1-07 scope add: "file the Linear app settings URL in `tasks/M1-07/app-url.txt`". In goal/EXECUTION.md §1 step 4 add a standing check: "with the §4 token, `query { viewer { id canBeAssigned } }`"? — no: `User.canBeAssigned` — use: "`issueUpdate` of a scratch issue with `delegateId=<app user id>` succeeds (then revert)"; on failure the next crew-operator task (or a `M1-07 re-enable` step run as a `container` task on L6, cap 1) re-enables the webhook in the app settings and re-runs the check, never a preflight defect. In every crew-operator proof task (M2-07, M3-07, M4-06, M5-06, M6-03, M7-04) add a first checklist item: "open the app settings page; if the webhook shows Disabled, re-enable it and note it in the task folder".

## R3-47 [blocker] Stale Chromium SingletonLock in the shared operator profile kills every later crew-operator launch

- File: `concept/roles.md`
- Where: §3 Hooks row (line 133: "refuse if … `SingletonLock` PID alive"), §9 row 8 (line 321); ROADMAP.md M1-05b row (line 135, preflight.sh spec); goal/EXECUTION.md §4 `container` row (line 190, Teardown/End steps)
- Risk: The profile dir ~/.jackin/agent-browser-profile is bind-mounted rw and used as Chromium's persistent user-data-dir (AGENT_BROWSER_PROFILE). Chromium writes SingletonLock -> `<hostname>-<pid>`. Every teardown in EXECUTION §4 (retry, quota hop, re-sync, stuck, resume) is `jackin eject` with no `agent-browser close --all`, so the lock survives. In the next container the hostname (container id) differs: Chromium's process-singleton check reports 'profile in use on another computer' and refuses to start, and preflight.sh's 'PID alive' test cannot see the old PID across PID namespaces (or worse, a reused PID makes it refuse forever). Every crew-operator task after the first eject (M1-07, M1-10, M1-09, M1-12, every proof run) fails 3 attempts -> exhausted -> critical path BLOCKED.
- Fix: roles.md §3 Hooks/§9 row 8 and ROADMAP M1-05b: replace 'refuse if SingletonLock PID alive' with 'preflight.sh removes `$AGENT_BROWSER_PROFILE/SingletonLock`, `SingletonSocket`, `SingletonCookie` unconditionally (the crew-operator cap of 1 guarantees no live holder; a PID check is meaningless across PID namespaces)'. EXECUTION §4 container row, Teardown: add 'for crew-operator: `docker exec -u agent <name> agent-browser close --all` before `jackin eject`, then, when `docker ps --filter label=jackin.role=donbeave/crew-operator` is empty, `rm -f ~/.jackin/agent-browser-profile/Singleton{Lock,Socket,Cookie}` before every operator launch'.

## R3-48 [blocker] Reserve rule <20 % (or weekly <15 %) leaves Claude-lane tasks in a state that is neither runnable, waiting, nor blocked — run idles for up to a week

- File: `goal/EXECUTION.md`
- Where: §4 'Reserve rule for this session's own account' (lines 233-250: 'below 20, dispatch Codex-lane tasks only'); §3 'Runnable means' (lines 124-128); GOAL.md prompt line 50 (quota rule) and line 55/57 (BLOCKED conditions)
- Risk: Below 20 % (weekly <15 % holds until a reset up to 7 days away) every L1..L3 task — the whole critical path M2-01, M2-02, M2-04, M3-05, M3-07, M4-04, M5-01… — is runnable by depends_on but must not be dispatched. No rule turns it into a D-071 quota hop (that fires only after a failed attempt) and no rule sets `waiting`; PREFLIGHT-DEFECTS gets no row. Outcome (B) needs an open defect row, (A) needs DONE, and GOAL says 'never end otherwise', so the /goal loop spins with nothing to do (or the judge re-prompts forever). Live snapshot today: session 56 %, weekly 65 %, pace 'Runs out in 48m', extra usage 0 % — this state is reached in the first window.
- Fix: EXECUTION §4 reserve rule, append: 'A Claude-lane task that the reserve rule refuses is dispatched at once as a quota hop on the D-071 chain (L1/L2/L3 → L4 → L5 → L6) on the `container` path, recorded `L1 reserve → L4`, no attempt consumed; when every Codex home is busy or throttled the row is `waiting` with the earlier of the `session`/`weekly` `resets_at` from the snapshot. The reserve rule never leaves a dependency-runnable row `ready` with nothing dispatched.' §3 Runnable: add 'or, for a Claude lane under the reserve rule, a free Codex lane of its chain'. GOAL.md line 50: add 'or refused by the reserve rule'.

## R3-49 [major] Every ~/.claude consumer exhausts together; hop target L4 has cap 1 and 'next lane busy' is undefined — hops stall or misfile as `waiting`

- File: `goal/EXECUTION.md`
- Where: §4 'Fallback (D-057, D-071)' bullet (lines 220-232: 'Hops respect the D-056 caps'; 'A chain fully throttled makes the row waiting'); ROADMAP.md §5 prose (lines 584-590)
- Risk: L1..L3 containers (≤2), the subagents-path task, ≤3 host subagents and the session share one 5-hour bucket, so they hit 0 in the same minute. Up to three tasks then hop to L4 (`~/.codex`, cap 1, the busiest Codex lane in M1..M4 waves). 'Respect the caps' says only that two of them cannot launch; nothing says whether they wait for L4, skip to L5/L6, or become `waiting` (which needs 'throttled', not 'busy'). Meanwhile the session itself is limited (same account) and cannot execute any hop until auto-continue at reset, after which the hop is pointless but the rule still demands it: the finished container is torn down and relaunched on L4.
- Fix: EXECUTION §4 Fallback bullet, replace 'Hops respect the D-056 caps' with: 'A hop skips any lane of the chain whose account home has a live container and takes the first free one; when every lane is busy or throttled the row returns to `ready` at the head of the wave priority and is dispatched on the first slot that frees (never `waiting` for a busy lane, no attempt consumed). A `~/.claude` limit that also limits this session is handled after auto-continue: re-lane only a container whose pane still shows the limit message; one whose pane is idle at its prompt is verified (§5 step 5), not re-laned.' Mirror in ROADMAP §5 line 588.

## R3-50 [major] L5/L6 reset times are unreadable: `jackin usage host snapshot` ignores CODEX_HOME and reads only the default profile; Codex 'session' slot is a ~5-day window

- File: `goal/EXECUTION.md`
- Where: §4 Fallback bullet (lines 228-232: 'read the earliest reset (`jackin usage host snapshot --agent <claude|codex> --format json` where exposed … else 30 minutes)'); §4 reserve rule lines 249-250 ('Only a weekly-cap message with a reset more than 24 hours away is a preflight defect'); goal/PREFLIGHT.md §1 usage item (lines 71-79)
- Risk: Verified on this host: `CODEX_HOME=~/.codex-chainargos jackin usage host snapshot --agent codex` still reports the `~/.codex` account (alexey@zhokhov.com; the three homes are three distinct accounts), and `jackin usage host accounts|verify` error with 'supports snapshot only'. The Codex bucket with `status_slot: session` resets in '4d 23h', not 5 h. So a throttled L5/L6 lane falls to the fixed 30-minute backoff: for days, every 30 min a full teardown + cold launch (sync, image, cargo) — a crawl that also burns the other lanes' caps; and if the >24 h weekly clause is read as applying to Codex lanes, one L4 weekly cap files a preflight defect and the run ends BLOCKED although L5/L6/L1 have headroom.
- Fix: EXECUTION §4 Fallback bullet: replace the reset-read sentence with 'Claude reset: `jackin usage host snapshot --agent claude` (`session`/`weekly` `resets_at`). Codex reset per home: the snapshot serves only `~/.codex`; for every home read the `resets`/`try again` timestamp from the runtime's own limit message (pane capture or `tasks/<id>/codex.log`), record it in `tasks/<id>/quota.json`, and retry no earlier than that time; the 30-minute backoff is used only when no timestamp exists, and a lane whose recorded reset is in the future is not re-tried. The >24 h weekly clause applies to `~/.claude` only, and only once every lane of the chain is throttled.' PREFLIGHT §1: note that L5/L6 buckets are not visible to the host snapshot.

## R3-51 [major] Every container attempt is a cold cargo build: promised per-lane cargo/target volumes exist nowhere in the lane templates or workspace mounts

- File: `concept/roles.md`
- Where: §3 Mounts row (line 138: 'named volumes for `~/.cargo/registry`, `~/.cargo/git`, `target/` per lane'); ROADMAP.md M1-13 row (line 145, template contents) and M1-02a row (line 131); goal/EXECUTION.md §4 container row Workspace step (line 190: `jackin workspace create task-<id> … --mount ~/.jackin/managed/<id>`)
- Risk: `jackin workspace create --mount` takes bind specs only (`path[:ro]`, `src:dst`); the M1-02a/M1-13 templates carry sync_source_dir, [env], [docker.grants] and no mounts; the-architect's Dockerfile cargo caches are BuildKit `--mount=type=cache` (image build only). Each `task-<id>` instance therefore starts with an empty `~/.cargo` and `target/`, and every retry, quota hop, re-sync or resume teardown discards them. jackin is 29 crates: fetch + build + nextest per attempt, up to three the-architect containers per M3/M4 wave plus the host `cargo install` of two crates (§5 step 4a) contend for 18 CPUs. Waves stretch to hours; long link/LTO phases with no new pane line can trip the 30-minute stuck clock and trigger a hop that starts cold again.
- Fix: ROADMAP M1-02a and M1-13: the lane templates add `[[mounts]]` bind directories per lane — `~/.jackin/cache/L<n>/cargo-registry → /home/agent/.cargo/registry`, `…/cargo-git → /home/agent/.cargo/git`, `…/target → /home/agent/cargo-target` — and `[env] CARGO_TARGET_DIR=/home/agent/cargo-target` for builder lanes (one directory per lane: the per-Codex-home cap 1 and the two ~/.claude lanes L1/L2/L3 being distinct directories prevent two writers). EXECUTION §4 Workspace step: 'merge the template mounts too (`jackin workspace edit task-<id> --mount src:dst` per line)'. roles.md line 138: 'named volumes' → 'per-lane host bind directories from the lane template (jackin mounts are bind-only)'. §2 stuck rule: 'an active `cargo`/`rustc` process in `docker top` counts as evidence'.

## R3-52 [major] Session-side 5-second model polling of tmux panes drains the very ~/.claude bucket the reserve rule protects

- File: `goal/EXECUTION.md`
- Where: §4 container row (line 190: 'Poll `tmux capture-pane -p -t <id>` every 5 s until … (15-minute budget for a cold build)'; 'Read progress with `capture-pane -p -S -200`'); §2 stuck rule (lines 99-108)
- Risk: As written the poll is a model turn every 5 s: up to 180 turns per launch, ~100 launches (tasks + hops + throwaways) plus progress reads and 30-minute stuck checks — all billed to the shared `~/.claude` 5-hour and weekly buckets (today's snapshot already shows the session pace 'Runs out in 48m' and Extra usage at 0 % remaining, so there is no overflow). This is the dominant quota sink of the run and pulls the account under the 40/20 % thresholds by itself, which then throttles L1..L3 containers and host subagents. The 15-minute budget also has no stated expiry action.
- Fix: Replace the polling sentence with: 'Wait without model turns: `timeout 900 sh -c 'until tmux capture-pane -p -t <id> | grep -qE "<runtime prompt regex>"; do sleep 5; done'` (or a Monitor until-loop); on expiry, if the pane's last 20 lines still change (`docker build`/`pull` output) extend once by 15 min, else apply the stuck rule. Progress is read by a Monitor loop on `wc -l` of the capture and on `tasks/<id>/verify.container.out`, at most once per 5 minutes per task; the session opens a capture only when the loop returns.' Add to §2: 'session tool turns spent waiting are minimized: never poll a pane in a model loop'.

## R3-53 [minor] Reserve rule reads only the `session`/`weekly` slots; the model-specific bucket (Fable, L1's model) runs out first and is invisible to it

- File: `goal/EXECUTION.md`
- Where: §4 reserve rule (lines 233-240: 'read the `session` bucket's `remaining_percent`'; 'a `weekly` bucket below 15'); goal/PREFLIGHT.md §1 lines 76-79
- Risk: Live snapshot: `Session` 56 %, `All models` 65 %, but the `Fable` bucket (label 'Fable', `status_slot: null`) is at 44 % and lower than both. L1 (Fable) containers, the L1 subagents-path tasks and host subagents on the same model hit the model cap while the reserve check passes; D-071 then calls it 'a model-specific limit with account headroom' and requires a failed attempt, a teardown and a relaunch on L2 — each a cold container (see cargo finding) — instead of a pre-launch model swap.
- Fix: §4 reserve rule: 'read three buckets: `status_slot == "session"`, `status_slot == "weekly"`, and the bucket whose `label` names the lane's model family (`Fable` for L1, `Opus` for L2, `Sonnet` for L3); the lowest `remaining_percent` drives the 40/20 thresholds. When only the model bucket is under 20, dispatch the task on the next Claude lane (`L1 → L2`, model-only hop, recorded `L1 model-cap → L2`) before launch; the host subagents use the model of the lane with the most headroom.' PREFLIGHT §1: same three-bucket reading in the proof.

## R3-54 [minor] The six-container cap and the 'same account home' check count DinD sidecars and cannot see account homes

- File: `goal/EXECUTION.md`
- Where: §3 lines 118-122 ('The caps count every running container … before every load the session checks `docker ps` for a live container on the same account home or role'); §1 step 2 line 17-19 (`docker ps --format '{{.Names}}'`); §4 container row line 190 (`docker ps --format '{{.Names}}' | grep -- "-task-<id slug>-"`)
- Risk: Every builder-lane load with `dind = "privileged"` (M1-13 template) creates two containers: the role container and a `jackin.kind=dind` sidecar. A session that counts `docker ps` names sees 2× the load and stops dispatching at three tasks (six names), halving every wave from M3 on; and no container name or label carries the account home, so 'a live container on the same account home' has no executable form and the per-Codex-home cap 1 is enforced by guesswork.
- Fix: §3: 'Count only `docker ps --filter label=jackin.managed=true --filter label=jackin.kind=role --format '{{.Names}}'`; map each name to its `task-<id>` (the `-task-<id slug>-` segment) and to its lane via `tasks/<id>/task.toml` `lane`, then to the account home via the lane template; sidecars (`jackin.kind=dind`) never count.' Apply the same filter in §1 step 2 and in the §4 container-name capture.

## R3-55 [blocker] Headless launch (M3-01) yields a container with no agent: the capsule spawns its first pane only after an attach Hello

- File: `ROADMAP.md`
- Where: M3-01 row (line 192) scope and host verify; M3-05 row (line 197) 'launch (M3-01)'; M4-01 row (line 214) 'positional prompt … at launch'
- Risk: `crates/jackin-capsule/src/daemon.rs:1188` keeps `pending_initial_spawn` until the first attach client sends `Hello` with terminal dimensions; `hardline` is what starts the agent today. M3-01 as written ('non-TTY entry … returns the instance identity') only removes the host TUI; a daemon-launched instance (M3-05) then sits with zero sessions until someone attaches. The M3-01 host verify masks this because `jackin hardline <instance id>` is exactly the Hello that spawns the pane. Downstream: M4-01 prompt-at-launch has no PTY to land in, M4-04 'capture contains the rendered prompt' and 'reply reaches the PTY within one interval' fail, M5-03 marks every run `stuck` within 5 minutes, M3-07/M4-06/M5-06 proofs exhaust their three attempts, and the daemon path never becomes usable — a 'size L' task hides a capsule protocol change.
- Fix: In the M3-01 scope append: 'Headless first session: when launched without an attach client (`LoadOptions.headless = true`), the host writes `JACKIN_HEADLESS_ROWS=50`/`JACKIN_HEADLESS_COLS=200` into the launch env and the capsule spawns the initial pane at startup instead of deferring to the first attach Hello (`crates/jackin-capsule/src/daemon.rs` `pending_initial_spawn`); a later attach resizes it.' Change the M3-01 host verify to: 'the e2e test launches with no TTY, then — before any `hardline` — `docker exec <container> jackin-capsule agents --format json` lists exactly one agent whose state is `working` or `idle`; only then `jackin hardline <instance id>` attaches to that same session.' Add the same 'no attach needed' sentence to the M3-05 container test (dispatch fixture asserts the headless flag is set).

## R3-56 [blocker] Per-task saved workspace has no `/workspace`: workdir and mount mirror the host path, so `docker exec -w /workspace`, `.jackin/task/…`, and the-architect's mise trust all miss

- File: `goal/EXECUTION.md`
- Where: §2 'Workspace' bullet (lines 130–136) and §4 `container` row (line 190): `jackin workspace create task-<id> --workdir ~/.jackin/managed/<id> --mount ~/.jackin/managed/<id>`, `cp … <ws>/.jackin/task/`, `docker exec -u agent -w /workspace …`; same text in DECISIONS.md D-085/D-086 (2), ROADMAP.md M1-02a (line 131) and M1-13 (line 145) probe workspaces
- Risk: `crates/jackin-config/src/mounts.rs:30-33`: a mount spec without `:dst` uses `dst = src`, and `workspace create` stores `resolve_path(workdir)`, so the container cwd is `/Users/donbeave/.jackin/managed/<id>` and nothing is mounted at `/workspace` (`attach.rs:675` uses `manifest.workdir`). Consequences: (a) every `docker exec -u agent -w /workspace … sh .jackin/task/verify.sh container` fails with 'chdir … no such file or directory', so no container-path task can ever produce `verify.container.out`; (b) the prompt line `Read .jackin/task/TASK.md` is relative to the cwd `~/.jackin/managed/<id>`, but §2 stages the folder under `~/.jackin/managed/<id>/<repo>/.jackin/task/`, one level deeper — the agent cannot find its task; (c) `jackin-the-architect/Dockerfile:36` sets `MISE_TRUSTED_CONFIG_PATHS=/workspace:/tmp/jackin-mise`, so in the mirrored path `mise install` refuses the untrusted `mise.toml` and the Rust toolchain never activates for M1-02..M5 jackin tasks. Every container-path task from wave 2 on exhausts.
- Fix: Replace the create command with `jackin workspace create task-<id> --workdir /workspace --mount ~/.jackin/managed/<id>/<repo>:/workspace` (operator/evidence tasks: `--mount ~/.jackin/managed/<id>:/workspace`; reviews add `:ro`), state in §2 that `<ws>` is the mount source of `/workspace` (so `<ws>/.jackin/task/` appears as `/workspace/.jackin/task/`), keep `-w /workspace` in the `docker exec` line, and require `jackin load … --dry-run --format json | jq -e '.data.mounts[]|select(.dst=="/workspace")'` next to the `.data.workspace` assertion. Apply the same `:/workspace` form to the M1-02a `task-probe` and M1-13 `probe-L<n>` workspaces and to D-085's text.

## R3-57 [blocker] M1-05d's primary verify calls `jackin config env list … --format json`, a flag that does not exist, and the table it prints carries no on-demand marker

- File: `ROADMAP.md`
- Where: M1-05d row (line 137) 'primary: `jackin config env list --role donbeave/crew-operator --format json` lists `OP_SERVICE_ACCOUNT_TOKEN` with its on-demand marker'; concept/task-format.md lines 111–113; DECISIONS.md D-090 (3) 'M1-05d's verify is authoritative on `jackin config env list --format json`'
- Risk: `crates/jackin/src/cli/config.rs:123-127`: `EnvCommand::List` has only `--role`; `jackin config` has no global `--format`, so clap exits 2 on the unknown flag. Even without the flag, `config_cmd.rs:390-408` renders `EnvValue::as_display_str()` (`env_value.rs:55-61`), which prints the `op` path for an `OpRef` and never says `on_demand`. M1-05d is a `host` task with no lane chain, so three failing attempts exhaust it inside M1; it gates M1-03, M1-06, M1-08, M1-13 and therefore the whole run ends BLOCKED at wave 4.
- Fix: Rewrite the verify sentence to: 'primary: `grep -E 'OP_SERVICE_ACCOUNT_TOKEN *= *\{.*on_demand *= *true' "${JACKIN_CONFIG_DIR:-$HOME/.config/jackin}/config.toml"` matches under `[roles."donbeave/crew-operator".env]`; secondary: `jackin config env list --role donbeave/crew-operator | grep -q OP_SERVICE_ACCOUNT_TOKEN` (proves the edited file still parses and the key is scoped to the role; the table shows the `op` path, jackin prints no on-demand marker; a `--format json` for `config env list` is a D-046 gap owned by M3-01)'. Mirror the wording in concept/task-format.md and D-090 (3).

## R3-58 [major] Container-name lookup `grep -- "-task-<id slug>-"` can never match: jackin strips every non-alphanumeric from the workspace component

- File: `goal/EXECUTION.md`
- Where: §4 `container` row (line 190): 'record the container: `docker ps --format '{{.Names}}' | grep -- "-task-<id slug>-"`'
- Risk: `crates/jackin-instance/src/naming.rs:85-96` (`compact_component`) keeps only ASCII alphanumerics and lowercases, so workspace `task-M2-01` becomes `taskm201` and the container is `jk-<id8>-taskm201-thearchitect`. A hyphenated pattern matches nothing, `tasks/<id>/container.txt` is never written, and every later step keyed on it (`docker exec` verify, `jackin eject "$(cat container.txt)"`, resume check) has no target; the fallback `jackin status --format json filtered by workspace` is only a parenthetical.
- Fix: Replace the grep with: `slug=$(printf %s "task-$id" | tr -cd '[:alnum:]' | tr '[:upper:]' '[:lower:]'); docker ps --filter label=jackin.managed=true --format '{{.Names}}' | grep -- "-${slug}-" | head -1 > tasks/<id>/container.txt` and state that the workspace component is lowercase alphanumerics only (`naming.rs` `compact_component`); make `jackin status --format json` the authoritative source when two names match.

## R3-59 [major] Reserve rule reads `.session.remaining_percent` / a `weekly` bucket from a JSON that has neither key

- File: `goal/EXECUTION.md`
- Where: §4 'Reserve rule for this session's own account' (lines 232–247); goal/PREFLIGHT.md §1 lines 76–77 ('shows the `session` bucket with `remaining_percent` above 40'); DECISIONS.md D-090 (6)
- Risk: `jackin usage host snapshot --agent claude --format json` prints `UsageProjectionV1` (`crates/jackin/src/cli/usage.rs:243`): `.providers[]` → `.accounts[]` → `.windows[]`, each window with `category` ∈ {`long_range`,`model`,`session`,`other`} (`usage_broker.rs:238-247`, snake_case) and `remaining_percent: u8|null` (`:327`). There is no top-level `session` or `weekly` bucket. A model-judged session applying the rule literally gets `null` at every dispatch and either never shrinks the caps (burning the shared `~/.claude` budget the host session needs) or treats null as 'below 20' and starves every Claude lane; the PREFLIGHT proof command fails at every session start.
- Fix: Give the exact reads: `pct=$(jackin usage host snapshot --agent claude --format json | jq '[.providers[]|select(.provider_id=="claude")|.accounts[]|.windows[]|select(.category=="session")|.remaining_percent|values]|min')` for the session bucket and the same with `select(.category=="long_range")` for the weekly bucket; state that an empty result (no window) counts as 'below 40' until one refresh succeeds and is logged in the result cell, never as a defect. Update PREFLIGHT §1 to that command and D-090 (6) to name the `category` field.

## R3-60 [major] M1-13 verify expects `jackin usage cache accounts` to list four accounts after throwaway loads, but a load never writes the host cache

- File: `ROADMAP.md`
- Where: M1-13 row (line 145) verify: '`jackin usage cache accounts --format json` lists four distinct provider accounts after the throwaway loads'
- Risk: `~/.jackin/data/daemon/accounts.db` (`crates/jackin/src/cli/usage/store.rs:39-41`) is written only by `jackin usage <instance> accounts --sync-host-cache` or `jackin usage host snapshot --sync-host-cache` (`store.rs` is the sole writer; `cache accounts --sync-host-cache` is rejected, `usage.rs:409-411`). Launching and ejecting six throwaways records nothing, and `host snapshot` probes only the default `~/.claude`/`~/.codex` homes, never `~/.codex-chainargos*`. The verify fails on all three attempts; M1-13 gates M1-09, M1-12, M3-05, M3-07, M4-04, M4-06.
- Fix: Add to the M1-13 scope: 'while each throwaway instance is up, run `jackin usage "$(cat tasks/M1-13/container-L<n>.txt)" accounts --sync-host-cache --format json > tasks/M1-13/usage-L<n>.json` (this is the only writer of the host cache); the capture files are the per-lane account record'. Keep the verify but qualify it: 'after those six `--sync-host-cache` runs'.

## R3-61 [minor] `jackin workspace delete task-<id>` is not a command; the subcommand is `remove`

- File: `goal/EXECUTION.md`
- Where: §4 `container` row (line 190) 'End. … `jackin workspace delete task-<id>` after the `PROGRESS.md` row'; DECISIONS.md D-085 'The workspace is deleted after the `PROGRESS.md` row'; ROADMAP.md M1-02a (line 131) 'the workspace is deleted afterwards'
- Risk: `crates/jackin/src/cli/workspace.rs:244` defines `Remove`; `delete` is a clap error at the end of every container-path task and in the M1-02a verify's cleanup. Leftover `task-<id>.toml` profiles also make a retry's `workspace create` fail with 'already exists', which §4 handles only through `workspace show`.
- Fix: Replace with `jackin workspace remove task-<id>` in EXECUTION §4, D-085, and M1-02a ('deleted afterwards with `jackin workspace remove task-probe`'); add 'a retry reuses the existing profile after re-merging the lane template' to the Workspace step.

## R3-62 [blocker] Runner has a third terminal outcome the plan never handles: goal is CLEARED (session stops silently) on context limit or an evaluator 'impossible' verdict

- File: `GOAL.md`
- Where: GOAL.md paragraph 3 ("The run ends in one of two outcomes") and goal/EXECUTION.md §7 "The run ends in exactly one of two outcomes"; §1 opening sentence
- Risk: Claude Code 2.1.247 /goal (verified in the binary): the goal is a prompt-based Stop-hook evaluator with outcomes met / not_met / impossible / error. On `impossible` the goal is cleared (`goal_status met:false failed:true`) and the session stops; on turn reasons `blocking_limit`, `prompt_too_long`, `rapid_refill_breaker` (repeated auto-compact refills), auth, billing, model_unavailable the goal is cleared with "Goal cleared after an unrecoverable error (...). Run /goal again to continue." Nothing re-arms it; the human is absent, so the run silently halts with containers still running, and the plan's "the judge cannot accept it and re-prompts" (D-083) is false in these cases. The prompt tells the session to read ROADMAP.md (117 KB), DECISIONS.md (118 KB), SPEC.md and the 36 KB EXECUTION.md after every compaction, which is exactly what trips the rapid-refill breaker over an 81-task run. The evaluator also "cannot run commands or read files", so a typed `status: DONE` line would satisfy it.
- Fix: (1) Make the loop deterministic and independent of the evaluator: add `.claude/settings.json` to this repository with `{"hooks":{"Stop":[{"hooks":[{"type":"command","command":"sh goal/stop-gate.sh"}]}]}}` and `goal/stop-gate.sh` (POSIX sh, reads the hook JSON from stdin): run `sh verify.sh`; exit 0 when its last line is `status: DONE`, or when it is `status: BLOCKED …` (see the verify.sh census finding) and the transcript's last assistant text (jq over `transcript_path`) starts with `GOAL BLOCKED`, or when `goal/HALT` exists (human kill switch); otherwise print `{"decision":"block","reason":"Goal not reached: <verify last line>. Repeat goal/EXECUTION.md §1 steps 2-6."}`. Amend AGENTS.md rule 1 and D-069 to allow `goal/stop-gate.sh` and `.claude/settings.json`. (2) Add to the GOAL.md prompt (Non-negotiable): "Never Read ROADMAP.md, SPEC.md, DECISIONS.md, analysis/*.md or a full tmux capture in this session: grep the task row / decision id, read at most the last 60 capture lines; subagents read whole files." and in EXECUTION §1 "after compaction re-read only GOAL.md and EXECUTION.md §1, §5". (3) GOAL.md paragraph 3: add "If Claude Code prints `Goal cleared after an unrecoverable error`, the Stop hook keeps the loop alive; if the session still ends, the human re-runs the invocation line."

## R3-63 [blocker] Runner re-prompt after BLOCKED silently opens a new attempt epoch for every `exhausted:` row: unbounded retry loop with no human involved

- File: `goal/EXECUTION.md`
- Where: §1 step 2 sentence "An `exhausted: <id>` row has no proof command … if its task row is still `blocked` and `Resolved` is empty, fill `Resolved` with this session's start timestamp, set the task to `ready`, and open a new attempt epoch"; GOAL.md paragraph 3 "an `exhausted:` row starts a new attempt epoch, D-084"; GOAL.md prompt clause (b) "(a resume starts a new epoch, D-084)"; §6 last paragraph; PREFLIGHT-DEFECTS.md header; D-084 (1)
- Risk: D-083 itself expects the model-judged evaluator may re-prompt after a `GOAL BLOCKED` turn (it is judged from the conversation, so a not_met is likely). GOAL.md then orders §1 steps 1–4, and step 2 closes every `exhausted:` row and sets the task `ready` because `Resolved` is empty — the session cannot tell a runner re-prompt from a human re-run. The exhausted task re-runs `limits.attempts` more attempts (each with a D-063 analysis), exhausts again, files a new row, prints GOAL BLOCKED, is re-prompted, opens epoch 3 … forever, burning every lane's quota. With the deterministic Stop hook of the previous finding the same loop exists unless the epoch reset needs a human act.
- Fix: EXECUTION §1 step 2: replace the sentence with "An `exhausted: <id>` row is closed only by the human: when its `Resolved` cell is non-empty (the human writes the UTC time they fixed the cause or edited the task) and the task row is still `blocked`, set the task to `ready` and open a new epoch (`epoch 2: 0/3`); a row with an empty `Resolved` cell keeps its task `blocked` — the session never fills `Resolved` on an `exhausted:` row." Same change in §6 ("is closed by the next start itself" → "is closed only when the human fills `Resolved`"), PREFLIGHT-DEFECTS.md header, goal/PREFLIGHT.md §6, D-084 (1). GOAL.md paragraph 3: "an `exhausted:` row whose `Resolved` cell the human filled starts a new epoch". Prompt clause (b): replace "(a resume starts a new epoch, D-084)" with "(new epoch only after the human fills Resolved, D-084)" — prompt goes from 3971 to 3989 chars, still under the cap.

## R3-64 [major] Outcome (B) — and half of (A) — is prose the evaluator cannot check: 'no row in-progress/waiting, nothing runnable, PROGRESS row per task, tree clean' are not in any script output

- File: `verify.sh`
- Where: root verify.sh (prints only done/pending); GOAL.md invocation line clause (B) "a final message whose first line is `GOAL BLOCKED`, followed by the open PREFLIGHT-DEFECTS.md rows and the literal output of `sh verify.sh` (last line `status: PENDING <n> remaining`)"; EXECUTION §6 "The run ends BLOCKED only when…" and §7
- Risk: The /goal evaluator judges the transcript only. The quiet-run preconditions of (B) are model-asserted, so a session that files one defect row can print GOAL BLOCKED while 60 rows are `ready` and runnable, or while containers are `in-progress`, and the evaluator (or the Stop hook) has no fact to reject it; `PENDING <n>` is printed for every non-DONE state, so the same verify output backs a legitimate and an illegitimate BLOCKED. Conversely for (A) "PROGRESS.md has a row per task; tree clean" is unverifiable.
- Fix: Extend root verify.sh (still read-only): parse the `Depends on` column of tasks/README.md and PREFLIGHT-DEFECTS.md, print a census line `rows: done a ready b in-progress c waiting d blocked e planned f; runnable g (deps done, M1-12 rule D-088, caps ignored); open defects h; progress rows missing i; dirty files j` (`git status --porcelain | wc -l`), and choose the last line: `status: DONE` only when remaining=0, i=0, j=0; `status: BLOCKED <n> remaining` only when c=d=g=0 and h>0; otherwise `status: PENDING <n> remaining`. Change the invocation clause (B) to "(B) the literal output of `sh verify.sh` whose last line is `status: BLOCKED <n> remaining`, preceded in the same message by a first line `GOAL BLOCKED` and the open PREFLIGHT-DEFECTS.md rows" (invocation stays ≤ 500 chars — the runner's `Goal condition is limited to` cap; it is 376 now). Update EXECUTION §6/§7, D-083, PREFLIGHT-DEFECTS header and the GOAL.md prompt's (B) sentence to name `status: BLOCKED`.

## R3-65 [major] Attempt counter of an in-progress task lives only in the model's context: compaction or a session restart resets it and the exhaustion cap never fires

- File: `goal/EXECUTION.md`
- Where: §1 step 2 "anything else restarts it as a new attempt counted in the result cell (D-070)"; §5 step 5 "after `limits.attempts` attempts … in the current epoch"; §5 PROGRESS.md row format; PROGRESS.md header ("One row per finished, blocked, or waiting task")
- Risk: The only persisted attempt record is the PROGRESS.md result cell, which is written when the task finishes. For a task whose attempts crash or bloat the session (a capture that trips compaction, a `tmux` pane that dies every time), every resume/compaction starts at attempt 1 again: the D-070 cap is never reached, the task is never `exhausted`, BLOCKED never fires, and the run loops on one task indefinitely. D-070's "a resume after a crash keeps the count" has no file behind it.
- Fix: Add `tasks/<id>/attempts.log` (append-only, committed with each README row change): §5 step 3 appends `<epoch> <n> <lane> <path> <UTC> dispatched`; step 5 appends `<epoch> <n> <verdict> <verify.out sha>`. §1 step 2 and §5 step 5 read `n` from this file, never from memory; the PROGRESS.md result cell summarises it. Change §1 step 2 to "restarts it as attempt n+1 per `tasks/<id>/attempts.log`" and PROGRESS.md header accordingly; add the file to concept/task-format.md's folder listing.

## R3-66 [major] No durable post-compaction / re-prompt trigger: the only re-entry rule is a sentence inside the prompt, and §1 step 1's dirty-tree rule misfires on a mid-run re-prompt

- File: `GOAL.md`
- Where: GOAL.md prompt sentence "after any context compaction repeat goal/EXECUTION.md §1 steps 2-3"; AGENTS.md (no run-mode rule; it is the CLAUDE.md symlink re-injected after compaction); EXECUTION §1 step 1 "Refuse to work on a dirty tree: commit or stash-drop leftovers from a crashed run first, then record what happened in PROGRESS.md"
- Risk: After compaction the runner re-injects CLAUDE.md/AGENTS.md, not the /goal argument or the GOAL.md prompt; the compaction summary may omit the re-entry sentence, so the session continues from a stale wave picture (double dispatch of an id, or a finished container never verified). When the evaluator re-prompts mid-run (not after BLOCKED), §1 applies to "every run, including … a re-prompt": step 1 treats the uncommitted `tasks/<id>/verify.container.out`, `container.txt` and staged evidence of live in-progress rows as crash leftovers and commits or drops them, corrupting attempts.
- Fix: AGENTS.md: add "## Run mode (GOAL.md)" — "When `tasks/README.md` has any row not `planned`, this session is the host session of GOAL.md: before dispatching or verifying anything after a context compaction or a runner re-prompt, re-read GOAL.md and goal/EXECUTION.md §1 and run its steps 2–3; the wave state lives in the files." EXECUTION §1 step 1: append "On a re-prompt or after compaction while any row is `in-progress`, skip the dirty-tree rule for files under `tasks/<in-progress id>/`; they are live evidence, committed at §5 step 7." Keep the prompt sentence, but point it at AGENTS.md so both agree.

## R3-67 [minor] Final-message shape contradicts the evaluator: summary list after the verify output breaks 'turn ends with the literal output', and (A) has no first-line marker

- File: `goal/EXECUTION.md`
- Where: §7 "In both cases the message also lists: tasks done, preflight defects open and resolved, fallbacks and waits taken, repositories touched …, and the Linear project URL"; GOAL.md invocation clause (A) "the current turn ends with … the literal output of `sh verify.sh`"
- Risk: The evaluator reads the conversation only. §7 orders a multi-item report in the same message with no position rule; if it follows the verify block, the message no longer "ends with" the literal output and a strict evaluator returns not_met → re-prompt → the session prints the same thing → loop; if the verify output is only in a tool result and the assistant text is the summary, the evaluator may not see it at all. (B) has a first-line marker, (A) has none, so the two outcomes are checked by different shapes.
- Fix: EXECUTION §7: "The final assistant message has a fixed shape: line 1 `GOAL COMPLETE` or `GOAL BLOCKED`; then the report list; for BLOCKED then the open PREFLIGHT-DEFECTS.md rows verbatim; then, as the last lines with nothing after them, the output of `sh verify.sh` run by this session in the same turn, copied verbatim from the tool result." GOAL.md invocation (A): "a final message whose first line is `GOAL COMPLETE` and whose last lines are the literal output of `sh verify.sh` with last line `status: DONE`". Mirror in the prompt's Done paragraph.

## R3-68 [minor] Host environment preconditions for the runner are missing from PREFLIGHT §1: interactive session only, and the global RTK PreToolUse hook rewrites every evidence command

- File: `goal/PREFLIGHT.md`
- Where: §1 Standing items (no item on the Claude Code session mode or on `~/.claude/settings.json` hooks); EXECUTION §5 step 5 "output to tasks/<id>/verify.container.out … `verify.out` is `cat verify.container.out verify.host.out`"
- Risk: The /goal Stop-hook evaluator is armed only when the session is interactive (`!isNonInteractiveSession`): a run started with `claude -p`/`--print`, a headless wrapper, or a jackin capsule TUI with the flag set never loops. The host's `~/.claude/settings.json` has `PreToolUse Bash → rtk hook claude`, which rewrites `cat tasks/<id>/verify.out` to `rtk read …`, `grep` to `rtk grep`, `git status --porcelain`/`git fetch`/`gh pr checks --watch` to `rtk …` filtered variants: what the session sees and copies as "literal output" is a token-compressed rendering, and `rtk read` elides long files, so the `status:` last line or a secret-scan hit can be dropped from the evidence the session files and commits.
- Fix: PREFLIGHT §1: add "The run is started in the interactive Claude Code TUI in this repository (never `claude -p`, `--print`, or a wrapper); proof: the `/goal` line prints `Goal set: …`." and "No Bash-rewriting `PreToolUse` hook for the run: `jq '.hooks.PreToolUse' ~/.claude/settings.json` prints `null` (the RTK hook is removed or moved to a profile not used for the run)." EXECUTION §5 step 5: "evidence is written by shell redirection, never by reading a tool's rendered output; the session reads `verify.out` with `tail -n 1` and files the file, not the tool output."

## R3-69 [blocker] crew-operator preflight.sh refuses to start before M1-05d mounts the profile: M1-05b throwaway load exits at entrypoint, M1-05b↔M1-05d cycle blocks wave 3

- File: `ROADMAP.md`
- Where: M1-05b row (line 135) scope + verify; concept/roles.md §3.1 row 'Hooks' (operator preflight.sh) and §3.2 operator sketch comment
- Risk: M1-05b's preflight.sh 'refuses if profile unwritable' and 'refuses with a message naming the M1-06 re-login' when `open https://linear.app` lands on a login page and `state load /home/agent/.agent-browser-profile/state.json` fails. The profile mount is added only in M1-05d (`jackin config mount add … --scope donbeave/crew-operator`), which depends_on M1-05b. jackin's entrypoint runs `run_hook preflight` and `exit $rc` on non-zero (jackin docker/runtime/entrypoint.sh:12-28,183-184), so the M1-05b verify launch (`jackin load donbeave/crew-operator --agent claude` from workspace task-M1-05b, mount = ~/.jackin/managed/M1-05b only) never reaches the capsule prompt: no state.json, no profile dir → refuse → container exits → verify fails 3x → `exhausted: M1-05b` → M1-05d, M1-06, M1-03, M1-13 and the whole run stay blocked in wave 3.
- Fix: ROADMAP M1-05b scope: replace 'refusing with a message naming the M1-06 re-login if it still does' with 'preflight.sh never exits non-zero for a missing or logged-out session: it refuses only when a live `SingletonLock` PID exists; when `/home/agent/.agent-browser-profile/state.json` is absent or `get url` is still a login page it prints `[operator-preflight] WARNING: no browser session — run the M1-06 re-login` and exits 0; the logged-in assertion belongs to M1-06 and to each operator task's own checklist'. Same wording in concept/roles.md §3.1 'Hooks' (operator column). M1-05b verify: add `--mount ~/.jackin/agent-browser-profile:/home/agent/.agent-browser-profile` to the throwaway load command so the state file is present when it exists, and add `docker logs <container> | grep -q 'operator-preflight'` as the evidence line.

## R3-70 [blocker] Standing check in PREFLIGHT §1 carries an unresolvable `<TAILROCKS_SKILLS_SHA>` placeholder: it fails at every session start and becomes a preflight defect before wave 0

- File: `goal/PREFLIGHT.md`
- Where: §1 last bullet (lines 142-145): 'Every ref the roles pin resolves … `gh api repos/tailrocks/tailrocks-skills/commits/<TAILROCKS_SKILLS_SHA>` … with the SHAs of `concept/roles.md` §3.1'
- Risk: concept/roles.md §3.1 names no tailrocks-skills SHA (row 'Codex skills': 'the SHA of HEAD is captured by M1-05a and recorded in its folder'), and ROADMAP M1-05a only says 'recorded in tasks/M1-05a/' without a file name. EXECUTION §1 step 4 re-runs every §1 check at each start and files a failed check the session cannot fix as a preflight defect (§6). With a literal placeholder the `gh api` call returns 404 at the very first start (tasks/M1-05a/ does not exist yet) → open PREFLIGHT-DEFECTS row → the run ends BLOCKED before M1-01, and every re-run repeats it. Verified: tailrocks-skills has no tag v0.28.0 (newest v0.25.0), HEAD becfafbe…; review-crucible commit 5936f0e0… exists.
- Fix: Replace the bullet with: '- [ ] Pinned refs resolve (D-078): `gh api repos/tailrocks/review-crucible/commits/5936f0e069946db0ee4408e72122b134800336e4 --jq .sha` prints the SHA. The tailrocks-skills pin is created by M1-05a: from wave 3 on the check is `gh api repos/tailrocks/tailrocks-skills/commits/$(cat tasks/M1-05a/tailrocks-skills-sha.txt) --jq .sha`; while that file does not exist the check is skipped, never a defect.' In ROADMAP M1-05a scope change 'recorded in `tasks/M1-05a/`' to 'recorded as the 40-hex line `tasks/M1-05a/tailrocks-skills-sha.txt` (captured with `gh api repos/tailrocks/tailrocks-skills/commits/HEAD --jq .sha` at task start)', and in M1-05c add `tasks/M1-05c/review-crucible-sha.txt` = `5936f0e069946db0ee4408e72122b134800336e4` so the verify's 'equals the SHA recorded in tasks/M1-05c/' has a file to read.

## R3-71 [blocker] M1-05a..c 'inside the role' checks and `jackin role validate` have no runnable home: the container part docker-execs into the-architect (no jackin binary, has cargo), not into the throwaway crew container

- File: `goal/EXECUTION.md`
- Where: §4 `container` row ('Container verify … docker exec … "$(cat tasks/<id>/container.txt)" sh .jackin/task/verify.sh container') and §5 step 5; ROADMAP M1-05a/M1-05b/M1-05c verify columns (lines 134-136)
- Risk: For M1-05a..c the task container is `the-architect` (role column), but the verify text is 'validate passes; `jackin load donbeave/crew-<p> --agent claude` starts; inside: …no cargo on PATH / no op / mise install no-op in a termrock checkout / $CODEX_HOME/agents populated after source.sh'. (1) the-architect's image ships no `jackin`/`jackin-role` binary (jackin-the-architect/Dockerfile has no jackin install; hooks/preflight.sh none), so `jackin role validate` inside the task container is 'command not found'. (2) `docker exec` into container.txt hits the-architect, where `cargo` exists (M1-05b 'no cargo on PATH' fails) and `mise install`/`rustup run 1.97.1` pass vacuously; the crew throwaway container has no recorded name, no workspace, and no termrock checkout as workdir. (3) D-091 makes the launch a host part but says nothing about exec-ing into the launched instance. The M1-01 author cannot write a deterministic verify.sh; each of the three tasks exhausts in wave 3.
- Fix: EXECUTION §4 `container` row, add a 'Throwaway load (role smoke test, M1-05a..c, M1-06, M1-13, M3-02a)' paragraph: '`jackin workspace create probe-<id> --workdir <checkout> --mount <checkout>` (M1-05a: a termrock clone at ~/.jackin/managed/M1-05a/termrock; M1-05b: ~/.jackin/managed/M1-05b plus `--mount ~/.jackin/agent-browser-profile:/home/agent/.agent-browser-profile`; M1-05c: any checkout `:ro`), merge the lane template, `tmux new-session -d -s <id>-probe … jackin load donbeave/crew-<p> probe-<id> --agent <runtime>`, wait for the prompt, `docker ps --format {{.Names}} | grep -- -probe-<id slug>- > tasks/<id>/throwaway.txt`; every "inside:" check of the verify column is the host part `docker exec -u agent "$(cat tasks/<id>/throwaway.txt)" sh -c '<check>'`; end with `jackin eject "$(cat tasks/<id>/throwaway.txt)"`, `tmux kill-session -t <id>-probe`, `jackin workspace delete probe-<id>`.' ROADMAP M1-05a..c verify: prefix with 'host (D-061, D-091): `jackin role validate ~/.jackin/roles/donbeave/crew-<p>/default` after the throwaway load (the cache HEAD equals the pushed main commit); inside the throwaway container (tasks/<id>/throwaway.txt): …'. Container part of these tasks = only `git -C /workspace log -1` sign-off check and file presence in the role checkout.

## R3-72 [major] M1-04a `ci.yml` with `jackin-version` pinned can never pass: jackin has no versioned release, and `download-jackin-role.sh` expects `jackin-<ver>-<target>.tar.gz` on tag `<ver>`

- File: `ROADMAP.md`
- Where: M1-04a row (line 133): '`uses: jackin-project/jackin-role-action@<sha>` with `jackin-version` pinned'; concept/roles.md §7 step 6; M11-01a verify ('`gh run list … --workflow ci.yml … conclusion=="success"` after a re-run for all three')
- Risk: jackin-role-action/scripts/download-jackin-role.sh: any value other than `latest-build` goes to `download_from_release` → `https://github.com/jackin-project/jackin/releases/download/<tag>/jackin-<tag#v>-<target>.tar.gz`. `gh release list -R jackin-project/jackin` shows only `preview` (0.6.4-preview.1155) plus jackin-dev tags; preview assets are `jackin-<target>.tar.gz` (unversioned) — so `jackin-version: preview` or any semver 404s at the download step and every ci.yml run of the template and the three crew repos is red from M1-04a on. That red is documented as 'expected' only from M3-02a, and M11-01a's verify requires ci.yml `success` after a re-run → M11-01a exhausts; M11-02 (depends_on M11-01a) never runs; run ends BLOCKED in M11. `latest-build` (artifacts `preview-GitHub-jackin-<target>` from preview.yml on main) is the only resolvable value.
- Fix: M1-04a scope: replace 'with `jackin-version` pinned' by 'with `jackin-version: latest-build` (the only value `scripts/download-jackin-role.sh` can resolve: jackin publishes no versioned release and the `preview` assets are unversioned; pinning is a jackin-role-action gap noted in the task folder, never done here)'. concept/roles.md §7 step 6 same. M1-04a verify: add `grep -q "jackin-version: latest-build" .github/workflows/ci.yml`. M11-01a verify keep as is (now satisfiable because latest-build tracks main).

## R3-73 [major] review-crucible checkout in `/opt` is root-owned: `git -C /opt/review-crucible rev-parse HEAD` as `agent` fails with 'dubious ownership', M1-05c verify never passes

- File: `concept/roles.md`
- Where: §3.1 row 'Codex skills', reviewer column (`git init /opt/review-crucible && git -C /opt/review-crucible fetch --depth 1 … && checkout FETCH_HEAD`); ROADMAP M1-05c scope and verify (line 136)
- Risk: `/opt` is writable only as root, so the clone runs under `USER root` and the .git dir is uid 0. The container agent runs as `agent` (derived image `USER agent`), git ≥2.35.2 (trixie ships 2.47) refuses `rev-parse` in a repository owned by another uid unless `safe.directory` is set; neither the construct Dockerfile nor jackin's runtime-setup sets it (grep safe.directory: none). M1-05c's verify line `git -C /opt/review-crucible rev-parse HEAD` equals `REVIEW_CRUCIBLE_SHA` therefore fails on every attempt → exhausted in wave 3 → M1-05d blocked → run BLOCKED.
- Fix: In roles.md §3.1 and ROADMAP M1-05c replace the clone fragment with: `install -d -o agent -g agent /opt/review-crucible` under `USER root`, then under `USER agent`: `git init /opt/review-crucible && git -C /opt/review-crucible fetch --depth 1 https://github.com/tailrocks/review-crucible "$REVIEW_CRUCIBLE_SHA" && git -C /opt/review-crucible checkout --detach FETCH_HEAD && git -C /opt/review-crucible rev-parse HEAD > /opt/review-crucible/.jackin-pin` and `ln -s /opt/review-crucible/skills/review-crucible /home/agent/.agents/skills/review-crucible`. Verify: `[ "$(cat /opt/review-crucible/.jackin-pin)" = "$(cat .jackin/task/refs/review-crucible-sha.txt)" ]` and `test -f /home/agent/.agents/skills/review-crucible/SKILL.md` (path exists at that SHA: skills/review-crucible/SKILL.md).

## R3-74 [major] Builder toolchain source contradicts between roles.md and ROADMAP; the M1-05a verify ('mise install in a termrock checkout is a no-op', `rustup run 1.97.1`, `cargo public-api`) is only satisfiable by termrock's own mise.toml + rust-toolchain.toml

- File: `concept/roles.md`
- Where: §3.1 row 'mise', builder column ('pre-warm regenerated from `jackin@feat/managed-execution` `mise.toml` … plus termrock's delta … rust 1.97.1 beside 1.98.0 … cargo-public-api 0.52.0') vs ROADMAP M1-05a scope ('termrock's mise toolchain (D-048: not jackin's)')
- Risk: ROADMAP M1-05a says 'Per concept/roles.md §3' and then 'termrock's mise toolchain, not jackin's'; roles.md §3.1 says jackin's 33-tool list plus a delta. A Codex subagent following roles.md builds the jackin set (rust 1.98.0 default, mise config from jackin's mise.toml); `mise install` in a termrock checkout (mise.toml: rust via rust-toolchain.toml 1.97.1, cargo-nextest 0.9.140, cargo-semver-checks 0.48.0, wasm-pack 0.15.0, git-cliff 2.13.1, gitleaks 8.30.1, cargo-public-api only via `mise x cargo:cargo-public-api@0.52.0` in a task, not [tools]) is then not a no-op, and `cargo public-api --version` is absent unless installed separately. Each divergence costs an attempt; three attempts across L4→L5→L6 exhaust M1-05a.
- Fix: roles.md §3.1 'mise' builder cell: replace with 'pre-warm exactly termrock's `mise.toml` [tools] plus `rustup toolchain install 1.97.1` (its `rust-toolchain.toml`), `rustup component add rust-analyzer clippy rustfmt`, `cargo binstall cargo-public-api@0.52.0 lychee`; `MISE_TRUSTED_CONFIG_PATHS=/workspace:/tmp/jackin-mise`; jackin's own toolchain is served by the-architect (D-048)'. ROADMAP M1-05a verify: name the checkout: 'inside the throwaway container whose workdir is a termrock clone: `mise install` prints no install lines (`mise install 2>&1 | grep -cE "installing|downloading"` = 0), `cargo nextest --version`, `cargo public-api --version`, `rustup run 1.97.1 cargo --version` exit 0'.

## R3-75 [minor] DCO sign-off hook duplicates a jackin built-in and is shadowed by it: `jackin config git dco enable` installs a global `core.hooksPath` that overrides the template's `--system` hook

- File: `ROADMAP.md`
- Where: M1-04a scope ('`githooks/prepare-commit-msg` … plus `git config --system core.hooksPath /opt/jackin-role/githooks`'), M1-13 scope ('install the D-089 prepare-commit-msg sign-off hook in the image'), DECISIONS D-089 (4), goal/EXECUTION.md §4 DCO bullet
- Risk: jackin already implements DCO injection: `jackin config git dco enable` → container env `JACKIN_GIT_DCO=1` → runtime-setup symlinks the capsule as prepare-commit-msg under `/jackin/state/git-hooks` and runs `git config --global core.hooksPath /jackin/state/git-hooks` (jackin-capsule/src/runtime_setup.rs:205-230). Git's global config wins over `--system`, so the moment either `dco` or `coauthor_trailer` is enabled (both default false today) the role hook is silently dead, and the-architect commits in waves 2-3 (before M1-13 adds the hook there) carry no trailer unless the Codex agent types `-s`. A missing trailer on the-architect branch triggers the only sanctioned `--force-with-lease` path; on jackin/termrock PRs it blocks the `DCO` required check with no bypass.
- Fix: Use the involved project's feature (D-046): M1-02a scope adds 'host: `jackin config git dco enable`; verify `grep -q "dco = true" ~/.config/jackin/config.toml`'. M1-04a: drop `githooks/prepare-commit-msg` and the `core.hooksPath --system` fragment ('DCO trailers come from jackin's `JACKIN_GIT_DCO` hook, D-089 (4) amended'); M1-13: drop 'install the D-089 prepare-commit-msg sign-off hook in the image'. EXECUTION §4 DCO bullet: 'every container launched on this host has `JACKIN_GIT_DCO=1` (M1-02a); the pre-push trailer count stays as the gate'. D-089 (4): amend accordingly. Codex-exec interim path (no jackin) keeps `git commit -s` in TASK.md.

## R3-76 [minor] M1-05d verify says '`jackin config` shows `trusted = true`' but bare `jackin config` is a subcommand-only command; and the `$CODEX_HOME` hook contract needs a default

- File: `ROADMAP.md`
- Where: M1-05d verify (line 137): '`jackin config` shows `trusted = true` for the three selectors'; concept/roles.md §3.1 'Hooks' (source.sh writes `$CODEX_HOME/config.toml`, `$CODEX_HOME/agents/`)
- Risk: `jackin config` with no subcommand prints usage and exits non-zero (`Usage: jackin config [OPTIONS] <COMMAND>`), so a verify.sh line built on it fails or is ambiguous. Separately, the source hook is `source`d by entrypoint.sh before `exec` of the agent, on every pane start, and jackin's own entrypoint uses `${CODEX_HOME:-$HOME/.codex}` (line 94) because `CODEX_HOME` is not guaranteed in the hook's env; a hook doing `mkdir -p "$CODEX_HOME/agents"` with the variable unset writes `/agents` (permission denied) and, if the hook sets `-e`, kills the entrypoint of every reviewer/builder launch on Claude lanes.
- Fix: M1-05d verify: replace '`jackin config` shows `trusted = true`' with '`jackin config trust list` lists the three selectors and `grep -A1 "^\[roles.\"donbeave/crew-<p>\"\]" ~/.config/jackin/config.toml | grep -q "trusted = true"` for each'. roles.md §3.1 Hooks row (all three columns) and ROADMAP M1-05a/M1-05c: state 'source.sh uses `CODEX_HOME="${CODEX_HOME:-$HOME/.codex}"`, never `set -e`/`exit` (it is sourced, not executed), and is a no-op when `JACKIN_LANE_CODEX_MODEL` is unset'.


## Lens: role creation and builds (returned via direct message)

Findings (most severe first), lens: role creation and builds.\n\n1. **M1-05b ↔ M1-05d deadlock: operator `preflight.sh` refuses without the profile mount, which only M1-05d adds**\n   - Where: `ROADMAP.md` M1-05b scope/verify (line 135), `concept/roles.md` §3.1 \"Hooks\" row (operator column), `goal/EXECUTION.md` §3 M1 wave table (waves 3–4).\n   - Risk: M1-05b's verify demands a throwaway `jackin load donbeave/crew-operator --agent claude` that \"starts\". The operator `preflight.sh` is specified to refuse when the profile is unwritable or `state.json` fails to load. The mount `~/.jackin/agent-browser-profile → /home/agent/.agent-browser-profile` is added by M1-05d, which `depends_on` M1-05b. jackin's `entrypoint.sh` `run_hook` exits the container on non-zero preflight (`docker/runtime/entrypoint.sh:12-28,183-184`), so the throwaway load never reaches a prompt; M1-05b exhausts (3 attempts), blocks M1-05d/M1-06/M1-03…, run ends BLOCKED in wave 3.\n   - Fix: in `ROADMAP.md` M1-05b and `concept/roles.md` §3.1 operator \"Hooks\" row replace \"refuse … if still on login\" with: \"`preflight.sh` warns and continues when `/home/agent/.agent-browser-profile` is absent or `state.json` is missing (`echo '[crew-operator] no browser state; M1-06 verifies login' >&2; exit 0`); it refuses only when the profile exists and `SingletonLock` has a live PID. The logged-in check is M1-06's verify, not a launch gate.\" Additionally add to M1-05b verify: \"the throwaway load passes `--mount ~/.jackin/agent-browser-profile:/home/agent/.agent-browser-profile`\".\n\n2. **Role smoke checks of M1-05a..c run in the wrong container: `verify.sh container` is `docker exec`'d into `tasks/<id>/container.txt` (the-architect), not the throwaway crew container; and `jackin`/`jackin-role` does not exist inside the-architect**\n   - Where: `goal/EXECUTION.md` §4 `container` row (\"Container verify … `docker exec … $(cat tasks/<id>/container.txt)`\") and §5 step 5; `ROADMAP.md` M1-05a/M1-05b/M1-05c verify columns (\"`jackin role validate` passes; … inside: …no `op`/no `cargo` on PATH\").\n   - Risk: `container.txt` names the the-architect instance running the Codex lane. `jackin role validate` fails there (`jackin-the-architect/Dockerfile` installs no jackin binary; the M1-02 checkout is not mounted). \"no `cargo` on PATH\" (M1-05b) fails inside the-architect; \"`mise install` no-op in a termrock checkout\" needs a termrock mount nobody declares. Deterministic 3× fail → exhaustion → BLOCKED before any role exists.\n   - Fix: in `goal/EXECUTION.md` §4 add a \"role smoke\" sub-rule: \"For M1-05a..c, M1-06, M1-13 the `verify.sh` host part (D-091) launches the throwaway under `tmux new-session -d -s <id>-smoke … jackin load <crew-role> probe-<id> --agent claude` (workspace `probe-<id>` with `--mount ~/Projects/tailrocks/termrock` for the builder and the profile mount for the operator), records its name in `tasks/<id>/smoke-container.txt`, runs the in-image checks via `docker exec -u agent \"$(cat tasks/<id>/smoke-container.txt)\" sh -c '…'`, ejects it by name\". In `ROADMAP.md` M1-05a..c verify, move `jackin role validate` to the host part: \"host (D-061): `jackin role validate ~/.jackin/roles/donbeave/crew-<p>/default` after `jackin load … --rebuild` refreshed the cache\".\n\n3. **PREFLIGHT §1 standing check contains an unresolvable placeholder `<TAILROCKS_SKILLS_SHA>`; it fails at every session start and becomes a \"human\" defect**\n   - Where: `goal/PREFLIGHT.md` lines 142-145 (\"`gh api repos/tailrocks/tailrocks-skills/commits/<TAILROCKS_SKILLS_SHA>` … with the SHAs of `concept/roles.md` §3.1\"); `concept/roles.md` §3.1 \"Codex skills\" row says the SHA \"is captured by M1-05a and recorded in its folder\" — no SHA exists in §3.1.\n   - Risk: EXECUTION §1 step 4 re-runs §1 checks at every start; a check \"that needs the human is a preflight defect (§6)\" → BLOCKED at wave 0 of the first run and every resume.\n   - Fix: replace the item with: \"`gh api repos/tailrocks/review-crucible/commits/5936f0e069946db0ee4408e72122b134800336e4` returns 200; `gh api repos/tailrocks/tailrocks-skills/commits/$(cat tasks/M1-05a/tailrocks-skills-sha.txt 2>/dev/null || git ls-remote https://github.com/tailrocks/tailrocks-skills HEAD | cut -f1)` returns 200\". (Verified today: review-crucible SHA exists and `skills/review-crucible/SKILL.md` is present; tailrocks-skills `origin/main` = `becfafbe…`, newest tag `v0.25.0`.)\n\n4. **Template `ci.yml` \"`jackin-version` pinned\" cannot resolve: the action's release download path expects `jackin-<ver>-<target>.tar.gz` under a versioned tag, and jackin has no versioned release**\n   - Where: `ROADMAP.md` M1-04a scope (\"`uses: jackin-project/jackin-role-action@<sha>` with `jackin-version` pinned\"), `concept/roles.md` §7 item 6, `ROADMAP.md` M11-01a verify (`ci.yml` conclusion `success` for all three).\n   - Risk: `jackin-role-action/scripts/download-jackin-role.sh:26-40` builds `releases/download/<tag>/jackin-${tag#v}-${target}.tar.gz`; the only jackin release is `preview` with assets `jackin-<target>.tar.gz` (no version segment) — every pinned value 404s. Role CI is red from M1-04a; M11-01a's verify (green `ci.yml` after re-run) can never pass → M11-01a exhausts → M11-02/M11-03/M12 never run.\n   - Fix: M1-04a scope: \"`jackin-version: latest-build` (the only value the action can resolve; it downloads the `preview-GitHub-jackin-<target>` artifact of jackin `main`); never a pinned tag\". Same wording in `concept/roles.md` §7 item 6.\n\n5. **D-089 DCO hook via `git config --system core.hooksPath` is shadowed by jackin's own `--global core.hooksPath`; jackin already ships DCO injection**\n   - Where: `ROADMAP.md` M1-04a scope (`githooks/prepare-commit-msg` + `git config --system core.hooksPath /opt/jackin-role/githooks`), `DECISIONS.md` D-089 (4), `goal/EXECUTION.md` §4 DCO rule, M1-13 \"install the D-089 hook in the image\".\n   - Risk: `jackin-capsule runtime-setup` writes `git config --global core.hooksPath /jackin/state/git-hooks` whenever `JACKIN_GIT_DCO=1` or `JACKIN_GIT_COAUTHOR_TRAILER=1` (`crates/jackin-capsule/src/runtime_setup.rs:205-232`); global beats system, so the role hook is dead code whenever either trailer is on, and Codex commits without `-s` land unsigned → `DCO` required check blocks the-architect merges (M1-13, M3-02).\n   - Fix: replace the template hook with the built-in: in `goal/PREFLIGHT.md` §1 add \"`jackin config git dco enable`; proof: `grep -q 'dco = true' ~/.config/jackin/config.toml`\"; in M1-04a drop `githooks/` and the `core.hooksPath` fragment; in D-089 (4) and M1-13 replace \"install the D-089 hook in the image\" with \"DCO trailer injection is jackin's `config git dco` (host), verified in `tasks/M1-05d/`\". Keep `git commit -s` in every `TASK.md`.\n\n6. **M1-05c verify `git -C /opt/review-crucible rev-parse HEAD` fails with \"dubious ownership\" when the clone is done as root**\n   - Where: `ROADMAP.md` M1-05c scope (`git init /opt/review-crucible && … fetch … checkout FETCH_HEAD`) and verify; `concept/roles.md` §3.1 \"Codex skills\" reviewer cell.\n   - Risk: `/opt` is root-owned in the construct; the fetch runs in a `USER root` stage; at run time `agent` runs `git -C /opt/review-crucible rev-parse HEAD` → `fatal: detected dubious ownership` (neither construct nor runtime-setup sets `safe.directory`). Verify fails 3× → M1-05c exhausted.\n   - Fix: in M1-05c scope append \"`chown -R agent:agent /opt/review-crucible` after the checkout (or run the clone as `USER agent` into `/home/agent/.local/share/review-crucible`)\"; and make the verify `git -C /opt/review-crucible -c safe.directory=/opt/review-crucible rev-parse HEAD`.\n\n7. **Builder toolchain source contradicts between documents (jackin's `mise.toml` vs termrock's)** — minor\n   - Where: `concept/roles.md` §3.1 \"mise\" row (\"regenerated from `jackin@feat/managed-execution` `mise.toml` … plus termrock's delta\") vs `ROADMAP.md` M1-05a (\"termrock's mise toolchain (D-048: not jackin's)\"); M1-05a verify \"`mise install` in a termrock checkout is a no-op; `rustup run 1.97.1`\".\n   - Risk: ROADMAP says \"Per concept/roles.md §3\" so the Codex author may bake jackin's toolchain; termrock's `mise.toml` (verified: nextest 0.9.140, cargo-semver-checks 0.48.0, wasm-pack 0.15.0, git-cliff 2.13.1, gitleaks 8.30.1, `rust-toolchain.toml` 1.97.1, `cargo-public-api@0.52.0` via `mise x`) is the set the verify actually needs; a no-op `mise install` is false with extra/different tools.\n   - Fix: rewrite the roles.md \"mise\" builder cell to \"pre-warm exactly `termrock/mise.toml` + `rust-toolchain.toml` (1.97.1) + `cargo-public-api 0.52.0`; nothing from jackin's `mise.toml` (D-048)\".\n\n8. **M1-05d verify \"`jackin config` shows `trusted = true`\" — `jackin config` has no bare output** — minor\n   - Where: `ROADMAP.md` M1-05d verify; `concept/roles.md` §7 item 7.\n   - Risk: `jackin config` without a subcommand prints usage/nothing; the M1-01 author writes a check that cannot pass.\n   - Fix: \"`jackin config trust list` lists the three selectors and `grep -A2 '^\\[roles.\"donbeave/crew-<p>\"\\]' ~/.config/jackin/config.toml | grep -q 'trusted = true'` for each\".\n\nVerified-OK (no finding): construct `0.36-trixie` digest `41815a35…` matches; op 2.39.0 zip SHA256s match (arm64 `829baeff…`, amd64 `6fba7f37…`); Debian trixie arm64 has `chromium` 151; agent-browser 0.35.1 ships `bin/agent-browser-linux-arm64`, `skill-data/`, honours `AGENT_BROWSER_EXECUTABLE_PATH`, auto-adds `--no-sandbox` in Docker; marketplace names/plugins resolve for `tailrocks-skills`, `caveman`, `jackin-marketplace`, official; `--rebuild` sets role-repo refresh TTL to 0 (fetch+ff from default branch); `standard` profile is open network (operator needs no allowlist grant); `[docker] dind = \"none\"` / workspace `[docker.grants] dind = \"privileged\"` are valid; `OpRef.path` mandatory as documented; Codex 0.150.1 reads `~/.agents/skills`."}],"stop_reason":null,"stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":187162,"cache_read_input_tokens":0,"cache_creation":{"ephemeral_5m_input_tokens":187162,"ephemeral_1h_input_tokens":0},"output_tokens":4,"service_tier":"standard","inference_geo":"not_available"},"diagnostics":{"cache_miss_reason":{"type":"system_changed","cache_missed_input_tokens":152579}}},"requestId":"req_011CeTUcAQAj19ov1cGTo1XS","attributionAgent":"general-purpose","type":"assistant","uuid":"a587c283-c040-49e8-b21c-80ae131cebfc","timestamp":"2026-08-27T15:19:20.896Z","effort":"medium","userType":"external","entrypoint":"cli","cwd":"/Users/donbeave/Projects/tailrocks/ecosystem","sessionId":"d0351e91-722b-444b-b4a2-1ffb839138d5","version":"2.1.247","gitBranch":"HEAD"}
