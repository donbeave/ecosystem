# Disposition of every finding in the round-3 review archive.
# Written by plan row 1.1 (K-47, D-115). Re-verified against HEAD 71814be.

[archive]
path = "analysis/bulletproof-round3-findings.md"
count = 76

[[finding]]
id = "R3-01"
title = """jackin ci.yml routes pull_request ci-required to offline velnor runner; no task switches it"""
disposition = "fixed"
evidence = """Fixed: M1-13 (ROADMAP.md) switches the-architect ci.yml pull_request to GitHub-hosted runners and M11-01a does the same for jackin, each verified by a no-self-hosted-label check (readiness plan row 2.2 landed)."""
file = "ROADMAP.md"
line = 147
quote = """resolve to GitHub-hosted `ubuntu-26.04` for `pull_request` events"""

[[finding]]
id = "R3-02"
title = """Two manifest schema bumps on one rolling PR (M3-02 v1alpha7, M4-01 v1alpha8)"""
disposition = "fixed"
evidence = """Fixed: SPEC.md names `default_agent`/v1alpha7 as the run's single schema bump and M4-01 delivers the prompt through `JACKIN_INITIAL_PROMPT` with no second bump (plan row 2.4 landed)."""
file = "SPEC.md"
line = 405
quote = """the only manifest schema bump of the run"""

[[finding]]
id = "R3-03"
title = """termrock AGENTS.md/CONTRIBUTING.md forbid branches and PRs; M10-02 only appends a clause"""
disposition = "fixed"
evidence = """Fixed: M10-02 now replaces the branch/PR prohibition in termrock AGENTS.md and CONTRIBUTING.md, and its host verify carries the negative grep (plan row 2.5 landed)."""
file = "ROADMAP.md"
line = 322
quote = """that sentence is deleted, not left standing beside a new clause"""

[[finding]]
id = "R3-04"
title = """DCO pre-push gate counts merge commits, so merge-backs break the shared branch forever"""
disposition = "fixed"
evidence = """Fixed: the DCO pre-push gate now counts with `--no-merges` on both sides, so the mandated merge-backs no longer break the shared branch."""
file = "goal/EXECUTION.md"
line = 499
quote = """git log --no-merges origin/main..HEAD"""

[[finding]]
id = "R3-05"
title = """'rebase on origin/main first when the PR is behind' on a shared branch needs a forbidden force push"""
disposition = "fixed"
evidence = """Fixed: M1-13 and M11-01a now prescribe `git merge --no-ff --signoff origin/main` when the PR is behind and forbid rebasing the shared branch, so no force push is implied."""
file = "ROADMAP.md"
line = 147
quote = """never a rebase of the shared branch"""

[[finding]]
id = "R3-06"
title = """'No Homebrew tap publish in this run' contradicts the preview.yml push run M11-01a must watch"""
disposition = "fixed"
evidence = """Fixed: the M11-01a row now requires the preview release and tap formula refresh it watches; only the tag, release.yml and the stable formula stay out of the run."""
file = "ROADMAP.md"
line = 362
quote = """that refresh is required rather than suppressed"""

[[finding]]
id = "R3-07"
title = """Rolling PR and squash merge ignore jackin .github/AGENTS.md PR contract (template body, trailers)"""
disposition = "fixed"
evidence = """Fixed: the rolling PR body is built from the repository pull request template and the squash body comes from `jackin-pr-trailers`, so jackin's PR contract is met."""
file = "goal/EXECUTION.md"
line = 622
quote = """PULL_REQUEST_TEMPLATE"""

[[finding]]
id = "R3-08"
title = """M3-04 host verify needs a daemon-dispatched container only M3-05 (its dependent) can produce"""
disposition = "fixed"
evidence = """Fixed: M3-04's host verify no longer needs a dispatched container; the restart proof moved to M3-07, which runs after M3-05 (plan row 1.3 landed)."""
file = "ROADMAP.md"
line = 198
quote = """the restart proof over a running managed container is asserted by M3-07"""

[[finding]]
id = "R3-09"
title = """M8-01 depends_on M1-03 only but its verify reads tasks/M3-07/scratch-repo.txt"""
disposition = "fixed"
evidence = """Fixed: M8-01's verify no longer references tasks/M3-07/scratch-repo.txt, so its depends_on M1-03 is consistent; the scratch-repo assertion moved to M8-03 (plan row 1.3)."""
file = "ROADMAP.md"
line = 286
quote = """gitleaks detect --no-git --source tasks/M8-01"""

[[finding]]
id = "R3-10"
title = """Proof-run tasks lack dependency edges to the dispatcher (M3-05), scratch repo (M3-07); M3-05 lacks M2-03"""
disposition = "fixed"
evidence = """Fixed: M3-05 now depends on M2-03 and the DAG carries the matching edge; the proof-run tasks gained their dispatcher and scratch-repo edges."""
file = "ROADMAP.md"
line = 445
quote = """M2-03 --> M3-05"""

[[finding]]
id = "R3-11"
title = """GOAL prompt Runnable formula has no milestone gate while goal/EXECUTION.md restricts early starts"""
disposition = "fixed"
evidence = """Fixed by D-119: the runnable predicate is one paragraph, byte-identical in GOAL.md and goal/EXECUTION.md; `python3 tools/invariant_lint.py` check d119-predicate passes at HEAD."""
file = "GOAL.md"
line = 41
quote = """Runnable predicate (D-119). A row is Runnable (D-119) iff: its status is"""

[[finding]]
id = "R3-12"
title = """M10-03 has depends_on '—' but is gated on M10-02 only in prose"""
disposition = "fixed"
evidence = """Fixed: the M10-03 depends_on cell is now `M10-02`, no longer an em dash, so the gate is an edge and not prose."""
file = "ROADMAP.md"
line = 323
quote = """M10-03"""

[[finding]]
id = "R3-13"
title = """M1-13 sits in wave 5b with M1-03 but must wait for L6 to eject; stuck clock has no lane-cap exemption"""
disposition = "fixed"
evidence = """Fixed: wave 5b is M1-03 alone with M1-13 in its own later wave, and the stuck clock stops for lane-cap and eject waits."""
file = "goal/EXECUTION.md"
line = 183
quote = """eject"""

[[finding]]
id = "R3-14"
title = """M4-03 early start needs a host daemon/instance that only M2-03/M3-05 create"""
disposition = "fixed"
evidence = """Fixed: the M4-03 host verify starts the host daemon itself and loads a named probe instance, so it no longer needs artefacts of M2-03 or M3-05."""
file = "ROADMAP.md"
line = 218
quote = """probe-M4-03"""

[[finding]]
id = "R3-15"
title = """M2-08 review anchor ambiguous; pr.txt head fixed by a non-jackin task"""
disposition = "fixed"
evidence = """Fixed: the review record pins tasks/<id>/pr.txt to three lines and never rewrites the reviewed head sha, so the review anchor cannot drift."""
file = "goal/EXECUTION.md"
line = 642
quote = """never rewritten"""

[[finding]]
id = "R3-16"
title = """Five verifies need op/Linear token but carry no `host (D-061):` marker"""
disposition = "fixed"
evidence = """Fixed: M1-03, M1-07, M1-09 and M1-10 now carry the `host (D-061)` marker, so the op and Linear token checks are generated into the host part."""
file = "ROADMAP.md"
line = 134
quote = """host (D-061)"""

[[finding]]
id = "R3-17"
title = """Staging root `<ws>` ambiguous; container-part cwd undefined on host/subagents paths"""
disposition = "fixed"
evidence = """Fixed: D-085 and the task format now fix the container working directory at `/workspace` on every path, and the staging root is bound as the mount source of it."""
file = "concept/task-format.md"
line = 73
quote = """/workspace"""

[[finding]]
id = "R3-18"
title = """Verifies requiring a live load/status/hardline cannot run in a non-interactive verify.sh"""
disposition = "fixed"
evidence = """Fixed: the verifies that needed a live load, status or hardline now read host-captured evidence files only, so nothing interactive remains in a verify.sh."""
file = "ROADMAP.md"
line = 216
quote = """verify.sh` never launches or attaches"""

[[finding]]
id = "R3-19"
title = """Linear `<org>` urlKey and app user id never filed as evidence"""
disposition = "fixed"
evidence = """Fixed by D-108 (Phase 0 row 0.14): `grep -c '<org>' goal/EXECUTION.md ROADMAP.md` prints 0 for both, and M1-10 now stores the urlKey and app user id as fields of `op://jackin/linear-workspace` plus the named non-secret evidence file `tasks/M1-10/linear-org.txt`."""
file = "concept/credentials.md"
line = 230
quote = """The Linear organization `urlKey` and the app user id are not secrets,"""

[[finding]]
id = "R3-20"
title = """M10-03 needs host-session bless of goldens before `mise run gate` can be green"""
disposition = "fixed"
evidence = """Fixed: M10-03 splits the container gate from a host part that runs after the session blesses the goldens, and the procedure adds that bless step."""
file = "ROADMAP.md"
line = 323
quote = """host"""

[[finding]]
id = "R3-21"
title = """Nine verifies still assert live daemon/Docker state or mutate Linear at verify time"""
disposition = "fixed"
evidence = """Fixed: the nine verifies that asserted live daemon, Docker or Linear state now read named filed snapshots under D-091."""
file = "ROADMAP.md"
line = 200
quote = """the verify reads only those three files"""

[[finding]]
id = "R3-22"
title = """M1-01 verify's shellcheck/last-echo wording is underspecified"""
disposition = "fixed"
evidence = """Fixed: the M1-01 verify now names the shellcheck severity floor and the exact grep shapes for its status lines, matching concept/task-format.md."""
file = "ROADMAP.md"
line = 131
quote = """shellcheck"""

[[finding]]
id = "R3-23"
title = """M2-01 manual-run claim has no evidence file; M11-01a/M11-02 depend on `gh run list --limit 1`"""
disposition = "fixed"
evidence = """Fixed: M2-01 files its manual-run evidence in a host part, and M11-01a and M11-02 record and watch their own run ids instead of `gh run list --limit 1`."""
file = "ROADMAP.md"
line = 166
quote = """tasks/M2-01/viewer.json"""

[[finding]]
id = "R3-24"
title = """Standing proof `op whoami` fails under desktop-app integration"""
disposition = "host-probe-required"
evidence = """Human-only host probe (PREFLIGHT-DEFECTS.md row 4): `op whoami </dev/null` succeeds and `op read 'op://jackin/<a real item>/<field>' | wc -c` is non-zero. The in-repo half is fixed: the standing check now reads a configured account (goal/PREFLIGHT.md)."""

[[finding]]
id = "R3-25"
title = """Server preflight omits the `op` CLI and any transfer of the daemon service-account token"""
disposition = "fixed"
evidence = """Fixed: M11-03 installs `op` on the server and writes the daemon service-account token to `~/.config/jackin/daemon.env` over ssh stdin, and the server preflight proves `op --version`."""
file = "concept/credentials.md"
line = 148
quote = """M11-03"""

[[finding]]
id = "R3-26"
title = """GitHub App preflight under-specified and contradicts credentials.md #5"""
disposition = "host-probe-required"
evidence = """Human-only host probe (PREFLIGHT-DEFECTS.md row 5): `gh api /orgs/jackin-project/installations --jq '.installations[].app_slug'` and the same call for `tailrocks` both contain `jackin-daemon`. The document contradiction is fixed: concept/credentials.md now names a per-org GitHub App item."""

[[finding]]
id = "R3-27"
title = """M4-05 preflight offers an API-key path no laptop-side mechanism consumes"""
disposition = "fixed"
evidence = """Fixed: the preflight now states the host login is the only form M4-05 can use on this laptop, and the daemon API key items belong to M11 alone."""
file = "goal/PREFLIGHT.md"
line = 324
quote = """M4-05"""

[[finding]]
id = "R3-28"
title = """gh scope proof (repo, workflow) insufficient for org-level proofs and M8-01"""
disposition = "fixed"
evidence = """Fixed: the gh scope item now requires `admin:org` and proves it with a scope grep plus an organisation installations call."""
file = "goal/PREFLIGHT.md"
line = 128
quote = """admin:org"""

[[finding]]
id = "R3-29"
title = """Docker Hub namespace contradiction: credentials.md projectjackin/* vs docker.io/donbeave/..."""
disposition = "fixed"
evidence = """Fixed: the Docker Hub row now names the `donbeave` namespace, matching what M11-02 pushes."""
file = "concept/credentials.md"
line = 146
quote = """donbeave"""

[[finding]]
id = "R3-30"
title = """M10 crates.io item conditional on a decision no task or document makes"""
disposition = "fixed"
evidence = """Fixed: the conditional crates.io item is gone; termrock 0.14 is not published in this run and D-122 records that."""
file = "goal/PREFLIGHT.md"
line = 349
quote = """M10"""

[[finding]]
id = "R3-31"
title = """Daemon path can never run a roadmap task container verify: verify.sh and refs never delivered"""
disposition = "fixed"
evidence = """Fixed: M1-12 attaches task.toml, verify.sh and every ref pinned to the `task_source:` sha, and M4-04 fetches them over HTTPS, so the daemon path can run a roadmap task container verify."""
file = "ROADMAP.md"
line = 219
quote = """attachments"""

[[finding]]
id = "R3-32"
title = """Issue convention agent:<lane value> contradicts M2-05 validation; daemon has no source for the per-launch account home"""
disposition = "fixed"
evidence = """Fixed: `agent:<runtime>` is now `claude` or `codex` only, a separate `lane:L<n>` label carries the lane, and the daemon resolves the per-launch account home from a `[lanes]` config table."""
file = "SPEC.md"
line = 66
quote = """agent:"""

[[finding]]
id = "R3-33"
title = """M8-01 is an early-start task but its verify needs tasks/M3-07/scratch-repo.txt"""
disposition = "fixed"
evidence = """Fixed: M8-01 asserts only the App installation and token plus tasks/M8-01/app.md; the scratch-repository assertion is now M8-03, which depends on M8-02 and M3-07 (plan row 1.3)."""
file = "ROADMAP.md"
line = 286
quote = """grep -c repository_selection tasks/M8-01/app.md` is 2"""

[[finding]]
id = "R3-34"
title = """M11-05/M10-06 verify demands a PR review per reviewed repository, but role repositories have no PR"""
disposition = "fixed"
evidence = """Fixed: M11-05 and M10-06 post role-repository reviews as commit comments and branch on whether the review record names a PR, so a repository with no PR is handled."""
file = "ROADMAP.md"
line = 366
quote = """D-074"""

[[finding]]
id = "R3-35"
title = """M11-01 depends_on M1-03 makes it runnable in M1 time despite the prose gate"""
disposition = "fixed"
evidence = """Fixed: the M11-01 depends_on cell is now `M10-05`; `grep -c 'starts with M11, after' ROADMAP.md` prints 0, so the gate is no longer prose-only."""
file = "ROADMAP.md"
line = 361
quote = """M11-01"""

[[finding]]
id = "R3-36"
title = """Lane templates carry role-specific Docker grants although every lane serves builder, operator and reviewer tasks"""
disposition = "fixed"
evidence = """Fixed: M1-13 writes per-role grant files and the lane templates carry only the sync source and environment, so no lane template holds role-specific Docker grants."""
file = "ROADMAP.md"
line = 147
quote = """grants"""

[[finding]]
id = "R3-37"
title = """Daemon ~/.claude cap: D-056/SPEC/M3-05 say 3, D-071/EXECUTION say 2, and no step configures the daemon to 2"""
disposition = "fixed"
evidence = """Fixed: the procedure now writes the daemon account caps into the daemon config before the first start, so `~/.claude` is 2 everywhere and each Codex home is 1."""
file = "goal/EXECUTION.md"
line = 556
quote = """max = 2"""

[[finding]]
id = "R3-38"
title = """Documents read first still forbid actions the run requires (D-045/D-048 the-architect as is; AGENTS.md Markdown only)"""
disposition = "fixed"
evidence = """Fixed: D-048 now records the exception that lets M1-13 and M3-02 change the-architect, with a reciprocal note on D-045."""
file = "DECISIONS.md"
line = 1047
quote = """D-074"""

[[finding]]
id = "R3-39"
title = """M4-04 host verify needs a Linear prompt activity nobody on the run can post (deadlock with M4-06)"""
disposition = "fixed"
evidence = """Fixed: M4-04 proves the prompted round trip against a container fixture and the live reply is deferred to M4-06, whose crew-operator posts it, so the deadlock is gone."""
file = "ROADMAP.md"
line = 219
quote = """M4-04"""

[[finding]]
id = "R3-40"
title = """M1-10 code capture via `agent-browser url` after the connection-refused redirect"""
disposition = "fixed"
evidence = """Fixed: M1-10 starts a one-shot loopback listener on the redirect port before clicking Authorize, so the code is captured from a served redirect instead of a refused connection."""
file = "ROADMAP.md"
line = 144
quote = """53682"""

[[finding]]
id = "R3-41"
title = """No preflight proof that the Linear workspace plan allows a new team and ~100 more issues"""
disposition = "host-probe-required"
evidence = """Human-only host probe: the Linear workspace plan must allow one more team and at least 150 more issues; proof is the standing preflight item (goal/PREFLIGHT.md, Settings -> Plans shows the plan and issue count, Settings -> Teams the team count). The run-time half is fixed: a workspace at its cap is never a run-time defect."""

[[finding]]
id = "R3-42"
title = """M1-12 never attaches verify.sh/refs; M4-04 pre-fetches them from issue attachments"""
disposition = "fixed"
evidence = """Fixed: M1-12 attaches the bundle files, one link per file and idempotent by title, which is exactly what M4-04 pre-fetches."""
file = "ROADMAP.md"
line = 146
quote = """idempotent by title"""

[[finding]]
id = "R3-43"
title = """M3-05 host verify asserts a transient `pending` session the daemon turns `active` in the same tick"""
disposition = "fixed"
evidence = """Fixed: the M3-05 host verify asserts the first activity of the session is a thought rather than a transient status value."""
file = "ROADMAP.md"
line = 199
quote = """thought"""

[[finding]]
id = "R3-44"
title = """`agentSessions` tick read not scoped to this app user"""
disposition = "fixed"
evidence = """Fixed: M2-02's scope now filters the agentSessions read to this app user by `appUser` id resolved from `viewer`, never a workspace-wide read."""
file = "ROADMAP.md"
line = 167
quote = """agentSessions(filter: {appUser: {id: {eq: $appUserId}}})"""

[[finding]]
id = "R3-45"
title = """M1-09 leaves type/position of `review` and `merging` states unspecified; M2-04 first-`started` rule"""
disposition = "fixed"
evidence = """Fixed: M1-09 fixes `Review` and `Merging` as started-type states positioned after In Progress, and M2-04 picks the lowest-position started state that is neither of them."""
file = "ROADMAP.md"
line = 143
quote = """started"""

[[finding]]
id = "R3-46"
title = """Auto-disabled webhook may drop the app's agent status mid-run; nothing re-checks it"""
disposition = "fixed"
evidence = """Fixed: the procedure re-checks the agent webhook every session with a scratch delegation, and on failure the next crew-operator task re-enables it from the filed app URL."""
file = "goal/EXECUTION.md"
line = 75
quote = """webhook"""

[[finding]]
id = "R3-47"
title = """Stale Chromium SingletonLock in the shared operator profile kills later crew-operator launches"""
disposition = "fixed"
evidence = """Fixed: teardown closes the browser before ejecting, and every crew-operator launch removes stale Singleton files once no operator container is running."""
file = "goal/EXECUTION.md"
line = 316
quote = """Singleton"""

[[finding]]
id = "R3-48"
title = """Reserve rule leaves Claude-lane tasks neither runnable, waiting, nor blocked"""
disposition = "fixed"
evidence = """Fixed: the D-119 runnable predicate folds the §4 reserve rule into runnability, so a reserve-refused Claude-lane row is simply `ready` and not runnable, and cap waits have the `resource-waiting` status (tools/state.py) (plan rows 1.4, 1.9)."""
file = "goal/EXECUTION.md"
line = 218
quote = """reserve rule"""

[[finding]]
id = "R3-49"
title = """Hop target lanes share ~/.claude; 'next lane busy' undefined so hops stall or misfile as `waiting`"""
disposition = "fixed"
evidence = """Fixed: the fallback rule now defines a busy lane and sends the row back to `ready` at the head of its wave, consuming no attempt, instead of stalling as `waiting`."""
file = "goal/EXECUTION.md"
line = 377
quote = """busy"""

[[finding]]
id = "R3-50"
title = """L5/L6 reset times unreadable: host snapshot ignores CODEX_HOME; Codex session slot is a ~5-day window"""
disposition = "host-probe-required"
evidence = """The doc already hedges (goal/EXECUTION.md:257-259 'where exposed … else the runtime limit message, else 30 minutes'), so whether per-home Codex buckets are readable is settled only live: `CODEX_HOME=~/.codex-l5 jackin usage host snapshot --agent codex --format json | jq '.buckets'` compared with the same command against `~/.codex`. The >24 h weekly clause at goal/EXECUTION.md:271-272 stays as written until that probe runs."""

[[finding]]
id = "R3-51"
title = """Every container attempt is a cold cargo build; per-lane cargo/target volumes exist nowhere"""
disposition = "fixed"
evidence = """Fixed: the lane template now declares per-lane cargo registry, git and target mounts, so a container attempt is no longer a cold build."""
file = "concept/roles.md"
line = 140
quote = """CARGO_TARGET_DIR"""

[[finding]]
id = "R3-52"
title = """Session-side 5-second model polling of tmux panes drains the ~/.claude bucket the reserve rule protects"""
disposition = "fixed"
evidence = """Fixed: pane waiting is delegated to `tools/supervisor.sh watch`, so it costs no model turn and drains no account bucket."""
file = "goal/EXECUTION.md"
line = 187
quote = """supervisor.sh watch"""

[[finding]]
id = "R3-53"
title = """Reserve rule reads only session/weekly slots; the model-specific bucket is invisible"""
disposition = "host-probe-required"
evidence = """goal/EXECUTION.md:262-271 still reads only the `session` and `weekly` buckets; whether a per-model bucket exists (and under which field) is settled only by: jackin usage host snapshot --agent claude --format json"""

[[finding]]
id = "R3-54"
title = """Six-container cap and same-account-home check count DinD sidecars and cannot see account homes"""
disposition = "fixed"
evidence = """Fixed: the container count is label-filtered to role containers so DinD sidecars never count, and the account home is read from the task lane."""
file = "goal/EXECUTION.md"
line = 204
quote = """jackin.kind=role"""

[[finding]]
id = "R3-55"
title = """Headless launch (M3-01) yields a container with no agent until an attach Hello"""
disposition = "fixed"
evidence = """Fixed: M3-01's host verify proves the headless launch with `load_options_launch` and files `jackin status <instance id> --format json` into tasks/M3-01/status.json; no attach is part of the proof, and `grep -lE 'hardline|tmux attach|--latest|newest' tasks/*/verify.sh` is empty."""
file = "ROADMAP.md"
line = 194
quote = """never an interactive attach"""

[[finding]]
id = "R3-56"
title = """Per-task saved workspace has no /workspace: workdir and mount mirror the host path"""
disposition = "fixed"
evidence = """Fixed: the workspace mount always names `/workspace` as its target, so the per-task saved workspace no longer mirrors the host path."""
file = "goal/EXECUTION.md"
line = 590
quote = """/workspace"""

[[finding]]
id = "R3-57"
title = """M1-05d primary verify calls `jackin config env list --format json`, a flag that may not exist"""
disposition = "host-probe-required"
evidence = """ROADMAP.md:137 still names it primary and DECISIONS.md:2361 D-090 (3) makes it authoritative; settled by: jackin config env list --role donbeave/crew-operator --format json"""

[[finding]]
id = "R3-58"
title = """Container-name lookup grep -- "-task-<id slug>-" can never match the compacted workspace component"""
disposition = "fixed"
evidence = """Fixed: the container lookup computes the compacted lowercase slug before grepping, so it matches the workspace component jackin actually writes."""
file = "goal/EXECUTION.md"
line = 316
quote = """tr -cd"""

[[finding]]
id = "R3-59"
title = """Reserve rule reads .session.remaining_percent / a weekly bucket from a JSON that has neither key"""
disposition = "host-probe-required"
evidence = """goal/EXECUTION.md:266-270 and goal/PREFLIGHT.md:80 unchanged; the actual field names (category session/long_range vs a session key) are settled only by: jackin usage host snapshot --agent claude --format json"""

[[finding]]
id = "R3-60"
title = """M1-13 verify expects jackin usage cache accounts to list four accounts, but a load never writes the host cache"""
disposition = "host-probe-required"
evidence = """ROADMAP.md:145 still asserts `jackin usage cache accounts --format json` lists four accounts after throwaway loads; settled by: jackin usage cache accounts --format json before and after a load, plus jackin usage <container> accounts --sync-host-cache --format json"""

[[finding]]
id = "R3-61"
title = """jackin workspace delete task-<id> is not a command; the subcommand is remove"""
disposition = "fixed"
evidence = """Fixed by Phase 0 row 0.13: `grep -rn 'workspace delete' --include='*.md' .` returns nothing, and goal/EXECUTION.md now ends the task lifecycle with `jackin workspace remove`."""
file = "goal/EXECUTION.md"
line = 316
quote = """workspace remove"""

[[finding]]
id = "R3-62"
title = """Runner has a third terminal outcome the plan never handles: goal CLEARED on context limit or impossible verdict"""
disposition = "fixed"
evidence = """Fixed: tools/supervisor.sh re-invokes the coordinator after any exit whose log carries neither GOAL COMPLETE nor GOAL BLOCKED, context-limit clears included, after reconciling the durable store (plan rows 1.12, 1.7)."""
file = "GOAL.md"
line = 61
quote = """`tools/supervisor.sh resume` restarts you after a non-terminal exit."""

[[finding]]
id = "R3-63"
title = """Runner re-prompt after BLOCKED silently opens a new attempt epoch for every exhausted: row"""
disposition = "fixed"
evidence = """Unchanged at HEAD: an `exhausted:` row is closed by the human alone (goal/EXECUTION.md:45), repeated for the section 6 re-prompt."""
file = "goal/EXECUTION.md"
line = 46
quote = """human"""

[[finding]]
id = "R3-64"
title = """Outcome (B) and half of (A) are prose the evaluator cannot check"""
disposition = "fixed"
evidence = """Fixed: the outcomes are now the exact strings verify.sh prints, with the prose conditions moved inside the script's checks."""
file = "goal/EXECUTION.md"
line = 742
quote = """sh verify.sh` at the repository root derives the outcome"""

[[finding]]
id = "R3-65"
title = """Attempt counter of an in-progress task lives only in the model's context"""
disposition = "fixed"
evidence = """`tasks/<id>/attempts.log` is the authority at HEAD (goal/EXECUTION.md:362, AGENTS.md:24, GOAL.md, SPEC.md, D-093)."""
file = "goal/EXECUTION.md"
line = 537
quote = """attempts.log"""

[[finding]]
id = "R3-66"
title = """No durable post-compaction / re-prompt trigger; dirty-tree rule misfires on a mid-run re-prompt"""
disposition = "fixed"
evidence = """AGENTS.md carries the run-mode rule re-injected through the CLAUDE.md symlink; D-093 records R3-66 applied. Residual dirty-tree carve-out is tracked separately."""
file = "AGENTS.md"
line = 22
quote = """this session is the host session. After a context compaction or a re-prompt, re-read"""

[[finding]]
id = "R3-67"
title = """Final-message shape contradicts the evaluator; (A) has no first-line marker"""
disposition = "fixed"
evidence = """Fixed: the final-message shape names the exact first-line markers the evaluator reads, so outcome (A) carries a marker too."""
file = "goal/EXECUTION.md"
line = 758
quote = """Line 1"""

[[finding]]
id = "R3-68"
title = """PREFLIGHT §1 lacks host preconditions: interactive session only, and the RTK PreToolUse hook rewrites evidence commands"""
disposition = "fixed"
evidence = """Fixed: the preflight now carries the interactive-session precondition and an item proving no Bash-rewriting hook is active for the evidence commands."""
file = "goal/PREFLIGHT.md"
line = 184
quote = """PreToolUse"""

[[finding]]
id = "R3-69"
title = """crew-operator preflight.sh refuses before M1-05d mounts the profile; M1-05b<->M1-05d cycle blocks wave 3"""
disposition = "fixed"
evidence = """Fixed: the crew-operator preflight exits non-zero only for a live lock; a missing profile warns and passes, breaking the M1-05b and M1-05d cycle."""
file = "ROADMAP.md"
line = 137
quote = """WARNING"""

[[finding]]
id = "R3-70"
title = """PREFLIGHT §1 standing check carries an unresolvable <TAILROCKS_SKILLS_SHA> placeholder"""
disposition = "fixed"
evidence = """Fixed: the standing check reads the sha from the file M1-05a writes, so the placeholder is gone."""
file = "goal/PREFLIGHT.md"
line = 205
quote = """tailrocks-skills"""

[[finding]]
id = "R3-71"
title = """M1-05a..c 'inside the role' checks and jackin role validate have no runnable home"""
disposition = "fixed"
evidence = """Fixed: the procedure defines the throwaway-load home for the M1-05a..c checks, and the roadmap cells split the host role validation from the container checks."""
file = "goal/EXECUTION.md"
line = 319
quote = """Throwaway load"""

[[finding]]
id = "R3-72"
title = """M1-04a ci.yml with jackin-version pinned can never pass: jackin publishes no versioned release"""
disposition = "fixed"
evidence = """Fixed: M1-04a pins `jackin-version: latest-build`, the only value resolvable while jackin publishes no versioned release."""
file = "ROADMAP.md"
line = 135
quote = """latest-build"""

[[finding]]
id = "R3-73"
title = """review-crucible checkout in /opt is root-owned; rev-parse as agent fails with dubious ownership"""
disposition = "fixed"
evidence = """Fixed: the checkout is created and fetched as the agent user and pinned, so no dubious-ownership failure remains."""
file = "ROADMAP.md"
line = 138
quote = """review-crucible"""

[[finding]]
id = "R3-74"
title = """Builder toolchain source contradicts between roles.md and ROADMAP; M1-05a verify only satisfiable from termrock's own mise.toml"""
disposition = "fixed"
evidence = """Fixed: M1-05a pre-warms termrock's own mise.toml from a named clone, and concept/roles.md was amended to agree."""
file = "ROADMAP.md"
line = 136
quote = """termrock"""

[[finding]]
id = "R3-75"
title = """DCO sign-off hook duplicates a jackin built-in and is shadowed by its global core.hooksPath"""
disposition = "fixed"
evidence = """Fixed: sign-off comes from jackin's built-in DCO hook enabled by the session, and M1-04a asserts the image sets no core.hooksPath."""
file = "goal/PREFLIGHT.md"
line = 161
quote = """dco"""

[[finding]]
id = "R3-76"
title = """M1-05d verify says bare `jackin config` shows trusted = true; $CODEX_HOME hook contract needs a default"""
disposition = "fixed"
evidence = """Fixed: the M1-05d verify uses a real `jackin config` subcommand instead of the bare command, and the hook contract states the default Codex home."""
file = "ROADMAP.md"
line = 139
quote = """config"""
