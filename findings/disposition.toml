# Disposition of every finding in the round-3 review archive.
# Written by plan row 1.1 (K-47, D-115). Re-verified against HEAD ff96266.

[archive]
path = "analysis/bulletproof-round3-findings.md"
count = 76

[[finding]]
id = "R3-01"
title = """jackin ci.yml routes pull_request ci-required to offline velnor runner; no task switches it"""
disposition = "fixed"
evidence = """Fixed: M1-13 (ROADMAP.md) switches the-architect ci.yml pull_request to GitHub-hosted runners and M11-01a does the same for jackin, each verified by a no-self-hosted-label check (readiness plan row 2.2 landed)."""
file = "ROADMAP.md"
line = 147
quote = """resolve to GitHub-hosted `ubuntu-26.04` for `pull_request` events"""
[[finding]]
id = "R3-02"
title = """Two manifest schema bumps on one rolling PR (M3-02 v1alpha7, M4-01 v1alpha8)"""
disposition = "fixed"
evidence = """Fixed: SPEC.md names `default_agent`/v1alpha7 as the run's single schema bump and M4-01 delivers the prompt through `JACKIN_INITIAL_PROMPT` with no second bump (plan row 2.4 landed)."""
file = "SPEC.md"
line = 398
quote = """the only manifest schema bump of the run"""
[[finding]]
id = "R3-03"
title = """termrock AGENTS.md/CONTRIBUTING.md forbid branches and PRs; M10-02 only appends a clause"""
disposition = "fixed"
evidence = """Fixed: M10-02 now replaces the branch/PR prohibition in termrock AGENTS.md and CONTRIBUTING.md, and its host verify carries the negative grep (plan row 2.5 landed)."""
file = "ROADMAP.md"
line = 323
quote = """that sentence is deleted, not left standing beside a new clause"""
[[finding]]
id = "R3-04"
title = """DCO pre-push gate counts merge commits, so merge-backs break the shared branch forever"""
disposition = "open"
evidence = """goal/EXECUTION.md:413-414 still compares `grep -c '^Signed-off-by:'` with `git rev-list --count origin/main..HEAD` without `--no-merges`, while :371 and :400 still create merge commits that carry no trailer."""

[[finding]]
id = "R3-05"
title = """'rebase on origin/main first when the PR is behind' on a shared branch needs a forbidden force push"""
disposition = "open"
evidence = """ROADMAP.md:147 M1-13 and :363 M11-01a still say 'rebase on `origin/main` ... when behind' for the shared branch that goal/EXECUTION.md:363 forbids force-pushing; only goal/EXECUTION.md:388-390 became a merge."""

[[finding]]
id = "R3-06"
title = """'No Homebrew tap publish in this run' contradicts the preview.yml push run M11-01a must watch"""
disposition = "open"
evidence = """ROADMAP.md:363 still ends 'No tag, no release, no Homebrew tap' in the row whose host verify asserts the preview release `targetCommitish`, so the row forbids the release it requires."""

[[finding]]
id = "R3-07"
title = """Rolling PR and squash merge ignore jackin .github/AGENTS.md PR contract (template body, trailers)"""
disposition = "open"
evidence = """goal/EXECUTION.md:495 still opens the PR with `--title feat/managed-execution --body "Rolling PR; ...` and ROADMAP.md:363 still merges with bare `gh pr merge <n> --squash`, so jackin's own PR template and trailer rules stay unmet."""

[[finding]]
id = "R3-08"
title = """M3-04 host verify needs a daemon-dispatched container only M3-05 (its dependent) can produce"""
disposition = "fixed"
evidence = """Fixed: M3-04's host verify no longer needs a dispatched container; the restart proof moved to M3-07, which runs after M3-05 (plan row 1.3 landed)."""
file = "ROADMAP.md"
line = 198
quote = """the restart proof over a running managed container is asserted by M3-07"""
[[finding]]
id = "R3-09"
title = """M8-01 depends_on M1-03 only but its verify reads tasks/M3-07/scratch-repo.txt"""
disposition = "fixed"
evidence = """Fixed: M8-01's verify no longer references tasks/M3-07/scratch-repo.txt, so its depends_on M1-03 is consistent; the scratch-repo assertion moved to M8-03 (plan row 1.3)."""
file = "ROADMAP.md"
line = 286
quote = """gitleaks detect --no-git --source tasks/M8-01"""
[[finding]]
id = "R3-10"
title = """Proof-run tasks lack dependency edges to the dispatcher (M3-05), scratch repo (M3-07); M3-05 lacks M2-03"""
disposition = "open"
evidence = """ROADMAP.md:199 M3-05 depends_on is still 'M3-02, M3-03, M3-04, M2-04, M1-13' with no M2-03 and no `M2-03 --> M3-05` edge in the DAG (ROADMAP.md:445), although M4-04 did gain M3-07."""

[[finding]]
id = "R3-11"
title = """GOAL prompt Runnable formula has no milestone gate while goal/EXECUTION.md restricts early starts"""
disposition = "fixed"
evidence = """Fixed by D-119: the runnable predicate is one paragraph, byte-identical in GOAL.md and goal/EXECUTION.md; `python3 tools/invariant_lint.py` check d119-predicate passes at HEAD."""
file = "GOAL.md"
line = 41
quote = """Runnable predicate (D-119). A row is Runnable (D-119) iff: its status is"""

[[finding]]
id = "R3-12"
title = """M10-03 has depends_on '—' but is gated on M10-02 only in prose"""
disposition = "fixed"
evidence = """Fixed: the M10-03 depends_on cell is now `M10-02`, no longer an em dash, so the gate is an edge and not prose."""
file = "ROADMAP.md"
line = 324
quote = """| M10-03 | termrock: `TerminalPane` widget"""

[[finding]]
id = "R3-13"
title = """M1-13 sits in wave 5b with M1-03 but must wait for L6 to eject; stuck clock has no lane-cap exemption"""
disposition = "open"
evidence = """goal/EXECUTION.md:230 keeps wave 5b as 'M1-03, M1-13' with 'L6 only after M1-03 ejected', and the stuck rule at :163-170 still exempts only §4 rate limits, not lane-cap or eject waits."""

[[finding]]
id = "R3-14"
title = """M4-03 early start needs a host daemon/instance that only M2-03/M3-05 create"""
disposition = "open"
evidence = """ROADMAP.md:218 M4-03 still has depends_on M1-02 and a host verify of `jackin daemon exec <instance>` with no named instance and no daemon-start step, while ROADMAP.md:519 still lets it start as soon as M1-02 exists."""

[[finding]]
id = "R3-15"
title = """M2-08 review anchor ambiguous; pr.txt head fixed by a non-jackin task"""
disposition = "fixed"
evidence = """Fixed: §6a pins `tasks/<id>/pr.txt` to three lines (URL, head SHA reviewed, previous review SHA); line 2 is never rewritten, so the review anchor cannot drift (plan rows 1.3, 2.1)."""
file = "goal/EXECUTION.md"
line = 498
quote = """and never rewrite line"""
[[finding]]
id = "R3-16"
title = """Five verifies need op/Linear token but carry no `host (D-061):` marker"""
disposition = "open"
evidence = """ROADMAP.md:134 M1-03 (also :141, :143, :144) still has no `host (D-061):` marker, so tools/bundle.py generated the `op item get` check into the container part at tasks/M1-03/verify.sh:57 where no `op` exists; only M8-01 got the split."""

[[finding]]
id = "R3-17"
title = """Staging root `<ws>` ambiguous; container-part cwd undefined on host/subagents paths"""
disposition = "open"
evidence = """goal/EXECUTION.md:277 still stages with an unbound `<ws>` and execs the container verify with `-w /workspace`, leaving the host and subagents paths without a cwd rule."""

[[finding]]
id = "R3-18"
title = """Verifies requiring a live load/status/hardline cannot run in a non-interactive verify.sh"""
disposition = "open"
evidence = """Live/interactive verifies survive at ROADMAP.md:136 M1-05a, :138 M1-05c, :216 M4-01 ('attach still works') and :219 M4-04; only M3-01 (:194) was made non-interactive."""

[[finding]]
id = "R3-19"
title = """Linear `<org>` urlKey and app user id never filed as evidence"""
disposition = "fixed"
evidence = """Fixed by D-108 (Phase 0 row 0.14): `grep -c '<org>' goal/EXECUTION.md ROADMAP.md` prints 0 for both, and M1-10 now stores the urlKey and app user id as fields of `op://jackin/linear-workspace` plus the named non-secret evidence file `tasks/M1-10/linear-org.txt`."""
file = "concept/credentials.md"
line = 229
quote = """The Linear organization `urlKey` and the app user id are not secrets,"""

[[finding]]
id = "R3-20"
title = """M10-03 needs host-session bless of goldens before `mise run gate` can be green"""
disposition = "open"
evidence = """ROADMAP.md:324 M10-03 keeps `mise run gate` green in the container part while `mise run bless-previews` stays in the host part, so the bless/gate circularity is unchanged and goal/EXECUTION.md §5 still has no bless step."""

[[finding]]
id = "R3-21"
title = """Nine verifies still assert live daemon/Docker state or mutate Linear at verify time"""
disposition = "open"
evidence = """ROADMAP.md:200 M3-06 still verifies by GraphQL `issueUpdate` plus `docker ps --filter` emptiness, and :221 M4-06 still asserts 'The file exists in the workspace' with no container/host split or filed evidence."""

[[finding]]
id = "R3-22"
title = """M1-01 verify's shellcheck/last-echo wording is underspecified"""
disposition = "open"
evidence = """ROADMAP.md:131 M1-01 still says only that `shellcheck -s sh tasks/M1-01/verify.sh` is clean with no severity floor; tools/roadmap_compile.py:206 and concept/task-format.md:63 match it and 'severity' occurs nowhere."""

[[finding]]
id = "R3-23"
title = """M2-01 manual-run claim has no evidence file; M11-01a/M11-02 depend on `gh run list --limit 1`"""
disposition = "open"
evidence = """ROADMAP.md:166 M2-01 still ends in a manual-run claim with no `host (D-061):` marker and no filed evidence file, and :363/:364 still read the workflow conclusion from `gh run list --limit 1`."""

[[finding]]
id = "R3-24"
title = """Standing proof `op whoami` fails under desktop-app integration"""
disposition = "open"
evidence = """goal/PREFLIGHT.md:85 still gives `op whoami` as the standing proof and goal/EXECUTION.md:60 still lists '`op` with stdin closed'; the wording survives human row 2.7 in PREFLIGHT-DEFECTS.md, which names the same check."""

[[finding]]
id = "R3-25"
title = """Server preflight omits the `op` CLI and any transfer of the daemon service-account token"""
disposition = "open"
evidence = """goal/PREFLIGHT.md:281 lists only build-essential, clang and pkg-config for the server, with no `op --version`, and ROADMAP.md:365 M11-03 has no step installing `op` or transferring the daemon service-account token (`daemon.env` occurs nowhere) while its verify demands every `op://` reference resolve on the server. The fix is in goal/PREFLIGHT.md and the M11-03 scope, both outside this agent's ownership."""

[[finding]]
id = "R3-26"
title = """GitHub App preflight under-specified and contradicts credentials.md #5"""
disposition = "open"
evidence = """concept/credentials.md:135 still names the item `github-app-jackin-daemon` with upper-case `App ID`/`Installation ID`, contradicting goal/PREFLIGHT.md's per-org lower-case fields, and credentials.md:256 keeps the open #5 bullet; waits on human row 2.8 in PREFLIGHT-DEFECTS.md."""

[[finding]]
id = "R3-27"
title = """M4-05 preflight offers an API-key path no laptop-side mechanism consumes"""
disposition = "open"
evidence = """goal/PREFLIGHT.md:241-243 still offers the API-key alternative with no per-runtime env-var name while the laptop keeps `auth_forward = "sync"` for the whole run (D-090); waits on human row 2.9 in PREFLIGHT-DEFECTS.md."""

[[finding]]
id = "R3-28"
title = """gh scope proof (repo, workflow) insufficient for org-level proofs and M8-01"""
disposition = "open"
evidence = """goal/PREFLIGHT.md:112 still proves only `repo` and `workflow` scopes and no `admin:org`/`read:org` occurs anywhere, so the M8-01 org-level checks are unproved; waits on human row 2.8 in PREFLIGHT-DEFECTS.md."""

[[finding]]
id = "R3-29"
title = """Docker Hub namespace contradiction: credentials.md projectjackin/* vs docker.io/donbeave/..."""
disposition = "open"
evidence = """concept/credentials.md:145 still says Docker Hub `projectjackin/*` while ROADMAP.md:364 M11-02 publishes `docker.io/donbeave/jackin-crew-<p>:latest`; the namespace sentence lives in concept/credentials.md, outside this agent's ownership, and the two must be reconciled by its owner."""

[[finding]]
id = "R3-30"
title = """M10 crates.io item conditional on a decision no task or document makes"""
disposition = "open"
evidence = """goal/PREFLIGHT.md:258-261 still heads the section 'Before M10..M12 (crates.io, server hosts)' with the conditional publish, and ROADMAP.md:316 keeps the same conditional; no decision settles whether termrock 0.14 publishes to crates.io, so the condition needs a DECISIONS.md entry, outside this agent's ownership."""

[[finding]]
id = "R3-31"
title = """Daemon path can never run a roadmap task container verify: verify.sh and refs never delivered"""
disposition = "open"
evidence = """ROADMAP.md:219 M4-04 still takes the task verify.sh and reference files 'from the issue attachments or description' and goal/EXECUTION.md:278 stages no content-addressed bundle on the daemon path."""

[[finding]]
id = "R3-32"
title = """Issue convention agent:<lane value> contradicts M2-05 validation; daemon has no source for the per-launch account home"""
disposition = "open"
evidence = """SPEC.md:66 still reads 'label `agent:<runtime>` (until M3-01 the lane value ...)' and concept/task-format.md:18 repeats it, while ROADMAP.md:199 M3-05 still says 'choose the account home per launch' with no label source."""

[[finding]]
id = "R3-33"
title = """M8-01 is an early-start task but its verify needs tasks/M3-07/scratch-repo.txt"""
disposition = "fixed"
evidence = """Fixed: M8-01 asserts only the App installation and token plus tasks/M8-01/app.md; the scratch-repository assertion is now M8-03, which depends on M8-02 and M3-07 (plan row 1.3)."""
file = "ROADMAP.md"
line = 286
quote = """grep -c repository_selection tasks/M8-01/app.md` is 2"""
[[finding]]
id = "R3-34"
title = """M11-05/M10-06 verify demands a PR review per reviewed repository, but role repositories have no PR"""
disposition = "open"
evidence = """ROADMAP.md:327 M10-06 and :367 M11-05 still demand one Reviews-API review per reviewed repository pinned to line 2 of a single pr.txt, while D-112 (DECISIONS.md:2769) keeps role repositories on `main` with no PR."""

[[finding]]
id = "R3-35"
title = """M11-01 depends_on M1-03 makes it runnable in M1 time despite the prose gate"""
disposition = "fixed"
evidence = """Fixed: the M11-01 depends_on cell is now `M10-05`; `grep -c 'starts with M11, after' ROADMAP.md` prints 0, so the gate is no longer prose-only."""
file = "ROADMAP.md"
line = 362
quote = """| M11-01 | Runtime credentials"""

[[finding]]
id = "R3-36"
title = """Lane templates carry role-specific Docker grants although every lane serves builder, operator and reviewer tasks"""
disposition = "open"
evidence = """ROADMAP.md:147 M1-13 still writes `[docker.grants] dind = "privileged"` into the builder lane templates and concept/roles.md:132/:351 still describe the grant as per-lane; no per-role grant file exists."""

[[finding]]
id = "R3-37"
title = """Daemon ~/.claude cap: D-056/SPEC/M3-05 say 3, D-071/EXECUTION say 2, and no step configures the daemon to 2"""
disposition = "open"
evidence = """Residual half unfixed: goal/EXECUTION.md:460 step 4a still only starts or restarts the daemon and writes no `~/.claude` cap; `daemon.accounts` and `max = 2` occur nowhere. The configuring step belongs in goal/EXECUTION.md §5 step 4a, outside this agent's ownership."""

[[finding]]
id = "R3-38"
title = """Documents read first still forbid actions the run requires (D-045/D-048 the-architect as is; AGENTS.md Markdown only)"""
disposition = "open"
evidence = """AGENTS.md half is fixed; DECISIONS.md:1039-1041 D-048 still says the-architect 'is used as it is; it is not extended with project-specific tooling' with no amendment note, contradicting M1-13 (ROADMAP.md:147). The fix is an amendment in DECISIONS.md, which only the agent assigned that decision may write."""

[[finding]]
id = "R3-39"
title = """M4-04 host verify needs a Linear prompt activity nobody on the run can post (deadlock with M4-06)"""
disposition = "open"
evidence = """ROADMAP.md:219 M4-04 still requires 'a real reply on the live session reaches the PTY within one interval', while goal/EXECUTION.md:278 still says a Linear `prompt` is one only a human actor can post."""

[[finding]]
id = "R3-40"
title = """M1-10 code capture via `agent-browser url` after the connection-refused redirect"""
disposition = "open"
evidence = """ROADMAP.md:144 still reads 'the browser lands on a connection-refused page — expected; `agent-browser url` and extract `code`' with no loopback listener step anywhere."""

[[finding]]
id = "R3-41"
title = """No preflight proof that the Linear workspace plan allows a new team and ~100 more issues"""
disposition = "open"
evidence = """goal/PREFLIGHT.md:155-157 still carries only the workspace-admin item; no plan or seat-headroom check exists and ROADMAP.md M1-09 only creates team JACKIN; waits on the human Linear rows in PREFLIGHT-DEFECTS.md."""

[[finding]]
id = "R3-42"
title = """M1-12 never attaches verify.sh/refs; M4-04 pre-fetches them from issue attachments"""
disposition = "open"
evidence = """ROADMAP.md:219 M4-04 still pre-fetches verify.sh and refs 'from the issue attachments or description' while M1-12 (:146) creates issues whose description is TASK.md only and attaches nothing."""

[[finding]]
id = "R3-43"
title = """M3-05 host verify asserts a transient `pending` session the daemon turns `active` in the same tick"""
disposition = "open"
evidence = """ROADMAP.md:199 M3-05 host verify still asserts that a `pending` session appears within one interval, a transient state the daemon flips to active in the same tick."""

[[finding]]
id = "R3-44"
title = """`agentSessions` tick read not scoped to this app user"""
disposition = "fixed"
evidence = """Fixed: M2-02's scope now filters the agentSessions read to this app user by `appUser` id resolved from `viewer`, never a workspace-wide read."""
file = "ROADMAP.md"
line = 167
quote = """agentSessions(filter: {appUser: {id: {eq: $appUserId}}})"""
[[finding]]
id = "R3-45"
title = """M1-09 leaves type/position of `review` and `merging` states unspecified; M2-04 first-`started` rule"""
disposition = "open"
evidence = """ROADMAP.md:143 M1-09 still says only 'a review state and a merging state per team' with no type, position or states.json, and :169 M2-04 still says 'move the issue to the first `started` state'."""

[[finding]]
id = "R3-46"
title = """Auto-disabled webhook may drop the app's agent status mid-run; nothing re-checks it"""
disposition = "open"
evidence = """ROADMAP.md:141 M1-07 still enables the webhook on an unreachable URL noting that Linear auto-disables it, with no re-check or re-enable step and no app-url artifact."""

[[finding]]
id = "R3-47"
title = """Stale Chromium SingletonLock in the shared operator profile kills later crew-operator launches"""
disposition = "open"
evidence = """goal/EXECUTION.md:277 Teardown is still `tmux kill-session` plus `jackin eject` with no SingletonLock removal and no `agent-browser close --all`, and concept/roles.md:133 still only refuses on a live lock PID. Both files are outside this agent's ownership; the fix is a Teardown line in goal/EXECUTION.md §4."""

[[finding]]
id = "R3-48"
title = """Reserve rule leaves Claude-lane tasks neither runnable, waiting, nor blocked"""
disposition = "fixed"
evidence = """Fixed: the D-119 runnable predicate folds the §4 reserve rule into runnability, so a reserve-refused Claude-lane row is simply `ready` and not runnable, and cap waits have the `resource-waiting` status (tools/state.py) (plan rows 1.4, 1.9)."""
file = "goal/EXECUTION.md"
line = 191
quote = """plus the §4 reserve rule of `goal/EXECUTION.md`"""
[[finding]]
id = "R3-49"
title = """Hop target lanes share ~/.claude; 'next lane busy' undefined so hops stall or misfile as `waiting`"""
disposition = "open"
evidence = """goal/EXECUTION.md:314 still reads 'A chain fully throttled makes the row `waiting`' with no 'next lane busy' rule and no use of `resource-waiting`."""

[[finding]]
id = "R3-50"
title = """L5/L6 reset times unreadable: host snapshot ignores CODEX_HOME; Codex session slot is a ~5-day window"""
disposition = "host-probe-required"
evidence = """The doc already hedges (goal/EXECUTION.md:257-259 'where exposed … else the runtime limit message, else 30 minutes'), so whether per-home Codex buckets are readable is settled only live: `CODEX_HOME=~/.codex-l5 jackin usage host snapshot --agent codex --format json | jq '.buckets'` compared with the same command against `~/.codex`. The >24 h weekly clause at goal/EXECUTION.md:271-272 stays as written until that probe runs."""

[[finding]]
id = "R3-51"
title = """Every container attempt is a cold cargo build; per-lane cargo/target volumes exist nowhere"""
disposition = "open"
evidence = """goal/EXECUTION.md:277 still creates the workspace with a single `--mount ~/.jackin/managed/<id>`; the per-lane cargo and target volumes promised at concept/roles.md:138 exist nowhere and `CARGO_TARGET_DIR` occurs nowhere."""

[[finding]]
id = "R3-52"
title = """Session-side 5-second model polling of tmux panes drains the ~/.claude bucket the reserve rule protects"""
disposition = "open"
evidence = """goal/EXECUTION.md:277 still polls `tmux capture-pane` every 5 s for a 15-minute budget; tools/supervisor.sh watches only the coordinator process, not task panes."""

[[finding]]
id = "R3-53"
title = """Reserve rule reads only session/weekly slots; the model-specific bucket is invisible"""
disposition = "host-probe-required"
evidence = """goal/EXECUTION.md:262-271 still reads only the `session` and `weekly` buckets; whether a per-model bucket exists (and under which field) is settled only by: jackin usage host snapshot --agent claude --format json"""

[[finding]]
id = "R3-54"
title = """Six-container cap and same-account-home check count DinD sidecars and cannot see account homes"""
disposition = "open"
evidence = """goal/EXECUTION.md:185 still checks `docker ps` with no label filter and no account-home labelling, so DinD sidecars keep counting against the cap and account homes stay invisible."""

[[finding]]
id = "R3-55"
title = """Headless launch (M3-01) yields a container with no agent until an attach Hello"""
disposition = "fixed"
evidence = """Fixed: M3-01's host verify proves the headless launch with `load_options_launch` and files `jackin status <instance id> --format json` into tasks/M3-01/status.json; no attach is part of the proof, and `grep -lE 'hardline|tmux attach|--latest|newest' tasks/*/verify.sh` is empty."""
file = "ROADMAP.md"
line = 194
quote = """never an interactive attach"""
[[finding]]
id = "R3-56"
title = """Per-task saved workspace has no /workspace: workdir and mount mirror the host path"""
disposition = "open"
evidence = """goal/EXECUTION.md:277 still creates the workspace with `--workdir ~/.jackin/managed/<id> --mount ~/.jackin/managed/<id>` while the same row execs `-w /workspace`, so the container verify cwd cannot exist. The fix is one command in goal/EXECUTION.md §4, outside this agent's ownership."""

[[finding]]
id = "R3-57"
title = """M1-05d primary verify calls `jackin config env list --format json`, a flag that may not exist"""
disposition = "host-probe-required"
evidence = """ROADMAP.md:137 still names it primary and DECISIONS.md:2361 D-090 (3) makes it authoritative; settled by: jackin config env list --role donbeave/crew-operator --format json"""

[[finding]]
id = "R3-58"
title = """Container-name lookup grep -- "-task-<id slug>-" can never match the compacted workspace component"""
disposition = "open"
evidence = """goal/EXECUTION.md:277 still records the container with `docker ps --format '{{.Names}}' | grep -- "-task-<id slug>-"`, which the compacted workspace component cannot match."""

[[finding]]
id = "R3-59"
title = """Reserve rule reads .session.remaining_percent / a weekly bucket from a JSON that has neither key"""
disposition = "host-probe-required"
evidence = """goal/EXECUTION.md:266-270 and goal/PREFLIGHT.md:80 unchanged; the actual field names (category session/long_range vs a session key) are settled only by: jackin usage host snapshot --agent claude --format json"""

[[finding]]
id = "R3-60"
title = """M1-13 verify expects jackin usage cache accounts to list four accounts, but a load never writes the host cache"""
disposition = "host-probe-required"
evidence = """ROADMAP.md:145 still asserts `jackin usage cache accounts --format json` lists four accounts after throwaway loads; settled by: jackin usage cache accounts --format json before and after a load, plus jackin usage <container> accounts --sync-host-cache --format json"""

[[finding]]
id = "R3-61"
title = """jackin workspace delete task-<id> is not a command; the subcommand is remove"""
disposition = "fixed"
evidence = """Fixed by Phase 0 row 0.13: `grep -rn 'workspace delete' --include='*.md' .` returns nothing, and goal/EXECUTION.md now ends the task lifecycle with `jackin workspace remove`."""
file = "goal/EXECUTION.md"
line = 277
quote = """jackin workspace remove"""

[[finding]]
id = "R3-62"
title = """Runner has a third terminal outcome the plan never handles: goal CLEARED on context limit or impossible verdict"""
disposition = "fixed"
evidence = """Fixed: tools/supervisor.sh re-invokes the coordinator after any exit whose log carries neither GOAL COMPLETE nor GOAL BLOCKED, context-limit clears included, after reconciling the durable store (plan rows 1.12, 1.7)."""
file = "GOAL.md"
line = 61
quote = """`tools/supervisor.sh resume` restarts you after a non-terminal exit."""
[[finding]]
id = "R3-63"
title = """Runner re-prompt after BLOCKED silently opens a new attempt epoch for every exhausted: row"""
disposition = "fixed"
evidence = """Unchanged at HEAD: an `exhausted:` row is closed by the human alone (goal/EXECUTION.md:45), repeated for the section 6 re-prompt."""
file = "goal/EXECUTION.md"
line = 45
quote = """and is closed by the human"""

[[finding]]
id = "R3-64"
title = """Outcome (B) and half of (A) are prose the evaluator cannot check"""
disposition = "open"
evidence = """goal/EXECUTION.md:582 still defines outcome (B) as 'status: PENDING <n> remaining', a string the rewritten oracle never prints (verify.sh:261 prints bare `status: PENDING`; the blocked verdict is `status: BLOCKED HUMAN` at verify.sh:265)."""

[[finding]]
id = "R3-65"
title = """Attempt counter of an in-progress task lives only in the model's context"""
disposition = "fixed"
evidence = """`tasks/<id>/attempts.log` is the authority at HEAD (goal/EXECUTION.md:362, AGENTS.md:24, GOAL.md, SPEC.md, D-093)."""
file = "goal/EXECUTION.md"
line = 448
quote = """Append the attempt to `tasks/<id>/attempts.log`"""

[[finding]]
id = "R3-66"
title = """No durable post-compaction / re-prompt trigger; dirty-tree rule misfires on a mid-run re-prompt"""
disposition = "fixed"
evidence = """AGENTS.md carries the run-mode rule re-injected through the CLAUDE.md symlink; D-093 records R3-66 applied. Residual dirty-tree carve-out is tracked separately."""
file = "AGENTS.md"
line = 22
quote = """this session is the host session. After a context compaction or a re-prompt, re-read"""

[[finding]]
id = "R3-67"
title = """Final-message shape contradicts the evaluator; (A) has no first-line marker"""
disposition = "fixed"
evidence = """goal/EXECUTION.md now fixes the four-part final-message shape with the first-line marker; GOAL.md and README.md mirror it (D-093)."""
file = "goal/EXECUTION.md"
line = 587
quote = """1. Line 1, alone: `GOAL COMPLETE` or `GOAL BLOCKED`."""

[[finding]]
id = "R3-68"
title = """PREFLIGHT §1 lacks host preconditions: interactive session only, and the RTK PreToolUse hook rewrites evidence commands"""
disposition = "open"
evidence = """goal/PREFLIGHT.md still states no interactive-session precondition and never mentions the RTK `PreToolUse` hook that rewrites evidence commands (grep for PreToolUse and rtk returns nothing)."""

[[finding]]
id = "R3-69"
title = """crew-operator preflight.sh refuses before M1-05d mounts the profile; M1-05b<->M1-05d cycle blocks wave 3"""
disposition = "open"
evidence = """ROADMAP.md:137 M1-05b still refuses 'with a message naming the M1-06 re-login' and its throwaway load carries no profile mount, so the M1-05b/M1-05d cycle is unbroken."""

[[finding]]
id = "R3-70"
title = """PREFLIGHT §1 standing check carries an unresolvable <TAILROCKS_SKILLS_SHA> placeholder"""
disposition = "open"
evidence = """goal/PREFLIGHT.md:150 still carries the unresolvable `<TAILROCKS_SKILLS_SHA>` placeholder and ROADMAP.md:136 still names no file to resolve it from."""

[[finding]]
id = "R3-71"
title = """M1-05a..c 'inside the role' checks and jackin role validate have no runnable home"""
disposition = "open"
evidence = """goal/EXECUTION.md §4 still has no 'Throwaway load' paragraph and ROADMAP.md:136-138 verify cells still read a bare 'inside: …' with no container source for `jackin role validate`."""

[[finding]]
id = "R3-72"
title = """M1-04a ci.yml with jackin-version pinned can never pass: jackin publishes no versioned release"""
disposition = "open"
evidence = """ROADMAP.md:135 M1-04a still pins `jackin-version` on `jackin-project/jackin-role-action@<sha>` while ROADMAP.md:196 and concept/roles.md:284 assume the `latest-build` validator; jackin publishes no versioned release."""

[[finding]]
id = "R3-73"
title = """review-crucible checkout in /opt is root-owned; rev-parse as agent fails with dubious ownership"""
disposition = "open"
evidence = """concept/roles.md:131 and ROADMAP.md:138 still carry the bare `git init /opt/review-crucible … checkout FETCH_HEAD` with no `install -d -o agent`, no `safe.directory` and no pin file, so `git rev-parse` as agent still fails on dubious ownership."""

[[finding]]
id = "R3-74"
title = """Builder toolchain source contradicts between roles.md and ROADMAP; M1-05a verify only satisfiable from termrock's own mise.toml"""
disposition = "open"
evidence = """concept/roles.md:126 still derives the builder mise pre-warm from jackin's mise.toml while ROADMAP.md:136 says termrock's toolchain (D-048) and names no clone."""

[[finding]]
id = "R3-75"
title = """DCO sign-off hook duplicates a jackin built-in and is shadowed by its global core.hooksPath"""
disposition = "open"
evidence = """ROADMAP.md:135 still ships githooks/prepare-commit-msg with `git config --system core.hooksPath /opt/jackin-role/githooks`, echoed at concept/roles.md:344, goal/EXECUTION.md:410 and DECISIONS.md:2344-2345; no `jackin config git dco` exists, so the hook stays duplicated and shadowed."""

[[finding]]
id = "R3-76"
title = """M1-05d verify says bare `jackin config` shows trusted = true; $CODEX_HOME hook contract needs a default"""
disposition = "open"
evidence = """ROADMAP.md:139 M1-05d still asserts '`jackin config` shows `trusted = true` for the three selectors' and no `CODEX_HOME:-$HOME/.codex` default appears, so the verifier stays ill-formed and the hook contract undefaulted."""
