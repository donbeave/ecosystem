# Synthetic canary task (readiness plan Phase 3). Written by tools/canary.sh,
# not by tools/bundle.py: it has no ROADMAP.md row on purpose.
id = "CANARY-01"
milestone = "CANARY"
title = "Rehearse the delivery path end to end"
repo = "donbeave/ecosystem"
repos = [
  "ecosystem",
]
branch = "managed/readiness-2026-08-28/CANARY-01"
base = "main"
role = "host"
runtime = "host"
lane = "L0"
delivery = "canary"
size = "S"
depends_on = []

[limits]
attempts = 1

[verify]
script = "verify.sh"
has_container_part = false
container = []
host = [
  "sh tasks/CANARY-01/verify.sh host",
]
