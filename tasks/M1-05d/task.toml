# Generated from the ROADMAP.md row for this task by tools/bundle.py
# (D-114). Do not edit by hand; edit the roadmap row and regenerate.
id = "M1-05d"
milestone = "M1"
title = "Grant trust, create vault and operator service account, configure host bindings"
repo = ""
repos = [
  "host",
  "1Password",
]
branch = "main"
base = "main"
role = "host"
runtime = "host"
lane = ""
fallback_lane = ""
account_home = ""
delivery = "prompt"
size = "S"
depends_on = [
  "M1-05a",
  "M1-05b",
  "M1-05c",
]
decisions = [
  "D-061",
  "D-076",
  "D-078",
  "D-090",
]

[limits]
attempts = 3

[verify]
script = "verify.sh"
has_container_part = true
container = [
  "jackin config",
  "op vault list --format json | jq '[.[]|select(.name==\"jackin\")]|length'",
  "op read op://tailrocks/op-service-account-jackin-operator/credential | wc -c",
  "jackin config env list --role donbeave/crew-operator --format json",
  "grep -E 'OP_SERVICE_ACCOUNT_TOKEN *= *{.*on_demand *= *true' \"${JACKIN_CONFIG_DIR:-$HOME/.config/jackin}/config.toml\"",
  "test ! -e ~/.jackin/config.toml",
  "jackin config trust list",
  "jackin load donbeave/crew-<p> --dry-run --format json | jq -r .data.role",
  "tmux",
  "script",
]
host = []

[[evidence]]
path = "verify.container.out"
part = "container"
contains = "status: DONE"
